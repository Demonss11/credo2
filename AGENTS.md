# AGENTS.md — репозиторий прототипа CREDO

Инструкция для агентов, работающих в **этом репозитории**: `prototypes/credo2` —
корень проекта и агентных сессий. Продуктовые DAR-документы — уровнем выше
(см. «Внешние рамки»).

## Что это за проект

**CREDO** — прототип DAR: минимальный язык банковских правил + MCP-сервер
(черновики в песочнице, публикация версий в git, опциональный REST).

- Продуктовая рамка — **DAR** (декларативный DSL кредитного конвейера).
- Исторический прототип `prototypes/credo` (in-memory песочница, инструменты
  `rule_*`/`db_*`) лежит рядом (`../credo`) и не развивается.

## Карта репозитория

| Путь | Что |
|---|---|
| `src/` | крейт: `main.rs` (вход), `lib.rs` (публикация/манифест/кэш), `mcp.rs`, `rest.rs`, `core.rs` (язык и исполнение) |
| `tests/` | интеграционные тесты: `publish.rs`, `rest.rs`, `features_inventory.rs` |
| `docs/` | документация: SPEC, GRAMMAR, журнал Q/D, требования, задачи; карта — `docs/README.md` |
| `.opencode/agents/` | рабочая группа агентов (ниже) |
| `.opencode/rules/` | правила ролей (`git-workflow.md`) |
| `.opencode/memory/`, `.opencode/mail/` | операционные данные ролей: память и лента задач (в git, не канон) |
| `.opencode/skills/rust-skills/` | skill по Rust для роли `rust-expert` |
| `target/release/credo2.exe` | бинарник MCP/REST |
| `.credo/` | данные CREDO: песочница и bare-git публикаций (вне git) |

## Рабочая группа агентов

Сессия по умолчанию стартует с **`lead`** (лидер команды); роли:

| Агент | Роль | Пишет в |
|---|---|---|
| `lead` | лидер: размер задачи (S/M/L), маршрут, вызовы ролей, возвраты на доработку, вопросы владельцу | лента задачи, своя память |
| `migrator` | ведёт журнал Q/D: перенос `Qx` из архива, новые Q/D, сверка с кодом, задачи | `docs/questions/**`, `docs/decisions/**`, `docs/tasks/**`, `docs/TRACEABILITY.md`, `docs/OPEN_QUESTIONS.md`, `docs/SPECIFICATION.md` (§10) |
| `docs-writer` | документация: требования, SPEC, GRAMMAR, BRIEF, README, CHANGELOG; статусы задач (🚧 при взятии, ✅ после приёмки) | `docs/**` (кроме журнала и `docs/reviews/**`) |
| `coder` | код задачи: `src/**` и юнит-тесты в модулях; тесты не запускает | `src/**`, `Cargo.toml` |
| `rust-expert` | идиоматика Rust: правки `src/**` без изменения поведения; skill `rust-skills` | `src/**` |
| `tester` | владелец `tests/**`: добавляет интеграционные тесты по сценариям; не запускает | `tests/**` |
| `validator` | единственная роль с `cargo test` и полным DoD: покрытие, приёмка, возврат на доработку | `docs/reviews/**` |
| `researcher` | внешние аналоги и стандарты; вне кода, журнала и приёмки | `docs/research/**` |
| `git` | git-операции: коммиты пакетом после подтверждения, идемпотентность | лента задачи, своя память |
| `auditor` | служебная зона: правки `AGENTS.md`, `.opencode/**`; независимый аудит «инструкция ↔ права» до коммита; `mode: all` | `AGENTS.md`, `.opencode/agents/**`, `.opencode/rules/**`, своя память |

### Цикл задачи

1. `lead` принимает задачу, определяет **размер** (S/M/L, ниже) и открывает
   ленту `.opencode/mail/<T-XX>.md`; `docs-writer` ставит 🚧 в карточке и сводке.
2. Класс **S**: `coder` → `validator` → `git`; классы **M/L**: `coder` →
   `rust-expert` → `tester` → `validator`; класс **L** дополнительно проходит
   аудит `auditor` до коммита, при внешних зависимостях — `researcher`.
3. `coder` реализует; компиляция (`fmt`/`check`/`clippy`) — да, тесты — нет.
4. `rust-expert` доводит идиоматику без изменения поведения.
5. `tester` добавляет недостающие тесты в `tests/**` и компилирует их
   (`cargo check --all-targets`).
6. `validator` оценивает покрытие, прогоняет полный DoD (`cargo test --all`) и
   либо возвращает `coder` с фактами (`lead` маршрутизирует, номер итерации —
   в ленте), либо принимает.
7. После приёмки `docs-writer` закрывает статусы, `git` коммитит пакетом
   (подтверждение пользователя — один раз), `lead` отчитывается.

Роли не общаются напрямую: результаты и вопросы — через `lead` и ленту задачи.
Другие маршруты: журнал (`Qx` — перенос или новая запись) → `migrator` →
`validator`; документы → `docs-writer` → `validator`; исследование →
`researcher` → `lead` (обзоры не канон).

### Размерные маршруты

Класс фиксируется `lead` в ленте с обоснованием:

- **S** — один файл/модуль, канон не затронут: `coder → validator → git`;
  `rust-expert`/`tester` — по необходимости (обосновать в ленте);
- **M** — код и тесты: полный маршрут без аудита;
- **L** — канон, права, несколько подсистем: полный маршрут + `auditor` до
  коммита + `researcher` при внешних зависимостях.

### Память и почта

- **Память роли** — `.opencode/memory/<роль>.md`: контекст, решения, находки,
  «следующее действие». Читай первым при возврате к задаче; чекпойнт — перед
  завершением и до/после тяжёлой операции. Файла нет — создай по своему шаблону.
- **Почта задачи** — `.opencode/mail/<T-XX>.md`: общая лента; `lead` открывает
  её при взятии задачи, роли дописывают отчёт в конец (append):

  ```markdown
  ## <роль> · <дата> · <статус>

  - Сделано: <1–2 строки со ссылками>
  - Проверки: <команды → результат>
  - Дальше / риски: <…>
  ```

- **До и после тяжёлой операции** (полный прогон тестов, сборка, push): отчёт в
  ленту и память — **до**, дополнение результатом — **после**.
- Один писатель на файл; чужие записи не переписываются. Память и почта — не
  канон (Q41): краткая выжимка, не пересказ; живут в git как рабочие данные.

### Лимиты шагов

У каждой роли во фронтматтере задан `steps` — предел шагов модели. Достигнут:
роль останавливается, пишет чекпойнт и краткий отчёт; продолжение — новым
вызовом (тем же `sessionID`) с опорой на память и ленту. Бюджеты чтения — в
ролях (численно).

### Служебная зона и аудит

`.opencode/**` и `opencode.json` — служебная зона владельца. Правки канона
агентов (`AGENTS.md`, `.opencode/agents/**`, `.opencode/rules/**`) вносит
**`auditor`**: как суб-агент — по задаче `lead`, отдельной сессией — для аудита;
`lead` канон не редактирует. Служебная (сервисная) сессия — сессия `auditor` по
мета-задаче. До коммита изменений системы агентов проводится независимый аудит
«инструкция ↔ права». Headless-запуск роли:
`opencode run --agent <роль> --model <модель роли>`.

Shell-команды давай одиночными (без `;`, пайпов и перенаправлений); отклонённую
команду сузь до разрешённой и повтори.

Процесс ведения журнала — `docs/BRIEF.md`; реестр задач — `docs/tasks/README.md`.

## Документы и решения

Политика Q41 («один факт — один канон»):

| Документ | Канон чего |
|---|---|
| `docs/README.md` | карта документации и канонов |
| `docs/SPECIFICATION.md` | целевая архитектура; §10 — краткие формулировки решений |
| `docs/questions/` + `docs/decisions/` | полный контекст «вопрос → решение» (правила — `docs/BRIEF.md`, связи — `docs/TRACEABILITY.md`) |
| `docs/OPEN_QUESTIONS.md` | архив до конца миграции журнала: очередь и указатели |
| `docs/features/README.md` | статусы, приоритеты и счётчики сценариев (проверяет `tests/features_inventory.rs`) |
| `docs/GRAMMAR.md` | язык v0.1 (EBNF включён) |
| `docs/tasks/` | реестр задач по коду (Источник — `Dn`/`Qn` журнала) |
| `docs/CHANGELOG.md` | хронология прототипа |
| `../../DECISIONS.md` | сквозные архитектурные ADR DAR (уровень рабочего пространства) |

Если код расходится с принятым решением — это фиксируется статусом 🟡 требования
(`docs/features/README.md`) и задачей в `docs/tasks/`, а не правится «на ходу».
Вместо копии канона — ссылка.

## CREDO — где что и как

- Сборка (release): `cargo build --release` → `target/release/credo2.exe`.
- MCP-сервер `credo` настроен в `opencode.json` этого репозитория; рабочая
  директория — корень репозитория, данные — `.credo/`.

Проверить статус подключения:

```sh
opencode mcp list
# ожидаем: ✓ credo  connected
```

## Как вызывать инструменты CREDO

Инструменты доступны агенту в Code Mode через `execute`; в каталоге имена идут с `_`
вместо точек: `tools.credo.check_create({ source })`.

| Инструмент | Назначение |
|---|---|
| `tools.credo.check_create({ source })` | Создать/перезаписать черновик из текста правила |
| `tools.credo.check_list_drafts()` | Список черновиков |
| `tools.credo.check_get_draft({ name })` | Показать черновик |
| `tools.credo.check_test({ name, input })` | Исполнить черновик на входных данных |
| `tools.credo.check_delete_draft({ name })` | Удалить черновик |
| `tools.credo.check_publish({ name, version, published_by? })` | Опубликовать версию: создаёт ветку `publish/{name}-{version}` |
| `tools.credo.check_deprecate({ name, version, reason })` | Пометить версию deprecated (пишет в `main`) |
| `tools.credo.check_list_published()` | Список опубликованного из `main` |
| `tools.credo.check_rebuild_manifest()` | Пересобрать `manifest.json` в `main` и перезагрузить кэш |

### Пример полного цикла

```js
const source = `Правило МинимальныйВозраст {
  Если (Клиент.Возраст < 21) {
    Решение = Отказ;
    Причина = "Возраст меньше 21";
  }
}`;

await tools.credo.check_create({ source });
await tools.credo.check_test({
  name: "МинимальныйВозраст",
  input: { "Клиент.Возраст": 19 },
});
// => { rule_name: "МинимальныйВозраст", condition: "Клиент.Возраст < 21",
//      matched: true, decision: "Отказ", reason: "Возраст меньше 21", actual_value: 19 }

// Публикация создаёт ветку publish/МинимальныйВозраст-1.0.0, но `main` не меняет.
// Merge ветки в `main` — git-командой из поля `next_step` ответа, затем:
await tools.credo.check_publish({ name: "МинимальныйВозраст", version: "1.0.0" });
await tools.credo.check_rebuild_manifest();
```

## Синтаксис правила

```dar
Правило ИмяПравила {
  Если (Поле < Значение) {
    Решение = Отказ;
    Причина = "текст причины";
  }
}
```

Поддерживаются:

- имя правила — одно слово (буквы/цифры/`_`, включая кириллицу);
- условие — **ровно одно сравнение**: `<`, `>`, `==`, `!=`;
- поле — любое имя, точки допускаются (`Клиент.Возраст`, `БКИ.Просрочки_12м`);
- значение — число (`21`, `3.5`) или строка;
- решение — одно слово (`Отказ`, `Одобрено`, ...);
- причина — строка в двойных кавычках (необязательна).

Строковые значения сравниваются только через `==` и `!=`.

## Важные особенности и подводные камни

1. **Состояние персистентно.** Черновики — `.credo/sandbox.json` в корне репозитория;
   опубликованные версии — bare-git `.credo/published-repo`, источник истины — ветка `main`.
   Перезапуск сервера состояние не теряет (в отличие от исторического `prototypes/credo`).
2. **Публикация — через ветку.** `check.publish` создаёт `publish/{name}-{version}` и не трогает
   `main`; до merge версия не видна ни REST, ни манифесту. Merge ветки в `main` — командой из поля
   `next_step` ответа; выполняет его человек (git-роль агентов ветки публикаций не мержит).
   После merge — `check.rebuild_manifest` (либо подождать: кэш REST обновится за ~2 с).
3. **Правила semver при публикации.** Дубликат версии и downgrade отклоняются; изменение входов
   контракта требует поднять MAJOR.
4. **Отсутствующее поле — строгая ошибка (Q8), а не `0`.** Несовместимые типы — тоже ошибка
   (Q9). В MCP это `isError`, в REST — 422 `evaluation_failed`. На вход подавайте реальные поля.
5. **Deprecated-версия не исполняется:** REST — 410 `version_deprecated`; active-эндпоинт при
   отсутствии active-версии — 409 `activation_unavailable`; в манифесте `active` — пустая строка,
   если все версии deprecated.
6. **Язык правил намеренно минимален.** Нет `И`, `Или`, `Но`, ветвлений, вложенных условий,
   списков, приоритетов и политик. Если задача требует этого — сначала расширьте
   `parse_rule`/`evaluate_rule` в `src/core.rs`, потом меняйте правила.
7. **Парсер — на регулярных выражениях**, не полноценный грамматический разбор. Формат нужно
   соблюдать буквально (ключевые слова, скобки, `;`).
8. **Обработка запросов конкурентна.** Сервер может ответить не в порядке отправки. Если нужна
   строгая последовательность — делайте вызовы последовательно (`await` один за другим), не пачкой.
9. **REST включается флагами** `--rest` / `--addr <ADDR>` (по умолчанию — только MCP/stdio;
   приоритет `--no-rest` > `--addr` > `--rest`). Адрес по умолчанию `127.0.0.1:8080`,
   Swagger — `/docs`, спека — `/openapi.json`. Ключ — `--api-key`/`CREDO_API_KEY` (заголовок
   `x-api-key`); без ключа API открыт (режим демо).

## Гигиена и чтение

- Обход дерева — по правилу `.opencode/rules/workspace.md`: узкие `glob`/`rg`
  по `src/`, `tests/`, `docs/`; не обходить `target/`, `node_modules/`,
  `.credo/`, `.git/`.
- Большие документы (`docs/OPEN_QUESTIONS.md`, `docs/SPECIFICATION.md`) читать
  по карте заголовков (`rg -n "^#{1,3} " <файл>`) и точечно, а не целиком.
- Методика ревью и приёмки — `.opencode/rules/review.md`.
- Быстрая проверка относительных ссылок — `docs/BRIEF.md` §9.

## Сборка, тесты и пересборка

```sh
cargo build --release

# DoD прототипа (из корня репозитория; полный прогон тестов — только validator):
cargo fmt --check
cargo clippy --all-targets -- -D warnings
cargo test --all
```

После пересборки `credo2.exe` MCP-сервер нужно переподключить (перезапустить службу,
`opencode service restart`), иначе будет работать старая версия.

## Git

- Конвенции коммитов — `.opencode/rules/git-workflow.md`; git-операции выполняет
  роль `git`: пакет коммитов пользователь подтверждает один раз (через `lead`,
  до вызова `git`), изменяющие команды идемпотентны, push — с таймаутом ≥ 5 мин.
- Коммит — после успешной приёмки (`validator`), одна запись/задача — один коммит.

## rust-analyzer

В `opencode.json` настроен MCP-сервер `rust-analyzer` с workspace — этот репозиторий.
Если инструмент падает с `rust-analyzer exited before responding`, проверьте, что установлен компонент:

```sh
rustup component add rust-analyzer
```

## Внешние рамки

- DAR-документы (`concept.md`, `SCOPE.md`, `NON_GOALS.md`, `ROADMAP.md`,
  `open-questions.md`) и корневые `AGENTS.md`/`opencode.json` — уровень рабочего
  пространства (`../..`); работа с ними — после демо, в этом репозитории их
  содержание не дублируется.
- Внешний `../../DECISIONS.md` (сквозные ADR DAR, другой проект) правит только
  `docs-writer` при согласовании; в таблицу ролей и «Пишет в» он не выносится
  (решение владельца, 2026-09-26).
