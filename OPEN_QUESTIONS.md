# Открытые вопросы: `SPECIFICATION.md` ↔ `features/`

Документ фиксирует расхождения между `SPECIFICATION.md` (целевая архитектура)
и `features/*.feature` + `features/README.md` (требования), а также с текущим
кодом `credo2`. Цель — до начала разработки принять решения, чтобы каждая
формулировка жила в одном месте и в одном виде.

**Как пользоваться.** Для каждого вопроса впишите ответ в блок «Решение»
(кто и когда). После решения обновите документы из поля «Обновить».
Нумерация сквозная и не зависит от порядка разделов: Q42 добавлен позже
остальных.

Легенда приоритета:

- 🔴 **Блокер** — без ответа нельзя начинать соответствующую разработку.
- 🟡 **Важно** — можно начать, но решение нужно до конца MVP.
- ⚪ **Оформление** — не влияет на код, чинит противоречие в документации.
- ✅ **Решено** — вопрос закрыт, решение зафиксировано в блоке «Решение»,
  документы из поля «Обновить» синхронизированы.

---

## 0. Общие вопросы (определяют всё остальное)

### Q1. ✅ SPEC — план «с нуля» или доработка `credo2`? (решено 2026-09-24)

- **Расхождение:** `SPECIFICATION.md` §5/§8 описывает новый монорепозиторий
  `dar/` (`crates/dar-core`, `crates/lsp-dar`, `crates/credo-server`,
  `notebook/`) и 13-недельный план с Фазы 1. Фактически уже существует
  рабочий прототип `prototypes/credo2` (крейт `credo2`: `core.rs`, `lib.rs`,
  `mcp.rs`, `rest.rs`, `main.rs`), а `features/README.md` помечает часть
  требований как ✅ реализованные.
- **Вопрос:** SPEC — это задание на переписывание с нуля или целевая
  архитектура, к которой `credo2` эволюционирует?
- **Варианты:** (а) greenfield по SPEC, `credo2` — референс; (б) эволюция
  `credo2` до структуры SPEC; (в) `credo2` остаётся монолитом, SPEC
  переписывается под него.
- **Рекомендация:** (б) с честной пометкой в SPEC, что Фазы 1–4 частично
  закрыты прототипом.
- **Обновить:** `SPECIFICATION.md` §5, §8, §10; `features/README.md`.
- **✅ Решение (2026-09-24):** вариант **(б) — эволюция `credo2`**, не
  greenfield. Монорепозиторий `dar/` из SPEC — целевая структура, к которой
  `credo2` перестраивается поэтапно: сначала выделение `dar-core` и
  `credo-server` из текущего крейта, `lsp-dar` и `notebook/` — новые крейты
  в том же workspace. Greenfield-ветка отклонена.
  - В `SPECIFICATION.md`: добавлена служебная заметка «Статус реализации»
    после заголовка; §5 переименован («Целевая структура монорепозитория»,
    с текущим маппингом `credo2::core → crates/dar-core`,
    `credo2::{mcp,rest} → crates/credo-server`); §8 — Фазы 1 и 4 помечены
    как частично закрытые прототипом (`credo2`), план переведён в режим
    «эволюция»; §10 — добавлено решение №15 (эволюция `credo2`).
  - `features/README.md`: в шапке зафиксировано, что требования
    верифицируются на эволюционирующем `credo2`, а не на отдельном
    greenfield-проекте.
  - Следствие для Q3/Q4: поскольку база — `credo2` с regex-парсером,
    решения по минимальному DSL принимаются в контексте этой базы
    (см. Q3).

### Q2. ✅ Где канонический DSL: `concept.md`, SPEC или features? (решено 2026-09-24)

- **Расхождение:** корневой `concept.md` описывает `Тогда` (строки 91, 162,
  257, 275, 309, 342), `Приоритет` (109, 212, 220, 267, 307, 365, 385) и
  doc-блоки `:::` (520–524). `SPECIFICATION.md` §1.3 перечисляет ключевые
  слова `Правило, Если, Тогда, Решение`, а §10 п. 1 заменяет `:::` на `//`.
  Фичи (`lexer.feature`, `parser.feature`, `execution.feature`) описывают
  язык без `Тогда`, но с `Приоритет: 100;` и `Решение = ...;`.
  `GRAMMAR.md` и `EBNF.md` — пустые файлы по 13/10 байт.
- **Вопрос:** какая грамматика v0.1 канонична и где она лежит?
- **Рекомендация:** зафиксировать грамматику v0.1 в `GRAMMAR.md`/`EBNF.md`
  (одно правило — один абзац), `concept.md` пометить как «видение, не
  норматив».
- **Обновить:** `GRAMMAR.md`, `EBNF.md`, `concept.md` (шапка),
  `SPECIFICATION.md` §1.3.
- **✅ Решение (2026-09-24):** каноническая грамматика v0.1 зафиксирована в
  **`GRAMMAR.md`** (создан; включает EBNF-описание — пустой `EBNF.md` более
  не нужен, дублирование запрещено). `SPECIFICATION.md` §1.3 приведён в
  соответствие: ключевое слово `Тогда` из перечня убрано (действие в v0.1
  выражается `Решение = ...;`). Файл `concept.md` в репозитории отсутствует;
  при возврате — помечать шапкой «видение, не норматив». Статусы ключевых
  слов (`Тогда`/`Приоритет`/`:::`) — таблица GRAMMAR.md §4.

### Q3. ✅ Минимальный DSL (regex, одно сравнение) или полноценный лексер/AST? (решено 2026-09-24)

- **Расхождение:** `credo2::core::parse_rule` — регулярки: одно сравнение,
  нет `Приоритет`, нет типизированных ошибок, нет позиций. Фичи
  `lexer.feature`, `parser.feature`, `execution.feature`, `errors.feature`,
  `explain.feature` описывают токены, AST, `Приоритет`, ошибки
  «Файл пуст» / «Несовместимые типы» / «ожидалась }».
  `features/README.md` называет языковые фичи «целевым состоянием DAR».
- **Вопрос:** какой парсер обязателен для MVP — regex-минимум или лексер+AST?
- **Рекомендация:** для MVP — regex-минимум, но с явным списком «язык v0.1»
  в `GRAMMAR.md`; языковые фичи перевести в раздел «целевое состояние».
- **Обновить:** `SPECIFICATION.md` §3.1/§8 (Фаза 1), `features/README.md`,
  `GRAMMAR.md`.
- **✅ Решение (2026-09-24):** вариант **regex-минимум** (текущий
  `core::parse_rule`) — парсер MVP без отдельного лексера и публичного AST.
  Язык v0.1 формально зафиксирован в `GRAMMAR.md` (EBNF + ограничения).
  Конфликта с планированным LSP нет: принята стратегия «толстый парсер,
  тонкий интерфейс» — весь доступ к `.dar` только через `parse_rule()`,
  внутренние типы (`Rule/Condition/Action`) в v0.2 обогащаются позициями и
  типизированными ошибками без смены публичного контракта (GRAMMAR.md §3).
  Файлы `lexer.feature`/`parser.feature` остаются целевым состоянием (v0.2).

### Q4. ✅ `Приоритет` входит в MVP? (решено 2026-09-24)

- **Расхождение:** `parser.feature` (сценарий 2), `execution.feature`,
  `explain.feature`, `editor.feature`, `lsp.feature` используют
  `Приоритет: 100;`; `client_explanation.feature` требует сортировку причин
  «по приоритету правил»; `credo2` не поддерживает; `features/README.md`
  (заметка 10) признаёт расхождение.
- **Вопрос:** реализуем `Приоритет` в MVP или убираем из фич?
- **Рекомендация:** если конвейер из нескольких правил не в MVP (см. Q38),
  убрать `Приоритет` из фич MVP и вернуть вместе с конвейерами.
- **Обновить:** все фичи с `Приоритет`, `SPECIFICATION.md` §7.
- **✅ Решение (2026-09-24):** `Приоритет` убран из MVP — ключевое слово не
  входит в язык v0.1 (`GRAMMAR.md` §4). В v0.1 одно правило = один файл
  (SPEC §10 п. 14), конвейеров нет (Q36), поэтому конфликтующих правил не
  возникает и порядок применения не требуется. Реализация вернётся в v0.2
  вместе с конвейерами. Фичи, использующие `Приоритет: 100;`
  (`parser.feature`, `execution.feature`, `explain.feature`,
  `editor.feature`, `lsp.feature`, `client_explanation.feature`), остаются
  целевым состоянием до переноса синтаксиса в v0.2.

### Q5. ✅ Единая легенда статусов и приоритетов (решено 2026-09-24)

- **Расхождение:** `features/README.md` использует ✅/🟡/⬜/⏸
  (реализовано/расходится/план/отложено), `SPECIFICATION.md` §6.2 —
  🔴/🟡/🟢/⏳ (обязательные/желательные/вау/отложенные). Один и тот же файл
  получает разные метки (например, `rest_api.feature` — ✅ и 🔴).
- **Вопрос:** какая система меток одна? Что означает «сделано»?
- **Рекомендация:** оставить одну таблицу в `features/README.md`:
  «статус реализации» + «приоритет MVP» отдельными колонками.
- **Обновить:** `features/README.md`, `SPECIFICATION.md` §6.
- **✅ Решение (2026-09-24):** приняты **две независимые оси** с одним
  каноном — `features/README.md`:
  - **статус реализации** (✅/🟡/⬜/⏸) — что уже работает в `credo2`;
  - **приоритет MVP** (🔴/🟡/🟢/⏳) — что и когда нужно демо.
  У каждого файла в сводных таблицах README теперь обе колонки.
  `SPECIFICATION.md` §6.1 удалил дублирующую таблицу и ссылается на README,
  §6.2 оставляет только смысл уровней и привязку к фазам.
  - **«Сделано» (✅):** поведение реализовано, сценарии не противоречат
    коду; backend покрыт автотестом либо явно помечен «ручной прогон»
    (см. «Соответствие коду» в README), UI проверяется вручную по сценарию.
    🟡 — расхождение зафиксировано в `OPEN_QUESTIONS.md`; ⬜ — план;
    ⏸ — вне MVP.
  - **Приоритеты** файлов перенесены из SPEC §6.1; файлы, которых там не
    было, размечены по здравому смыслу: `lexer` ⏳; `parser`, `execution`,
    `explain`, `errors`, `semver`, `storage_paths`, `manifest`,
    `mcp_tools`, `testing`, `lsp_notebook` 🔴; `immutability`,
    `manifest_sync`, `deprecation` 🟡.
  - **Следствие:** Q6 закрыт попутно (см. ниже).

---

## 1. Инвентаризация и цифры

### Q6. ✅ Пересчитать сводную таблицу SPEC §6.1 (решено 2026-09-24, попутно с Q5)

- **Расхождение:** SPEC: 18 файлов / 93 сценария. Факт: 36 файлов / 191
  сценарий на 2026-09-24 (после решений Q11/Q20/Q21 — 36/192, см.
  `features/README.md`). Ошибки по категориям: REST — 7 против 22
  (`evaluate` 7 + `rest_api` 10 + `rest_auth` 5); объяснимость — 3 против 7
  (`explain` 4 + `explain_full` 3); редактор — 8 против 7; LSP — 6 против
  21 (`lsp` 15 + `lsp_notebook` 6). Не упомянуты вовсе: `lexer`, `parser`,
  `execution`, `errors`, `publish_rules`, `immutability`, `storage_paths`,
  `manifest`, `manifest_sync`, `deprecation`, `semver`, `mcp_tools`,
  `testing`, `deferred`, `lsp_notebook`.
- **Вопрос:** переписываем §6.1 из фактических файлов или оставляем как
  исторический срез?
- **Рекомендация:** заменить таблицу на актуальную, генерируемую
  (скрипт подсчёта сценариев).
- **Обновить:** `SPECIFICATION.md` §6.1.
- **✅ Решение (2026-09-24):** закрыт попутно с Q5. Таблица не
  пересчитывается, а **удалена** из SPEC как дублирующая: единственный
  канон инвентаризации — `features/README.md` (актуальные счётчики — там
  же). Актуальность счётчиков проверяет тест
  `tests/features_inventory.rs`, а не ручной пересчёт.

### Q7. ✅ Термины в глоссарии (решено 2026-09-26)

- **Расхождение:** §11 не содержит терминов, которыми оперируют фичи:
  «check», «active», «supported», «kind», «service_hash», «контракт»,
  «песочница», «манифест».
- **Вопрос:** дополнить глоссарий?
- **Рекомендация:** да, добавить перечисленные термины с привязкой к
  структурам (`ManifestEntry`, `CheckMeta`, `CheckContract`).
- **Обновить:** `SPECIFICATION.md` §11.
- **✅ Решение (2026-09-26):** глоссарий пополнен **терминологическим
  каноном** (один термин — одно место, §11): 14 новых статей + уточнение
  двух существующих:
  - реестр и публикация: `check (проверка)`, `name` (с явной оговоркой
    «термин `rule_id` не используется»), `Контракт (CheckContract)`,
    `Метаданные версии (CheckMeta)`, `Песочница (sandbox)`,
    `published-repo`;
  - манифест и API: `Манифест (manifest.json)`, `ManifestEntry`,
    `active`, `supported`, `deprecated`, `service_hash`, `kind`;
  - среда: `Workspace`;
  - уточнены: `Черновик` (вычисляемые `stale`/`test_valid`, Q12/Q29) и
    `Публикация` (ветка `publish/{name}-{version}` и отдельный шаг
    `credo merge`, Q14/Q15).

  Определения сверены с кодом (`lib.rs`: `ManifestEntry`, расчёт
  `service_hash`) и решениями Q13/Q17/Q19/Q21/Q29/Q32/Q36.
  - **Заодно зафиксировано расхождение:** `publish.feature` требует в
    `meta.json` поля `display_name`, `source_hash`, `compiler_version`, но
    `CheckMeta` в коде их пока не пишет — добавлено «Следствие для кода» в
    Q13 и шапочная пометка в `publish.feature` (политика Q41: расхождения
    не «молчат»).
  - Терминологический канон: новое понятие описывается статьёй в §11, а не
    заново в разных документах.

---

## 2. Язык и семантика

### Q8. ✅ Отсутствующее поле: ошибка или `0`? (решено 2026-09-24)

- **Расхождение:** `errors.feature`, `test_draft.feature`,
  `explain_full.feature`, `batch.feature` ожидают ошибку «Неизвестное
  поле». `evaluate_rule` (`core.rs`) подставлял `0.0`, из-за чего условие
  `< 21` для отсутствующего поля **срабатывало**. `AGENTS.md` фиксировал
  текущее поведение как особенность.
- **Вопрос:** какое поведение канонично для v0.1?
- **Варианты:** (а) строго — ошибка при отсутствии поля; (б) мягко — `0`,
  но с предупреждением в объяснении; (в) явный `null`/`unknown`.
- **Рекомендация:** (а) для `check.test` и REST, потому что иначе возможны
  ложные отказы; ложные срабатывания опаснее падения.
- **Обновить:** `core.rs::evaluate_rule`, `errors.feature`, фичи с этим
  ожиданием, `AGENTS.md`.
- **✅ Решение (2026-09-24):** вариант **(а) — строгая ошибка**.
  Разделение состояний «нет параметра» и «есть параметр, но пустой»
  плановое (v0.2+, вместе со строгой типизацией), но не вопрос MVP.
  - `core.rs`: `evaluate_rule` возвращает `Result<Explanation, EvalError>`;
    `EvalError::UnknownField(поле)` с русским сообщением «Неизвестное
    поле: …» (тексты согласованы с фичами, см. также Q11).
  - `rest.rs::eval_inner`: ошибка исполнения → HTTP 422
    `{ "error": "Неизвестное поле: …" }`.
  - `mcp.rs::test`: ошибка исполнения → ошибка инструмента MCP.
  - Фичи (`errors.feature`, `test_draft.feature`, `explain_full.feature`,
    `batch.feature`, `inline_execution.feature`) уже описывали строгое
    поведение — код приведён в соответствие, изменений в фичах не
    потребовалось. `AGENTS.md` в репозитории отсутствует.
  - Тесты: `core::tests::missing_field_is_strict_error_q8`,
    `missing_field_does_not_trigger_false_positive_q8`.

### Q9. ✅ Типизация и сравнение чисел (решено 2026-09-24)

- **Расхождение:** `errors.feature` требует ошибку «Несовместимые типы» при
  `Клиент.Возраст < "двадцать"`; `evaluate_rule` молча возвращал
  `matched = false`. SPEC §10 п. 13: `f64` в MVP, `Decimal` в v0.2;
  `rust_decimal` указан в §3.1 как зависимость.
- **Вопрос:** проверяем типы на этапе парсинга/исполнения? Что с `==` для
  `f64` (точное равенство или эпсилон)?
- **Рекомендация:** ошибка типов — в MVP; для `==` зафиксировать допуск
  либо запретить `==` для чисел вне целых.
- **Обновить:** `core.rs`, `errors.feature`, `SPECIFICATION.md` §10.
- **✅ Решение (2026-09-24):** строгая статическая типизация — плановая
  (v0.2), это не вопрос MVP. В MVP принято «как в рекомендации»:
  проверка типов **на исполнении**.
  - `core.rs`: сравнение значений разных типов →
    `EvalError::TypeMismatch` («Несовместимые типы: поле … имеет тип …,
    ожидался …»); строки поддерживают только `==`/`!=`, `<`/`>` для строк
    — та же ошибка типов.
  - Равенство `f64` (`==`/`!=`) остаётся точным; эпсилон не вводится
    (риски ложных срабатываний выше). Переход на `Decimal` снимет вопрос
    в v0.2 (SPEC §10 п. 13).
  - Замечание: `errors.feature` ожидает ошибку типов на этапе парсера —
    это целевое поведение лексера/AST (Q3: regex-минимум в MVP ловит
    ошибку на исполнении); сценарий остаётся целевым состоянием v0.2.
  - Тест: `core::tests::type_mismatch_is_error_q9`.

### Q10. ✅ Словарь решений и «не сработало» (решено 2026-09-24)

- **Расхождение:** `contract_from_rule` добавлял решение `"Pass"`;
  `lsp.feature` предлагает «Отказ», «Одобрено», «РучноеРассмотрение»;
  `client_explanation.feature` — «Заявка одобрена»; фичи используют «пусто»
  (`test_draft.feature`), «решение не установлено» (`execution.feature`),
  пустую строку (код).
- **Вопрос:** канонический набор решений и представление результата
  «правило не сработало»?
- **Рекомендация:** зафиксировать enum решений в грамматике + одно
  представление «нет решения» (`null` в JSON, пустая строка в ядре — как
  сейчас), убрать `"Pass"` из контракта или объяснить его.
- **Обновить:** `core.rs`, `lsp.feature`, `client_explanation.feature`,
  `GRAMMAR.md`.
- **✅ Решение (2026-09-24):** жёсткого перечня решений в ядре **нет** —
  словарь решений определяет банк, в каждом банке он свой. Ядро
  оперирует решениями как свободными идентификаторами (`Решение = <Имя>`
  в `GRAMMAR.md` уже так и определено).
  - Форму для заполнения словаря заложим при разработке БД/справочников
    (per-bank таблица/HashMap) — вне текущего кода, не блок MVP-ядра.
  - `contract_from_rule` больше **не** добавляет `"Pass"`: в контракт
    попадают только решения, реально используемые в правиле.
  - «Правило не сработало»: единое представление — пустая строка
    `decision`/`reason` в ядре, `""` в JSON объяснения (сценарии
    «пусто»/«решение не установлено» в фичах трактуются как «пустое
    значение», код менять не нужно).
  - Подсказки LSP (`lsp.feature`) смогут брать словарь из конфигурации
    банка — учтено в плане Q36–Q39.
  - Тесты: `contract_decisions_follow_rule_vocabulary_q10`,
    `unmatched_rule_has_empty_decision_q10`.

### Q11. ✅ Язык сообщений об ошибках — русский для всего человекочитаемого;
латиница — только для машиночитаемых идентификаторов, кодов и ключей.
(решено 2026-09-25)

- **Расхождение:** фичи ожидают русские тексты («ожидалась }», «Файл пуст»,
  «Неизвестное поле», «черновик не найден»), код частично английский
  (`not found`, `missing or invalid x-api-key`, `unknown tool`).
- **Рекомендация:** русский текст + стабильный `code` в JSON ошибки.
- **Обновить:** `evaluate.feature`, `rest_api.feature`, `rest_auth.feature`
  (тексты ошибок — на русские; пути и примеры версий — по Q20).
- **✅ Решение (2026-09-25):** принцип «машина / человек» (сквозной для
  Q11, Q13, Q42):
  - человекочитаемый текст ошибки (`message`) — русский;
  - машиночитаемое (идентификаторы проверок `name`, коды ошибок, ключи
    JSON, сегменты путей) — латиница.
  Это согласуется с Q13 (идентификаторы — латиница) и Q42 (ключи
  объяснения — латиница, тексты `decision`/`reason` — русские).

  Канонические тексты и статусы (пути и ресурсы — по решению Q20):
  ```
  404  проверка не найдена: {name}
  404  версия не найдена: {name}@{version}
       — POST /checks/{name}/versions/{version}/evaluate
  410  версия выведена из эксплуатации: {name}@{version}
       — обращение к deprecated-версии
  409  активация недоступна: {name}
       — active-эндпоинт при пустом active (все версии deprecated, Q21)
  401  ошибка аутентификации: неверный или отсутствующий x-api-key
       — фиксировано решением Q22 (заголовок x-api-key)
  ```
  Статусы 410 (Gone) и 409 (Conflict) зафиксированы 2026-09-25.

  Граница с соседними вопросами (чтобы не дублировать решения):
  - Q11 = **язык** и сами тексты человекочитаемых сообщений;
  - Q23 = **формат/конверт** ошибки в JSON (структура `code`/`message`) —
    решён 2026-09-25: `{"error": {"code", "message"}}`;
  - Q22 = аутентификация: заголовок и тексты 401.

  - Ядро (`core.rs`: парсер, semver, `EvalError`), публикация (`lib.rs`) и
    MCP («неизвестный инструмент: {name}») уже соответствуют канону —
    приведены при Q8/Q9.
  - Транспорт REST требует правки: канон — «проверка не найдена: {name}»
    (в коде — «проверка '{name}' не найдена» с кавычками); 404-текст
    «версия не найдена: {name}@{version}» уже соответствует канону.
  - Исключения канона: служебные/диагностические сообщения (ошибки git в
    `lib.rs`, описания в OpenAPI-документе) остаются техническими.

### Q42. ✅ Каноническая схема объяснения и язык полей (решено 2026-09-24)

- **Расхождение:** `explain.feature` описывала поля по-русски («Правило»,
  «Приоритет», «Условие», «Значение»); `explain_full.feature` и код —
  латиницей (`rule_name`, `condition`, `actual_value`, `matched`,
  `decision`, `reason`, `priority`). При этом в `Explanation` из `core.rs`
  не было `condition`, а `matched`/`decision`/`reason` есть.
- **Вопрос:** один контракт объяснения: какие поля, на каком языке ключей,
  где живут `condition` и `priority`?
- **Рекомендация:** канон — латиница (`explain_full.feature`), добавить
  `condition`; `priority` — только если Q4 решится в пользу `Приоритет`;
  `explain.feature` переписать под тот же контракт.
- **Обновить:** `core.rs::Explanation`, `explain.feature`,
  `explain_full.feature`, SPEC (схема объяснения).
- **✅ Решение (2026-09-24):** Q4 решён не в пользу `Приоритет` (см. решение Q4), поэтому
  **канон — латиница**: ключи `snake_case` (`rule_name`, `condition`,
  `actual_value`, `matched`, `decision`, `reason`), **тексты полей
  (`decision`, `reason`) — русские** (предложение пользователя: канон —
  русский язык для содержательных значений; ключи остаются латиницей ради
  стабильного JSON-контракта; текст ошибки — канонический русский
  (Q11)).
  - `core.rs::Explanation` дополнен полем `condition` (каноническая
    строковая форма `<поле> <оператор> <значение>`,
    `condition_to_string`).
  - `priority` в схему **не входит** — вне v0.1 (Q4), вернётся в v0.2 с
    конвейерами.
  - Ошибки исполнения (Q8/Q9) — вне `Explanation` (возвращаются через
    `Result`): отдельного варианта «объяснение с ошибкой» в схеме нет.
  - `explain.feature` переписана под канон (ключи латиницей, сценарий с
    приоритетом удалён); в `explain_full.feature` убраны упоминания
    `priority`, добавлен шапочный комментарий о каноне.
  - `SPECIFICATION.md` §10: добавлено решение №21 (схема объяснения).
  - Тест: `explanation_has_latin_keys_and_condition_q42`.

---

## 3. Хранилище, публикация, git

### Q12. ✅ Единственный источник истины: `.dar`-файлы или песочница+JSON? (решено 2026-09-25)

- **Расхождение:** SPEC §1.3/§4.3: «файл на диске — источник истины»
  (`rules/*.dar`). Но жизненный цикл §7 начинается с MCP-черновика в
  `.credo/sandbox.json`, а публикация пишет `rule.json`/`contract.json`/
  `meta.json` в bare-репозиторий; `.dar`-файлы в этом цикле не участвуют
  вовсе.
- **Вопрос:** как связаны файлы workspace, черновики и опубликованные
  артефакты? Что происходит при расхождении?
- **Варианты:** (а) `.dar` — истина, черновик/публикация — производные;
  (б) песочница — истина для черновиков, `.dar` — только UI-представление;
  (в) два независимых контура в MVP.
- **Рекомендация:** определить один направленный поток
  (`файл → черновик → публикация`) и запретить обход.
- **Обновить:** `SPECIFICATION.md` §1.3, §4.3, §7; `notebook_ui.feature`,
  `editor.feature`, `file_management.feature`.
- **✅ Решение (2026-09-25):** источник истины — `.dar`-файл рабочего контура
  (`rules/`, под git). `.credo/sandbox.json` — только черновик: производный
  от файла или созданный агентом с нуля; временное состояние, которое можно
  удалить/пересоздать без потери данных; черновики не участвуют в REST
  `evaluate`. Поток строго направленный:
  `rules/*.dar → черновик → артефакт публикации`.
  - Обход потока запрещён: нельзя опубликовать то, чего нет в песочнице/файле;
    `evaluate` читает только опубликованное.
  - При расхождении (файл изменён вручную, черновик устарел) выигрывает файл;
    черновик помечается `stale` при первой операции над ним (сверка
    `source_hash` исходного `.dar`).
  - Черновик без привязанного `.dar`-файла при публикации обязан сначала
    сохранить текст в `rules/{name}.dar` — сохраняется инвариант «у каждого
    опубликованного артефакта есть исходник».
  - Точное хранение опубликованных правил для `evaluate` в MVP —
    `HashMap`/`Vec` в памяти; будущая векторная БД — только производный
    поисковый индекс, не источник истины для исполнения.

### Q13. ✅ Что именно публикуется и по какому пути? (решено 2026-09-25)

- **Расхождение:** `publish.feature`: «в ветке содержится файл
  „Правило МинимальныйВозраст 1.0.0“». `storage_paths.feature`:
  `checks/{name}/{version}/` с `rule.json`, `contract.json`, `meta.json`.
  `publish_rules.feature`: путь `checks/CreditAgeMin/2.0.0/`. SPEC §10 п. 14:
  «одно правило = один файл». Код: JSON-тройка. Всё разное.
- **Вопрос:** канонический артефакт публикации — JSON или `.dar`-текст?
  Если JSON — то где лежит исходный `.dar`?
- **Рекомендация:** оставить JSON-тройку (как в коде) и переписать
  `publish.feature`; «один файл» уточнить как «один check = один каталог
  версии».
- **Обновить:** `publish.feature`, `storage_paths.feature`,
  `SPECIFICATION.md` §10 п. 14.
- **✅ Решение (2026-09-25):** публикация — неизменяемый скомпилированный
  артефакт, а не исходный DSL-текст. Каталог `checks/{name}/{version}/`
  содержит три файла:
  - `rule.json` — исполняемое представление правила (AST или иной
    машинно-читаемый формат);
  - `contract.json` — контракт проверки: входные поля с типами и формат
    результата;
  - `meta.json` — метаданные издания: `name`, `version`, `display_name`,
    `published_at`, `published_by`, `checksum`, `source_hash`,
    `compiler_version`.
  - Исходный `.dar` остаётся источником истины в рабочем контуре; в v0.1 в
    реестр публикаций не попадает. Добавление `source.dar` в каталог версии —
    v0.2; связь артефакта с исходником обеспечивает `source_hash`.
  - `name` — машиночитаемый латинский идентификатор (например,
    `CreditAgeMin`); человекочитаемое имя — `display_name` в `meta.json`.
  - Каталог версии неизменяем: изменение правила требует новой версии.
    Формулировка «одно правило = один файл» устарела; канон — «один check =
    один каталог версии».
  - **Уточнение Q32 (2026-09-26):** структура каталога версии —
    `checks/{name}/{X}/{Y}/{Z}/` (X = major, Y = minor, Z = patch, без
    избыточности); публичный REST передаёт версию одной строкой, сервер
    маппит `{version}` → `{X}/{Y}/{Z}`. Pre-release (`1.0.0-rc.1`) — вне
    MVP (вариант пути `checks/{name}/{X}/{Y}/{Z-prerelease}/`; итог Q18:
    публикация остаётся вне MVP, сравнение — числовое). Обновлены
    `storage_paths.feature` и фичи с путём,
    `SPECIFICATION.md` §10 (решения №14/№28).
  - **Следствие для кода (зафиксировано 2026-09-26, Q7):** `CheckMeta`
    (`src/lib.rs`) пишет только `name`, `version`, `published_at`,
    `published_by`, `checksum` (+ `deprecated_at`/`deprecation_reason`);
    канонические `display_name`, `source_hash`, `compiler_version` пока не
    записываются — сценарий «Публикация сохраняет метаданные»
    (`publish.feature`) ожидает кода; отдельная задача.

### Q14. ✅ Имя ветки публикации (решено 2026-09-25)

- **Расхождение:** `publish.feature` — `checks/{name}/{version}`;
  `publish_rules.feature` и код — `publish/{name}-{version}`.
- **Вопрос:** какое имя канонично?
- **Рекомендация:** `publish/{name}-{version}` (уже реализовано и описано
  в `publish_rules.feature`).
- **Обновить:** `publish.feature`, `features/README.md` (заметка 5).
- **✅ Решение (2026-09-25):** каноническое имя ветки —
  `publish/{name}-{version}` (например, `publish/CreditAgeMin-2.0.0`), как
  в коде и `publish_rules.feature`. `checks/{name}/{version}/` — путь
  артефакта внутри реестра, `publish/{name}-{version}` — имя ветки доставки;
  использовать `checks/...` как имя ветки запрещено, чтобы не смешивать
  пространства артефактов и доставки.

### Q15. ✅ Кто и как мержит ветку в `main` (решено 2026-09-25)

- **Расхождение:** SPEC §7 показывает «git commit+branch» и сразу
  `POST /evaluate`, без шага merge. MCP-ответ предлагает ручную команду
  `git update-ref refs/heads/main ...`. `deferred.feature` откладывает
  настоящий PR через GitHub API. UI (`git_integration.feature`) содержит
  кнопку «Опубликовать» и «Commit», но непонятно, к какому репозиторию они
  применяются.
- **Вопрос:** минимальный цикл публикации в MVP: кто переводит ветку в
  `main` — человек командой, Notebook или сервер?
- **Рекомендация:** для MVP — полуручной цикл с одной документированной
  командой и кнопкой «Смержить» в UI.
- **Обновить:** `SPECIFICATION.md` §7, `git_integration.feature`,
  `deferred.feature`.
- **✅ Решение (2026-09-25):** минимальный цикл публикации — двухшаговый:
  1. `check.publish` создаёт ветку `publish/{name}-{version}` от `main`;
  2. отдельный явный шаг слияния в `main` — команда
     `credo merge {name} {version}`.
  Правило становится видимым для `evaluate`, MCP и UI **только после слияния
  в `main`**; до этого ветка — заготовка, не доступная потребителям.
  - Канон MVP — «человек командой». Кнопка «Смержить» в UI и MCP-инструмент
    `check.merge` — в планах; команда `credo merge` проектируется так, чтобы
    позже её мог вызывать MCP-инструмент без изменения семантики.
  - Механика мержа в bare-репозитории: ветка обязана быть потомком `main`
    (`git merge-base --is-ancestor`); если нет — отказ **без авто-ребейза**
    с предложением перепубликовать. Иначе атомарное обновление `main` через
    `git update-ref refs/heads/main <sha-ветки> <ожидаемый-sha-main>`
    (CAS-проверка против параллельных мержей). После успеха ветка
    `publish/{name}-{version}` удаляется; артефакт неизменяемо живёт в `main`.
  - Принцип «четырёх глаз» (публикует один, мержит другой) в MVP не
    требуется, но закладывается архитектурно: шаг мержа отделён от шага
    публикации и допускает другого исполнителя.
  - Настоящий PR через GitHub API остаётся отложенным (`deferred.feature`).

### Q16. ✅ Публикация без теста и контрольная сумма (решено 2026-09-26, в рамках Q34)

- **Расхождение:** `publish.feature` требует отклонять публикацию без
  тестирования и при несовпадении контрольной суммы после последнего теста.
  В коде нет ни учёта тестов, ни такой проверки. SPEC это не описывает.
- **Вопрос:** оставляем требование? Если да — где хранится
  «checksum последнего теста»?
- **Рекомендация:** реализовать: хранить в черновике
  `last_test_checksum`/`tested_at` (одно поле в `sandbox.json`).
- **Обновить:** `draft.feature`, `publish.feature`, `lib.rs`, SPEC §4.2.
- **✅ Решение (2026-09-26):** требование оставляем. Сервер хранит в
  черновике (`.credo/sandbox.json`) `last_test_checksum` и `tested_at`;
  успешный `check.test` обновляет их. Публикация без успешного теста и
  публикация при несовпадении контрольной суммы (черновик изменён после
  теста) отклоняются. Канонические файлы тестов (`tests/*.тест`) — вне
  MVP (Q34).
  - **Следствие для кода:** учёт тестов и проверка checksum в `lib.rs`
    отсутствуют — отдельная задача; в этих правках код не менялся.
  - **Обновлено:** `features/publish.feature` (`last_test_checksum`/
    `tested_at`, отказ без теста и при изменении черновика),
    `features/test_draft.feature` (метка тестирования вместо
    `draft.feature` — учёт ведёт сервер, а не UI-черновик),
    `SPECIFICATION.md` §4.2/§4.3 и §10 (решение №32).

### Q17. ✅ Иммутабельность: файловая система или bare-git? (решено 2026-09-26)

- **Расхождение:** `immutability.feature` описывает защиту через
  `create_dir_all`/`AlreadyExists`; в `credo2` хранилище — bare git, защита
  — проверка дубликата + атомарный `create_ref`.
- **Вопрос:** какой механизм считаем нормативным (и нужно ли переносить
  хранилище на рабочее дерево)?
- **Рекомендация:** оставить bare-git, переписать сценарии фичи.
- **Обновить:** `immutability.feature`, `features/README.md`
  (строка и счётчик), `OPEN_QUESTIONS.md` (Приложение A, заметка 6).
- **✅ Решение (2026-09-26):** оставить bare-git, НЕ вводя отдельного
  механизма версионирования (никаких `refs/rules/...` на версию — это
  противоречило бы Q13/Q15/Q32). Иммутабельность обеспечивается уже
  принятой моделью реестра и потоком публикации в три слоя:

  1. Единица иммутабельности — каталог версии
     `checks/{name}/{X}/{Y}/{Z}/` в `main` (Q13/Q32). Содержит только
     `rule.json`, `contract.json`, `meta.json`; исходный `.dar` в реестр
     НЕ попадает (связь через `source_hash`; добавление `source.dar` —
     v0.2). Изменение правила = новая версия (новый каталог), а не правка
     существующего.
  2. Отказ при повторной публикации — на шаге `check.publish` (Q29):
     - если `checks/{name}/{X}/{Y}/{Z}/` уже существует в `main` — отказ
       с кодом `publish_failed` в конверте
       `{"error": {"code", "message"}}`;
     - downgrade запрещён (Q32);
     - ветка `publish/{name}-{version}` создаётся атомарно (`create_ref`
       без перезаписи).
  3. Защита при слиянии — на шаге `credo merge` (Q15): атомарный
     `git update-ref refs/heads/main <sha> <expected-sha>` с CAS-проверкой;
     при параллельных мержах один выигрывает, второй получает отказ.

  Источник истины опубликованного артефакта — git-состояние (каталог
  версии в `main`), а не произвольный каталог файловой системы.

  **Доменная мотивация:** банковский аудит и доказуемость. Версия,
  действовавшая в момент принятия кредитного решения, не может быть
  перезаписана, удалена или подменена задним числом; история публикаций
  в `main` — источник для аудита.

  **Терминология:** `name` — машиночитаемый латинский идентификатор
  (например, `CreditAgeMin`, Q13); термин `rule_id` не используется
  (пополнение глоссария §11 — открытый Q7).

  **Поверхность ошибок:** публикация — MCP-инструмент `check.publish`;
  отказы — код `publish_failed` по §4.5 (НЕ HTTP 409; статус 409 занят
  `activation_unavailable`, Q23).

  **Изменения в `immutability.feature`:** два существующих сценария
  обновлены на месте — в первом добавлен код `publish_failed`, второй
  переписан с файлового механизма `create_dir_all`/`AlreadyExists` на
  bare-git (повторная публикация при существующей ветке, `create_ref`
  без перезаписи). Дополнительно введены 2 новых поведенческих сценария
  (ранее отсутствовали): параллельная публикация одной версии; публикация
  новой версии не изменяет ранее опубликованные артефакты. Отдельный
  сценарий «публикация другого контента под существующей версией» не
  вводится: отказ не зависит от контента и покрыт сценарием 1. Итого:
  2 → 4 сценария. Статус фичи в `features/README.md` остаётся 🟡: код
  (`lib.rs`) пока пишет плоский путь `checks/{name}/{version}` — это
  расхождение Q32.

### Q18. ✅ Сортировка pre-release и build-метаданные (решено 2026-09-26)

- **Расхождение:** `deferred.feature` фиксирует лексикографическое
  сравнение (`rc2` vs `rc10`) как текущее; `core.rs` уже сравнивает
  числово; `semver.feature` ожидает `1.2.3+build` как валидную версию, а
  `storage_paths.feature` и код убирают build из пути.
- **Вопрос:** числовое сравнение — норматив? Что с двумя публикациями
  `1.0.0+a` и `1.0.0+b` (дубликат)?
- **Рекомендация:** числовое сравнение как в semver; build отбрасывать и
  явно запрещать повторную публикацию базы.
- **Обновить:** `deferred.feature`, `semver.feature`,
  `SPECIFICATION.md`, `storage_paths.feature` (комментарий про Q18),
  `features/README.md` (счётчики), `OPEN_QUESTIONS.md`
  (Приложение A, заметка 7).
- **✅ Решение (2026-09-26):** принимается рекомендация.

  1. **Сравнение версий — по семантике semver** (semver 2.0.0):
     pre-release сортируется ниже release; идентификаторы pre-release
     сравниваются слева направо, числовые — как числа (`1.0.0-rc2` <
     `1.0.0-rc10`), буквенно-цифровые — лексикографически; ошибка
     парсинга при сравнении — ошибка, а не `0.0.0`. Код уже
     соответствует (`core::Semver::compare_pre`; тесты
     `prerelease_numeric_ordering`, `prerelease_sorts_below_release`).
     Лексикографическая сортировка `rc2`/`rc10` из `deferred.feature`
     снята как устаревшая — сценарий удалён.
  2. **Build-метаданные не входят в идентичность версии.** При парсинге
     `+build` сохраняется (диагностика, `Display`), но `PartialEq`/`Ord`
     его игнорируют: `1.0.0+a == 1.0.0+b` (тест
     `eq_ignores_build_metadata_consistent_with_ord`).
  3. **При записи в реестр build отбрасывается** (`as_storage`): путь и
     `meta.version`/`contract.version` — без `+build`
     (`checks/{name}/{X}/{Y}/{Z}/`); отдельная связь с исходной строкой
     версии не требуется.
  4. **Повторная публикация базы с другим build запрещена:**
     `1.0.0+a` и `1.0.0+b` — один слот версии `1.0.0`; вторая публикация
     отклоняется как обычный дубль — `publish_failed` (§4.5, Q29),
     сообщение «версия уже существует». Код (`lib.rs`) уже сравнивает
     storage-версии; обёртка с кодом `publish_failed` — часть Q29
     (отдельная задача).
  5. **Публикация pre-release остаётся вне MVP** (Q32 п. 8): сравнение
     из п. 1 нормативно уже сейчас, публикация `1.0.0-rc.1` вернётся в
     v0.2 (вариант пути — `checks/{name}/{X}/{Y}/{Z-prerelease}/`).

  - Следствие для кода: правок не требуется — поведение совпадает с
    решением. `deferred.feature`: сценарий «Числовая сортировка
    prerelease» удалён (6 → 5). `semver.feature` дополнена 4 сценариями
    (4 → 8): числовое сравнение pre-release; build не влияет на
    идентичность; build отбрасывается при записи; повторная публикация
    базы с другим build отклоняется.

### Q19. ✅ Где живут данные: `.credo/` и `.dar-notebook/` (решено 2026-09-26)

- **Расхождение:** SPEC §4.2 упоминает `.credo/sandbox.json` и
  `.credo/published-repo/`, §4.3 — `.dar-notebook/` (`layout.json`,
  `results-cache.json`, `agent-history.json`). В фичах фигурирует только
  `.dar-notebook/`.
- **Вопрос:** описать оба каталога и их владельцев?
- **Рекомендация:** да, добавить в SPEC §4.3 таблицу «каталог → что
  хранит → попадает ли в git».
- **Обновить:** `SPECIFICATION.md` §4.3, `features/notebook_ui.feature`
  (`.gitignore`).
- **✅ Решение (2026-09-26):** оба служебных каталога живут **в корне
  workspace** (тот же каталог, что и `rules/`), владельцы разные:

  | Каталог | Владелец | Содержимое | В git? |
  |---|---|---|---|
  | `rules/*.dar` | пользователь / workspace | исходники правил — источник истины (Q12) | да |
  | `.credo/` | `credo-server` | `sandbox.json` (черновики, Q12); `published-repo/` (bare-git публикаций, Q15/Q32) | нет (`.gitignore`) |
  | `.dar-notebook/` | DAR Notebook | `layout.json`; `results-cache.json` («быстрые прогоны», Q34); `agent-history.json` (Q31) | нет (`.gitignore`) |

  - **Почему `.credo/` в `.gitignore`:** каталог лежит внутри
    workspace-репозитория, а `sandbox.json` и вложенный bare-repo —
    служебные данные; без игнорирования они загрязняют git-статус
    пользователя. `.gitignore` workspace создаётся с записями `.credo/` и
    `.dar-notebook/` (`notebook_ui.feature`).
  - **Домены не смешиваются:** `.credo/` — серверный (черновики и
    публикации), `.dar-notebook/` — клиентский (UI-метаданные, кэш тестов,
    история чата); общий только корень workspace.
  - **Диаграмма §4.3 исправлена:** `.dar-notebook/` больше не показан
    снаружи workspace (это противоречило `.gitignore`); оба каталога
    показаны внутри `consumer-credit/`.
  - **Имя `.credo/` оставлено прототипным** (решение 2026-09-26);
    переименование в `.dar/` вместе с эволюцией в `credo-server`/`dar-core`
    (Q1) — вопрос пост-MVP, отдельным решением. В текущую работу не
    входит.
  - Фичи и счётчики не менялись по числу сценариев (актуальные числа — в
    `features/README.md`); в `notebook_ui.feature` уточнён только шаг
    `.gitignore`.

---

## 4. REST API

### Q20. ✅ Канонические пути REST (решено 2026-09-25)

- **Расхождение:** `rest_api.feature` — `/checks/:name/versions/:version/...`;
  `evaluate.feature` — `/checks/:name/:version/evaluate` и
  `GET /checks/:name/9.9.9`; `import_export.feature` —
  `/checks/:name/1.0.0/export`. Реализация — вариант с `/versions/`.
- **Вопрос:** какой путь нормативен?
- **Рекомендация:** `/checks/{name}/versions/{version}/...` (как в коде);
  переписать `evaluate.feature` и `import_export.feature`.
- **Обновить:** `evaluate.feature`, `import_export.feature`, `rest_api.feature`.
- **✅ Решение (2026-09-25):** канонический путь —
  `/checks/{name}/versions/{version}/...` (сегмент `/versions/` обязателен,
  как в коде и `rest_api.feature`). Исполнение —
  `POST /checks/{name}/versions/{version}/evaluate`.

  `name` и `version` — параметры пути, а не тела: одно правило не создаёт
  новый эндпоинт, это один шаблон пути с параметрами. Плоский вариант
  («имя в теле») не вводится и не планируется: он дублировал бы RPC-стиль
  MCP и терял наблюдаемость (аудит/метрики по пути) и возможность
  кэширования GET-представлений. Внешним конвейерам в v0.2 предназначен
  отдельный ресурс `/pipelines/{name}/versions/{version}/evaluate`;
  алиасы не вводятся.

  Терминология: «конвейер» и pipeline — одно понятие (см.
  `SPECIFICATION.md` §1.3.1/§11): в текстах — «конвейер», латиница —
  только в машинных идентификаторах (`pipeline`/`pipelines`, URL).

  Атомарная модель исполнения (принцип атомарной композиции, «принцип
  Макдоналдса», см. `SPECIFICATION.md` §1.3.1). В v0.1 единица исполнения —
  одно правило (один `evaluate` → одно `Explanation`, Q42). Полный прогон
  («вся картина», по практике ~90% реальных вызовов) сознательно отложен
  в v0.2, но атом спроектирован под него как композируемый элемент:
  - атомарный `evaluate` — чистая функция (правило, данные) → Explanation,
    без состояния и без сведения решений; чистота гарантируется на уровне
    ядра (`dar-core::evaluate_rule`), транспорты — тонкие обёртки;
  - сведение нескольких решений к одному вердикту — слой конвейера,
    требует `Приоритет` (Q4) и конвейеров (Q36) — v0.2;
  - конвейер в v0.2 появится как **новый** ресурс
    `/pipelines/{name}/versions/{version}/evaluate`, путь `/checks/...` при
    этом не меняется.

  `evaluate` читает только опубликованные версии, влитые в `main` (Q12/Q15);
  черновики в REST-исполнении не участвуют. Серверный агрегирующий эндпоинт
  в MVP не вводится; оркестрация полного прогона — на стороне клиента
  (манифест → цикл по `active`-версиям → цикл атомарных `evaluate`);
  кто именно клиент, определит Q30.

  Состав REST в MVP минимальный (YAGNI):
  ```
  POST /checks/{name}/versions/{version}/evaluate  — атом, ядро продукта;
  GET  /checks                                     — манифест (схема —
                                                     решение Q21);
  GET  /health, GET /version, GET /openapi.json    — инфраструктура.
  ```
  Вне MVP-минимума (реализованы в прототипе; решение 2026-09-25 —
  **не удалять и не развивать в MVP**): `POST /checks/{name}/evaluate`
  (active-версия), `GET /checks/{name}/versions`,
  `GET /checks/{name}/versions/{version}`. Судьба этих эндпоинтов
  (сохранение/удаление) — вопрос пост-MVP, не задача MVP. За горизонтом
  MVP: `GET /checks/{name}` (детали), batch (Q24 — отложен),
  export/import (Q26 — отложен), конвейеры (v0.2).

  Обновить: `evaluate.feature` (пути с `/versions/`, схема манифеста,
  404-сценарий на несуществующую версию), `rest_api.feature` (сверка
  путей); `import_export.feature` — отложено по решению Q26 (способ B):
  файл остаётся отдельным, строка переносится в раздел «Отложено»
  `features/README.md` со статусом ⏸ и приоритетом ⏳, в `deferred.feature`
  НЕ вливается, счётчики не меняются; в файл добавляется шапочный
  комментарий об отсрочке, путь сценария экспорта приводится к канону
  (`/checks/{name}/versions/{version}/export`); `SPECIFICATION.md` (§1.3.1 —
  принцип атомарной композиции, §10 — решение №22).

### Q21. ✅ Схема ответа `GET /checks` (решено 2026-09-25)

- **Расхождение:** `rest_api.feature` — `count`/`service_hash`/`checks`
  (`ManifestEntry`: `name`/`active`/`supported`/`deprecated`);
  `dashboard.feature` — «три записи со статусами активна/deprecated»;
  `evaluate.feature` — `name`/`kind`/`versions`/`latest`.
- **Вопрос:** одна схема ответа — какая? Входит ли `kind` в манифест?
- **Рекомендация:** канон — `ManifestEntry` из кода; `kind` добавить,
  только если он нужен публикации (сейчас есть только в фичах).
- **Обновить:** `dashboard.feature`, `evaluate.feature`,
  `SPECIFICATION.md` (схема API).
- **✅ Решение (2026-09-25):** канон схемы `GET /checks` — манифест как
  есть: ответ идентичен `manifest.json` в main (один источник истины):
  `{ "schema_version": 1, "count", "service_hash", "checks": [ ManifestEntry ] }`.

  `ManifestEntry` (канонические сценарии — `manifest.feature`):
  - `name: string` — машиночитаемый идентификатор (латиница, Q13);
  - `active: string` — максимальная версия среди **не** помеченных
    deprecated (пример: 1.1.0 в deprecated → active = 1.0.1); если все
    версии deprecated — пустая строка (конвенция «нет значения = пустая
    строка», Q10/Q42); обращение к active-эндпоинту в этом состоянии —
    409 «активация недоступна: {name}» (Q11);
  - `supported: string[]` — все не-deprecated версии по убыванию, включая
    active (денормализация намеренная);
  - `deprecated: string[]` — версии, выведенные из эксплуатации.
  Инварианты: каждая версия ровно в одном из supported/deprecated;
  при непустом supported `active == supported[0]`.

  Поле `kind` в манифест **не** добавляется: в v0.1 единственный вид
  проверок — правила (конвейеры/скоринги/таблицы вне MVP, Q36), `kind` —
  YAGNI, вернётся в v0.2. Отдельный `GET /checks/{name}` (детали) в MVP не
  нужен — манифеста достаточно для дашборда и для клиентской оркестрации
  полного прогона (Q20): итерация по `active`-версиям.

  Обновить: `dashboard.feature`, `evaluate.feature` (убрать `kind`/
  `versions`/`latest` из схемы списка), `SPECIFICATION.md` (схема API,
  §4.4 + решение №23), `OPEN_QUESTIONS.md` Q20 (ссылка на манифест).

### Q22. ✅ Аутентификация: `x-api-key` или `Authorization` (решено 2026-09-25)

- **Расхождение:** `rest_auth.feature` и код — `x-api-key`;
  `evaluate.feature` — `Authorization` и текст «ошибка аутентификации».
- **Рекомендация:** `x-api-key`; путь `/openapi.json` тоже открыт (как в
  коде) и зафиксировать это в `rest_auth.feature`.
- **Обновить:** `evaluate.feature`, `rest_auth.feature`.
- **✅ Решение (2026-09-25):**
  1. Заголовок — **`x-api-key`** (канон; `Authorization` в фичах не
     используется). OAuth/JWT/mTLS — вне v0.1, кандидаты v0.2+ (Q39).
  2. Ключ — **единственный серверный секрет** (`CREDO_API_KEY`), не
     идентификатор клиента: пер-клиентские ключи, квоты и аудит — v0.2+.
  3. Текст 401 (в конверте Q23: `code = unauthorized`):
     `ошибка аутентификации: неверный или отсутствующий x-api-key` —
     единый для отсутствующего и неверного ключа (не раскрывает, был ли
     заголовок). Канон текстов Q11 обновлён.
  4. Открытые пути (без ключа): `/health`, `/docs`, `/openapi.json` —
     как в коде; зафиксировано в `rest_auth.feature`.
  5. Режим без `CREDO_API_KEY` — **штатный режим демо/разработки**: API
     открыт; в проде переменная обязательна (проверка на старте — v0.2).
     Полезно для быстрого локального прогона и упрощения ручного UI-прогона
     на demo без настройки секретов.
  6. Способ задания: флаг `--api-key <KEY>` с переменной окружения
     `CREDO_API_KEY` как источником значения по умолчанию (реализация в
     `src/main.rs`); приоритет — явный флаг выше переменной. Формат ключа
     в v0.1 не регламентирован (любая непустая строка; генерация и
     требования сложности — v0.2 вместе с проверкой на старте).

  ### Уточнение редакции 2 (2026-09-25): Q22 закрыт полностью

  При сверке решения с кодом выявлено единственное расхождение: ответ 401
  в обработчике `auth` (`src/rest.rs`) возвращал старый плоский вид
  `{"error": "<текст>"}`. Это **не новый вопрос**, а часть уже закрытого
  Q23: конверт `{"error": {"code", "message"}}` с `code = unauthorized`
  покрывает 401 (реализовано вместе с остальными ошибками Q23). Правок
  формулировок решения не требуется: Q22 определяет заголовок, тексты и
  открытые пути, форму тела ошибки — Q23.

  Итог по коду: по итогам Q22 собственных изменений не вносится — конверт
  401 закрыт решением Q23; фичи и SPEC актуальны.

  Обновлено: `features/rest_auth.feature` (текст 401, `/openapi.json`),
  `features/evaluate.feature` (полный текст 401), `SPECIFICATION.md`
  (§4.4 — текст 401, §10 — решение №26), `OPEN_QUESTIONS.md` (Q11 — канон
  текста 401).

### Q23. ✅ Формат ошибок REST (решено 2026-09-25)

- **Расхождение:** тексты ошибок унифицированы русским каноном (Q11), но
  конверт не определён: код возвращает `{"error": "<сообщение>"}` без
  машиночитаемого `code`, фичи структуру не описывают. Добавление `code` и
  конверта — остаток, переданный из Q11.
- **Вопрос:** единый конверт ошибки и набор стабильных кодов?
- **Рекомендация:** конверт `{"error": {"code", "message"}}`, тексты
  по-русски, коды латиницей (`not_found`, `unauthorized`).
- **Обновить:** `rest.rs`, `rest_api.feature`, `evaluate.feature`.
- **✅ Решение (2026-09-25):** единый конверт ошибки для всех ответов REST
  с ненулевым статусом (401/404/409/410/422):

  ```json
  {"error": {"code": "<стабильный код>", "message": "<русский текст>"}}
  ```

  - `message` — канонический человекочитаемый текст по Q11 (русский);
  - `code` — стабильный машиночитаемый идентификатор: `snake_case`,
    латиница (принцип «машина/человек», Q11);
  - необязательное поле `details` — в v0.1 **не вводится** (YAGNI; вернётся
    при появлении сценария валидации, например batch в Q24).

  Стабильные коды (канонические тексты — Q11, пути — Q20):

  | HTTP | `code` | `message` |
  |---|---|---|
  | 401 | `unauthorized` | `ошибка аутентификации: неверный или отсутствующий x-api-key` |
  | 404 | `check_not_found` | `проверка не найдена: {name}` |
  | 404 | `version_not_found` | `версия не найдена: {name}@{version}` |
  | 409 | `activation_unavailable` | `активация недоступна: {name}` |
  | 410 | `version_deprecated` | `версия выведена из эксплуатации: {name}@{version}` |
  | 422 | `evaluation_failed` | текст ошибки ядра (`EvalError`) |

  Клиенты связывают статус и код: `check_not_found` / `version_not_found`
  различаются кодом при одном статусе 404; `code` стабилен и не меняется
  без major-версии API, текст `message` может уточняться.

  Границы: Q11 — язык и тексты `message`; Q22 — заголовок аутентификации и
  текст/код 401 (решено 2026-09-25: `x-api-key`, единый текст); Q24 — схема
  ответов batch (отложен; при возврате — тот же конверт для ошибок
  транспорта).

  Следствие для кода: `err()` в `rest.rs` и обработчик `auth` переведены
  на новый конверт. В MVP не меняются состав
  эндпоинтов и тексты — только обёртка ответа.

  Обновлено: `SPECIFICATION.md` (§4.4 — таблица конверта и кодов, §10 —
  решение №25), `features/rest_api.feature`, `features/evaluate.feature`,
  `features/rest_auth.feature` (шапочные заметки и ассершены структуры
  ошибок).

### Q24. ✅ Массовый прогон: отложен (решено 2026-09-26)

- **Расхождение:** `batch.feature` шлёт `{"заявки": [...]}` на тот же
  `POST .../evaluate` и ждёт «статистика/всего/отказано/одобрено». Схема
  запроса/ответа нигде не описана, ключи русские — вразрез с остальным
  API.
- **Вопрос:** отдельный `POST /checks/{name}/versions/{v}/batch` или
  перегрузка `evaluate`? Ключи латиницей?
- **Рекомендация:** отдельный эндпоинт, ключи латиницей (`applications`,
  `stats`), русские — только в текстовых значениях.
- **Обновить:** `batch.feature` (шапочный комментарий),
  `features/README.md` (перенос в «Отложено», статус ⏸ / приоритет ⏳),
  `SPECIFICATION.md` (§10, решение №36), `OPEN_QUESTIONS.md`
  (Q20 — упоминание batch).
- **✅ Решение (2026-09-26):** массовый прогон (batch) — **вне MVP**,
  пост-MVP/v0.2.

  Обоснование:
  - в v0.1 единица исполнения — атомарный `evaluate` (принцип атомарной
    композиции, Q20): полный прогон оркестрируется клиентом по манифесту
    (цикл по `active`-версиям → цикл атомарных `evaluate`, Q21),
    серверный агрегат не требуется;
  - MVP-минимум REST закрыт Q20 (versioned evaluate + `GET /checks` +
    инфраструктура); сценария пакетного прогона в демо нет — YAGNI;
  - схема batch (ключи, агрегаты, частичные ошибки) не стабилизирована,
    вводить её в MVP преждевременно.

  Направление на v0.2 (если вернёмся):
  - отдельный эндпоинт `POST /checks/{name}/versions/{version}/batch`
    (не перегрузка `evaluate`: путь с параметрами из Q20 не меняется);
  - ключи — латиницей (`applications`, `stats`), русские — только
    текстовые значения (принцип Q11);
  - ошибки транспорта — конверт Q23; частичные ошибки — целевое
    состояние `batch.feature`.

  Следствие: в `rest.rs`/`mcp.rs` ничего не добавляется; `batch.feature`
  остаётся целевым состоянием с шапочным комментарием об отсрочке;
  число файлов и сценариев не меняется (3 сценария, backend), меняется
  только статус/приоритет файла в README.

### Q25. ✅ Объяснение для клиента: отложено (решено 2026-09-26)

- **Расхождение:** `client_explanation.feature` не говорит, куда
  обращаться (REST? MCP?), какие поля, откуда берётся «основание — политика
  кредитования» и порядок причин «по приоритету».
- **Вопрос:** определить эндпоинт/инструмент и схему.
- **Рекомендация:** MCP-инструмент `check.explain_client` для v0.1,
  шаблон текста — из контракта.
- **Обновить:** `client_explanation.feature` (шапочный комментарий),
  `features/README.md` (перенос в «Отложено», статус ⏸ / приоритет ⏳),
  `SPECIFICATION.md` (§4.5 — ссылка на Q25; §10, решение №37).
- **✅ Решение (2026-09-26):** клиентское объяснение — **вне MVP**,
  пост-MVP/v0.2.

  Обоснование (зависимости сценариев):
  - **шаблон и «основание — политика кредитования»** — банковский
    юридический текст; источника нет ни в `CheckContract` (`description` —
    техническая строка «Если … → …», `decisions` — только id решений), ни
    в SPEC; в целевом состоянии — БД/справочники (Q37) или расширение
    контракта;
  - **«Заявка одобрена»** — клиентская формулировка, отображаемая из
    `decision`-id; словарь решений задаёт банк (Q10), фиксированного
    маппинга в ядре нет;
  - **несколько причин и порядок по приоритету** — конвейеры и
    `Приоритет`, вне MVP (Q4/Q36);
  - **потребителя в MVP нет**: оператора колл-центра в продукте нет,
    Notebook — для риск-технологов; агент в чате пересказывает
    `Explanation` и без отдельного инструмента (Q31).

  Направление на v0.2 (если вернёмся): MCP-инструмент
  `check.explain_client` для оператора (шаблоны и «политика» — из
  БД/справочников, Q37), маппинг `decision` → клиентская формулировка —
  банковский словарь (Q10), мультипричины и порядок — конвейеры
  (Q4/Q36); для внешнего конвейера возможен REST-рендер — решить тогда.
  «Шаблон из контракта» в текущем виде невозможен: требует расширения
  схемы `CheckContract` — отдельное решение v0.2.

  Следствие: в `mcp.rs`/`rest.rs` ничего не добавляется; MVP-сырьё для
  клиентского текста — `Explanation` (Q42, русские `decision`/`reason`),
  рендер — задача потребителя; `client_explanation.feature` остаётся
  целевым состоянием с шапочным комментарием об отсрочке; число файлов и
  сценариев не меняется (3 сценария, backend), меняется только
  статус/приоритет файла в README.

### Q26. ✅ Импорт/экспорт: контракт (решено 2026-09-25)

- **Расхождение:** `import_export.feature` использует инструменты
  `check.export_draft` и `check.import`, которых нет в коде;
  `Content-Type: text/dar`; флаг «импортированный» нигде не хранится.
- **Вопрос:** реализуем в MVP или переносим в `deferred.feature`?
- **Рекомендация:** сначала решить Q12 (истина в файлах), затем решать
  формат обмена.
- **Обновить:** `import_export.feature`, `mcp.rs`.
- **✅ Решение (2026-09-25):** импорт/экспорт `.dar` — вне MVP, переводится
  в отложенные (пост-MVP, v0.2+).

  Обоснование (вытекает из закрытых вопросов):
  - Q12: источник истины — `.dar`-файл в git-репозитории workspace.
    Поэтому «экспорт» в MVP вырождается в файловую копию / `git show`,
    а «импорт» — в создание `rules/{name}.dar` + `check.create`.
    Отдельный транспорт не нужен.
  - Q13: публикуется не DSL-текст, а артефакт `checks/{name}/{X}/{Y}/{Z}/`
    (структура пути уточнена в Q32).
    «Экспорт версии» как перенос артефакта — сценарий несвязанных
    инсталляций, которого в MVP нет.
  - Q20: состав REST минимален (YAGNI), импорт/экспорт уже отнесён к
    пост-MVP.

  Следствие: в MVP не добавляются ни REST-эндпоинты, ни MCP-инструменты
  `check.export_draft`/`check.import`, ни `Content-Type: text/dar`, ни флаг
  «импортированный» (в `mcp.rs` и `rest.rs` ничего не добавляется). Формат
  обмена вернётся в v0.2, если появится сценарий переноса правил между
  несвязанными workspace/инсталляциями.

  Обновить: `features/README.md` (строку `import_export.feature` — статус
  ⏸ / приоритет ⏳, перенести в раздел «Отложено»); `import_export.feature`
  (шапочный комментарий «отложено до v0.2, см. Q26»); `OPEN_QUESTIONS.md`
  (уточнить строку «Обновить» в Q20); в `mcp.rs`/`rest.rs` ничего не
  добавлять.

### Q27. ✅ Порт REST (решено 2026-09-25)

- **Расхождение:** SPEC §4.2 — порт 8080; код — `127.0.0.1:8080` по
  умолчанию и выключен, если не передан `--rest`/`--addr`. Фичи не
  описывают запуск.
- **Рекомендация:** зафиксировать в SPEC и README запуска.
- **Обновить:** `SPECIFICATION.md` §4.2, README запуска.
- **✅ Решение (2026-09-25):** канон — поведение кода (`src/main.rs`),
  расхождение было только в формулировке SPEC:
  - Адрес по умолчанию — **`127.0.0.1:8080`** (порт 8080 зафиксирован
    вместе с хостом; loopback — безопасное значение по умолчанию).
  - REST поднимается **только по явному флагу**: `--rest` (адрес по
    умолчанию) или `--addr <ADDR>` (включает REST на указанном адресе).
    Без флагов — штатный режим «только MCP (stdio)». Приоритет:
    `--no-rest` > `--addr` > `--rest`.
  - Наружное развёртывание — через `--addr` (например, `--addr
    0.0.0.0:8080`); отдельного конфига/переменной порта в MVP нет.
  - Режим «только MCP» согласуется с планируемыми Notebook-сценариями
    (Q30: локальный stdio-процесс) и является режимом демо по умолчанию.
  - Следствие для кода: **правок не требуется** — реализация совпадает с
    принятым решением.
  - Обновлено: `SPECIFICATION.md` §4.2 (блок «Запуск и адрес REST» +
    таблица флагов, схема сервера — `127.0.0.1:8080`), §10 — решение №27.
    Корневой README отсутствует (эволюция `credo2`, см. Q1); блок запуска
    в §4.2 — единственный источник истины по запуску до появления README.
    Файлы `features/*.feature` не меняются: запуск — инфраструктура, а не
    функциональное требование (принцип «машина / человек»).

---

## 5. MCP: контракт для агента

### Q28. ✅ Параметры `check.create` (решено 2026-09-26)

- **Расхождение:** `draft.feature` — `name`, `text`, `expected_kind`;
  ответы `list_drafts` — размер в байтах и формат `"text"`; код —
  `source`, хранит распарсенное правило, отдаёт `sandbox_file`. Фичи
  `agent_minimal.feature` полагаются на результат («Черновик создан»), но
  параметров не показывают.
- **Вопрос:** канонический контракт `check.create`?
- **Рекомендация:** один вызов — `source: string` (как в коде),
  `expected_kind`/`name` убрать из MVP; переписать `draft.feature`.
- **Обновить:** `draft.feature`, `mcp.rs`, README инструментов.
- **✅ Решение (2026-09-26):** контракт `check.create` для MVP:

  ```json
  { "name": "string", "source": "string" }
  ```

  1. Оба параметра обязательны. `source` — весь текст `.dar`-файла;
     сервер парсит и валидирует его, ошибки парсинга возвращаются как
     `error`.
  2. `name` извлекается из заголовка правила (первая строка
     `Правило {name}` согласно `GRAMMAR.md`); если заголовка нет или он
     невалиден — ошибка валидации.
  3. **Идемпотентность:** повторный `check.create` с тем же `name`
     перезаписывает черновик (обновляет `source`) — ментальная модель
     «сохранить = обновить»; отдельного флага перезаписи нет.
  4. **Ответ:** `{ "status": "ok", "name": "..." }`. Метаинформация
     (список полей) — вне MVP, задача LSP.
  5. Без `expected_kind` и `contract` — типы выводятся автоматически из
     `source`.
  - **Следствие для кода:** сейчас `check.create` принимает только `source`
    и возвращает `{status, draft, overwritten, sandbox_file}`; требуется
    принимать/сверять `name` и вернуть `{status, name}` — отдельная задача.
    Перезапись уже реализована (`upsert_draft`).
  - **Обновлено:** `features/draft.feature` (контракт, «сохранить =
    обновить», ошибка невалидного `source`), `features/agent_minimal.feature`
    (агент передаёт `name` и `source`), `features/README.md` (строка,
    заметка), `SPECIFICATION.md` §7 и §10 (решение №31).

### Q29. ✅ Ответы `check.test`, `check.list_published`, `check.deprecate` (решено 2026-09-26)

- **Расхождение:** фичи ждут поля `matched`, `decision`, `reason`,
  `actual_value`, `deprecated_at` (это есть), но также «размер» и
  «формат» черновика; `check.deprecate` в коде требует `reason`
  обязательным.
- **Вопрос:** зафиксировать JSON-схемы всех MCP-инструментов в одном
  месте.
- **Рекомендация:** добавить в SPEC раздел «Контракты MCP-инструментов»
  с таблицей параметров/ответов.
- **Обновить:** `SPECIFICATION.md` (новый §4.5),
  `features/mcp_tools.feature`, `features/draft.feature`,
  `features/test_draft.feature`, `features/publish.feature`,
  `features/deprecation.feature`, `features/README.md`.
- **✅ Решение (2026-09-26):** JSON-контракты MCP-инструментов фиксируются
  в `SPECIFICATION.md`, раздел «Контракты MCP-инструментов» (§4.5).

**Общие правила:**

1. Успешный ответ:
   - `isError = false`;
   - `content[0].text` содержит JSON-объект;
   - объект содержит поле `status`;
   - `status` может быть общим `"ok"` или доменным: `"published"`,
     `"deprecated"`.

2. Ошибочный ответ:
   - `isError = true`;
   - `content[0].text` содержит JSON-объект вида
     `{"error": {"code": "...", "message": "..."}}`;
   - `code` — стабильный машиночитаемый идентификатор, `snake_case`,
     латиница;
   - `message` — человекочитаемый русский текст (Q11).

3. Ключи ответов — `snake_case`, латиница. Текстовые значения
   `decision`, `reason`, `deprecation_reason`, `message` — русские.

4. Каноническое имя входных данных для исполнения — `input`.
   Альтернативы `data`, `params`, `payload` не используются.

**Стабильные коды ошибок:**

| Код | Когда используется |
|---|---|
| `validation_failed` | неверные параметры или невалидный `.dar`-текст |
| `draft_not_found` | черновик не найден |
| `evaluation_failed` | ошибка исполнения правила (Q8/Q9) |
| `publish_failed` | публикация отклонена: дубль, downgrade, отсутствие успешного теста, изменение черновика после теста, ошибка git |
| `version_not_found` | опубликованная версия не найдена |
| `version_deprecated` | попытка исполнить выведенную из эксплуатации версию |
| `deprecation_conflict` | попытка повторно пометить версию как устаревшую |
| `manifest_error` | не удалось пересобрать или записать манифест |
| `unknown_tool` | неизвестное имя инструмента |
| `internal_error` | неожиданная внутренняя ошибка |

**Инварианты:**

1. Любой черновик в песочнице всегда парсится: `check.create`
   валидирует `source` до записи. Черновик с невалидным текстом
   существовать не может.

2. Черновик в песочнице не раскрывает внутренний тип `Rule`.
   MCP-ответы возвращают только рендеренные строки: `source`,
   `condition`, `decision`, `reason`. Структуры `Rule/Condition/Action`
   остаются приватным представлением ядра (Q3, GRAMMAR.md §3).

3. `stale` черновика определяется так:
   - файл `rules/{name}.dar` отсутствует → `stale = false`;
   - файл существует и его `source_hash` равен `source_hash`
     черновика → `stale = false`;
   - файл существует и его `source_hash` отличается → `stale = true`.

   `stale` не блокирует `check.test`: устаревший черновик можно
   тестировать (Q12).

4. `test_valid` черновика определяется так:
   `test_valid = (last_test_checksum != null && last_test_checksum == source_hash)`.

5. Поля `size` и `format` черновика не вводятся. Формат всегда `.dar`;
   размер при необходимости вычисляется клиентом из `source`.

**Контракты инструментов:**

#### 1. `check.create`

Вход: `{name, source}`, оба обязательны.

Успех:
```json
{ "status": "ok", "name": "..." }
```

Повторный вызов с тем же `name` перезаписывает черновик
(«сохранить = обновить», Q28).

#### 2. `check.list_drafts`

Вход: `{}`.

Успех:
```json
{
  "status": "ok",
  "count": 1,
  "drafts": [
    {
      "name": "...",
      "updated_at": "...",
      "condition": "...",
      "decision": "...",
      "stale": false
    }
  ]
}
```

`condition` и `decision` — рендеренные строки, не объекты.

#### 3. `check.get_draft`

Вход: `{name}`.

Успех:
```json
{
  "status": "ok",
  "draft": {
    "name": "...",
    "source": "...",
    "source_hash": "sha256:...",
    "condition": "...",
    "decision": "...",
    "reason": "...",
    "created_at": "...",
    "updated_at": "...",
    "last_test_checksum": "sha256:..." ,
    "tested_at": "...",
    "test_valid": true,
    "stale": false
  }
}
```

`last_test_checksum` и `tested_at` могут быть `null`, если тест не
выполнялся. Поле `rule` не возвращается. `condition` — результат
`condition_to_string`; `decision` и `reason` — значения действия.
`test_valid` определяется инвариантом 4, `stale` — инвариантом 3.

#### 4. `check.test`

Вход: `{name, input}`.

Успех возвращает поля канонического объяснения (Q42)
и метаданные теста на верхнем уровне, без вложенного `explanation`:
```json
{
  "status": "ok",
  "rule_name": "...",
  "condition": "...",
  "actual_value": 19,
  "matched": true,
  "decision": "...",
  "reason": "...",
  "source_hash": "sha256:...",
  "tested_at": "...",
  "last_test_checksum": "sha256:..."
}
```

Успешный `check.test` обновляет `last_test_checksum` и `tested_at`
черновика. `last_test_checksum` равен `source_hash` черновика
на момент успешного теста. `stale` не блокирует `check.test`.

Ошибки исполнения возвращаются как `evaluation_failed`.

#### 5. `check.delete_draft`

Вход: `{name}`.

Успех:
```json
{ "status": "ok", "name": "...", "deleted": true }
```

Если черновика не было — `deleted: false`. Отсутствие черновика
не является ошибкой (идемпотентность).

#### 6. `check.publish`

Вход: `{name, version, published_by?}`.

Успех:
```json
{
  "status": "published",
  "name": "...",
  "version": "...",
  "branch": "publish/{name}-{version}",
  "path": "checks/{name}/{X}/{Y}/{Z}/",
  "published_at": "...",
  "published_by": "...",
  "commit_msg": "...",
  "next_step": "..."
}
```

Поля `branch`, `path`, `name`, `version` стабильны. Поле `next_step` —
необязательная человекочитаемая подсказка; формат текста не
гарантируется.

Публикация без успешного теста и публикация после изменения
черновика отклоняются с кодом `publish_failed`.

#### 7. `check.deprecate`

Вход: `{name, version, reason}`, все поля обязательны,
`reason` непустой.

Успех:
```json
{
  "status": "deprecated",
  "name": "...",
  "version": "...",
  "reason": "...",
  "deprecated_at": "...",
  "service_hash": "..."
}
```

Инструмент помечает версию как устаревшую, пересобирает манифест
и возвращает новый `service_hash`. Повторная попытка депрекации
возвращает `deprecation_conflict`.

#### 8. `check.list_published`

Вход: `{}`.

Успех:
```json
{
  "status": "ok",
  "count": 2,
  "service_hash": "...",
  "versions": [
    {
      "name": "...",
      "version": "...",
      "path": "checks/{name}/{X}/{Y}/{Z}/",
      "status": "supported",
      "active": true,
      "published_at": "...",
      "published_by": "...",
      "deprecated_at": null,
      "deprecation_reason": null
    }
  ]
}
```

`count` — число версий в списке. `active` вычисляется из манифеста.
Сортировка: имя по возрастанию, версия по убыванию **по семантике
semver** (`semver.feature`, Q18).

#### 9. `check.rebuild_manifest`

Вход: `{}`.

Успех:
```json
{
  "status": "ok",
  "service_hash": "...",
  "written": true,
  "count": 2
}
```

`count` — число проверок в манифесте, согласовано с `GET /checks`
(Q21).

#### 10. `check.run`

Вход: `{name, version, input}`, все поля обязательны.

Успех возвращает поля объяснения на верхнем уровне:
```json
{
  "status": "ok",
  "name": "...",
  "version": "...",
  "rule_name": "...",
  "condition": "...",
  "actual_value": 19,
  "matched": true,
  "decision": "...",
  "reason": "..."
}
```

`check.run` исполняет только опубликованные версии из `main`.
Обращение к выведенной из эксплуатации версии возвращает
`version_deprecated`.

**Вне Q29:** `check.merge` (Q15, план — команда `credo merge`
и будущая кнопка UI) остаётся за Q15 и в контракты Q29 не входит.

### Q30. ✅ Транспорт MCP в Notebook (решено 2026-09-26)

- **Расхождение:** SPEC §2.3/§4.1: «MCP-клиент (подключение к
  credo-server)», транспорт MCP — «stdio / HTTP+SSE». `agent_minimal.feature`
  — «панель подключена к MCP-серверу через stdio». Не сказано, кто
  запускает сервер: Notebook сам спавнит процесс или подключается к
  развёрнутому.
- **Вопрос:** в MVP Notebook сам запускает локальный `credo2` (sandbox
  локальный) или ходит на общий сервер?
- **Рекомендация:** MVP — локальный stdio-процесс на workspace; общий
  сервер — следующий шаг.
- **Обновить:** `SPECIFICATION.md` §2.3/§4.1, `agent_minimal.feature`.
- **✅ Решение (2026-09-26):**
  1. **MVP: Notebook спавнит `credo-server` как локальный sidecar-процесс
     через stdio (JSON-RPC 2.0).** Это штатный режим демо.
  2. **credo-server работает в режиме «только MCP» (без REST)** — согласно
     Q27: без флагов сервер поднимает только stdio-транспорт.
  3. **Опубликованный bare-репозиторий** (`.credo/published-repo/`) —
     локальный, на машине пользователя. Совместная работа — вне MVP
     (согласуется с Q32: workspace-репозиторий и published-repo локальны).
  4. **Notebook автоматически запускает `credo-server` при старте**
     (скрыто от пользователя). Пользователь видит только UI.
  5. **HTTP+SSE транспорт и внешний (общий) сервер — вне MVP**, вернутся
     для multi-user сценариев (v0.2+).
  - **Следствие для кода:** текущий `credo2` уже работает как MCP-сервер
    по stdio — правок в коде не требуется (интеграция — задача Notebook).
  - **Обновлено:** `SPECIFICATION.md` §2.2/§2.3/§4.1 (stdio — транспорт
    MVP, credo-server как sidecar Notebook, внешний сервер — v0.2+) и §10
    (решение №29), `agent_minimal.feature` (автозапуск sidecar, режим
    «только MCP»), `features/README.md` (заметка про транспорт MCP).

---

## 6. Notebook, LSP, UI

### Q31. ✅ Где живёт чат агента (решено 2026-09-26)

- **Расхождение:** SPEC §10 п. 12: «Чат-интерфейс: отдельно от основного
  UI | Обсуждается отдельно». `notebook_ui.feature`: правая панель
  «содержит чат с агентом», `agent_minimal.feature` описывает панель в
  приложении.
- **Рекомендация:** в MVP — панель в основном окне; удалить п. 12 или
  переформулировать.
- **Обновить:** `SPECIFICATION.md` §10, `notebook_ui.feature`.
- **✅ Решение (2026-09-26):**
  1. **Чат с агентом — правая панель основного окна Notebook**, третья
     колонка трёхколоночного layout (файловое дерево | редактор | чат).
     По умолчанию видима; можно стянуть через resize или скрыть (toggle).
  2. **Чат общий для всего workspace**, но агент получает контекст
     активного правила (открытый таб) как часть MCP-запроса.
  3. **Результаты исполнения** (`Explanation` из `check.test`):
     - не показываются отдельным сообщением в чате;
     - отображаются инлайн в редакторе: подсветка строки условия,
       которое сработало, плюс блок результата под правилом
       (человекочитаемый формат из `Explanation`);
     - это реализует принцип «результат рядом с кодом».
  4. **Рассуждения агента** (chain-of-thought, вызовы инструментов):
     - отображаются в чате как живой диалог;
     - вызовы инструментов — сворачиваемые блоки («▶ Вызов
       check.create», «▶ Вызов check.test»);
     - JSON-ответы инструментов в чате не показываются, только краткое
       человекочитаемое резюме.
  5. **Отдельное окно чата** (внешний Slack) — вне MVP.
  - **Обновлено:** `features/notebook_ui.feature` (панель чата: видима по
    умолчанию, resize/toggle), `features/agent_minimal.feature` (контекст
    активного правила, инлайн-результаты, свёрнутые вызовы без JSON),
    `features/inline_execution.feature` (подсветка сработавшего условия),
    `features/README.md` (счётчики, заметка), `SPECIFICATION.md` §10
    (решение №12 переформулировано).

### Q32. ✅ Два git-репозитория и мультиверсионность (решено 2026-09-26)

- **Расхождение:** SPEC §4.1 — у Notebook локальный `git CLI` и
  «локальный репозиторий»; §4.2 — у сервера свой bare-репозиторий
  публикаций. `git_integration.feature` показывает статус ветки, diff,
  commit и «Опубликовать» — непонятно, к какому репозиторию.
- **Вопрос:** какие операции UI относятся к workspace-репозиторию, а какие
  — к публикациям?
- **Рекомендация:** UI работает с workspace-репо; «Опубликовать» —
  отдельная операция MCP, результат виден в панели публикаций.
- **Обновить:** `git_integration.feature`, `SPECIFICATION.md` §4.1/§4.2.
- **✅ Решение (2026-09-26):**
  1. **Три уровня источника правды:**
     - грязный буфер редактора → черновик (draft-first, `check.create`)
       → локальный прогон `check.test`; единый механизм — Q33;
     - файл на диске (`rules/*.dar`) → для git-операций
       (`commit` / `publish`);
     - опубликованная версия → для исполнения «как в проде»
       (явный выбор версии).
     По умолчанию: грязный буфер → черновик; иначе → файл на диске.
  2. **Панель git в Notebook работает только с workspace-репозиторием.**
     Опубликованный bare-репозиторий — внутренняя деталь сервера;
     пользователь видит его содержимое через панель «Версии»
     (read-only, источник — манифест / `check.list_published`).
  3. **Переключение версий** — выпадающий список в тулбаре редактора,
     группировка по major.minor (1.x, 2.x). Workspace при
     переключении не затирается.
  4. **Структура реестра публикаций** (уточнение к Q13):
     `checks/{name}/{X}/{Y}/{Z}/`,
     где X = major, Y = minor, Z = patch (без избыточности).
     Внутри: `rule.json` + `contract.json` + `meta.json`.
  5. **REST** — без изменений: канон Q20
     `/checks/{name}/versions/{version}/evaluate`; сервер внутренне
     маппит `{version}` → `{X}/{Y}/{Z}/` для доступа к файлам реестра.
  6. **Публикация только с новым номером версии:** существующую
     версию переопубликовать нельзя (отказ с предложением
     следующего номера); downgrade запрещён.
  7. **Сопровождение нескольких версий** (хотфикс 1.0.x при живой
     разработке 2.0.0): рабочая ветка создаётся от линии 1.0.x,
     правка, публикация с bumped-номером (1.0.1); линия 2.0.0 не
     затрагивается.
  8. **Pre-release версии** (1.0.0-rc.1) — вне MVP, возвращаются в v0.2
     (Q18: сравнение — числовое по семантике semver; публикация — по
     решению Q32 остаётся вне MVP). Когда дойдём — вариант А:
     `checks/{name}/{X}/{Y}/{Z-prerelease}/`.

  **Примечание:** п. 5 не отменяет и не изменяет канон Q20
  (путь с `/versions/`). «Короткий формат» означает
  передачу версии одной строкой вместо трёх сегментов
  пути, а не изменение URL-шаблона.

  - **Следствие для кода:** `lib.rs` строит путь `checks/{name}/{version}`;
    требование Q13/Q32 — `checks/{name}/{X}/{Y}/{Z}`. Правка кода — отдельная
    задача; в этих правках код не менялся, требование зафиксировано в фичах,
    статусы затронутых файлов понижены до 🟡 (`features/README.md`).
  - **Обновлено:** `features/storage_paths.feature` (раскладка X/Y/Z,
    pre-release — вне MVP), `features/git_integration.feature`
    (workspace-only git-панель, read-only «Версии», переключатель версий,
    хотфикс-линия), `features/agent_minimal.feature` (буфер → черновик →
    `check.test`; файл — публикация), путь `checks/{name}/{X}/{Y}/{Z}/` приведён в
    `publish.feature`, `publish_rules.feature`, `immutability.feature`,
    `deprecation.feature`, `mcp_tools.feature`; `features/README.md`
    (счётчики, статусы, заметки); `SPECIFICATION.md` §4.1/§4.2/§7/§10;
    `OPEN_QUESTIONS.md` Q13 (пометка об уточнении).

### Q33. ✅ Чем исполняется правило в редакторе (решено 2026-09-26)

- **Расхождение:** `inline_execution.feature` — кнопка «Выполнить» прямо
  под правилом; `editor.feature` — диагностика из LSP; `agent_minimal` —
  `check.test` для черновика. LSP не исполняет правила, а источник данных
  для inline-запуска не определён (локальное ядро Tauri? MCP?).
- **Вопрос:** какой движок исполняет код из редактора и как туда попадает
  правило (сохранённый файл, черновик, несохранённый буфер)?
- **Рекомендация:** исполнять **буфер** через локальный `dar-core` (Tauri),
  без обязательного создания черновика; кнопку из `agent_minimal` не
  смешивать.
- **Обновить:** `inline_execution.feature`, `SPECIFICATION.md` §2.2/§4.1.
- **✅ Решение (2026-09-26):**
  1. **Единый механизм исполнения:** кнопка «Выполнить» в редакторе и
     агент через чат — оба идут через MCP `check.test`. Локального
     исполнения через Tauri `dar-core` **нет**.
  2. **Кнопка «Выполнить» активна только при наличии черновика.** Если
     черновик не создан/не сохранён — кнопка неактивна, пользователю
     предлагается сохранить правило (создать черновик).
  3. **Поток исполнения:** (а) пользователь сохраняет правило → создаётся/
     обновляется черновик; (б) кнопка «Выполнить» активируется; (в) клик →
     `check.test` на черновике → `Explanation`.
  4. **Источники данных для исполнения:** черновик — обязателен для
     `check.test`; опубликованная версия — `check.run` (не через черновик).
  5. **Семантика «сохранить» (draft-first):** «Создать черновик» →
     `check.create(source = буфер)`, файл не создаётся; кнопка «Выполнить»
     активируется после создания черновика; файл `rules/{name}.dar`
     материализуется при публикации (Q12). Противоречия с Q12 нет: Q33
     требует наличия черновика, а не его происхождения из файла.
  - **Следствие для кода:** `check.run` (исполнение опубликованной версии,
    п. 4) в текущем `credo2` отсутствует — требование зафиксировано в
    `mcp_tools.feature`, реализация — отдельная задача. `check.test` уже
    работает.
  - **Обновлено:** `features/inline_execution.feature` (кнопка неактивна
    без черновика, draft-first, исполнение через `check.test`),
    `features/agent_minimal.feature` (агент — через `check.test` на
    черновике), `features/draft.feature` (черновик из буфера без файла),
    `features/mcp_tools.feature` (`check.run`), `features/README.md`
    (счётчики, заметки), `SPECIFICATION.md` §2.2/§4.1/§4.2/§7 и §10
    (решение №30); `OPEN_QUESTIONS.md` Q32 (ссылка на Q33).

### Q34. ✅ Тесты: внутри `.dar` или в `.dar-notebook/` (решено 2026-09-26)

- **Расхождение:** SPEC §1.2: `.dar` — одновременно тест; §4.3: каталог
  `tests/` с `*.тест`; `inline_execution.feature`: последние входные данные
  и несколько наборов тестов сохраняются в `.dar-notebook/`
  (`results-cache.json`), то есть вне git.
- **Вопрос:** где канонические тесты и можно ли их версионировать?
- **Рекомендация:** для MVP — тесты в `.dar-notebook/` (локально), формат
  `tests/*.тест` не вводить; принцип «тест в файле» перенести в целевую
  версию.
- **Обновить:** `SPECIFICATION.md` §1.2/§4.3, `inline_execution.feature`.
- **✅ Решение (2026-09-26):**
  1. **В MVP тесты живут только в локальном кэше Notebook:**
     `.dar-notebook/results-cache.json`. Они не версионируются в git.
  2. **«Быстрые прогоны»** (ввёл JSON в UI → нажал «Выполнить»)
     сохраняются в кэш для удобства повторного запуска. Это черновики
     тестов.
  3. **Канонические файлы тестов** (формат `tests/*.тест`, принцип
     code=doc) — вне MVP, возвращаются в v0.2.
  4. **Готовность правила к публикации** проверяется не наличием файла
     теста, а фактом успешного `check.test` на черновике (Q16): сервер
     хранит `last_test_checksum` и `tested_at`, публикация без успешного
     теста отклоняется.
  5. **`.dar-notebook/` — в `.gitignore`**, не попадает в
     workspace-репозиторий.
  - **Следствие для кода:** учёт `last_test_checksum`/`tested_at` и проверка
    при публикации в `credo2` отсутствуют — отдельная задача (Q16);
    в этих правках код не менялся.
  - **Обновлено:** `features/inline_execution.feature` (кэш
    `.dar-notebook/results-cache.json`, вне git), `features/publish.feature`
    (`last_test_checksum`/`tested_at`, гейт публикации),
    `features/test_draft.feature` (метка тестирования),
    `features/notebook_ui.feature` (структура workspace без `tests/`,
    `.gitignore`), `features/README.md` (счётчики, заметки),
    `SPECIFICATION.md` §1.2/§4.2/§4.3 и §10 (решение №32);
    `OPEN_QUESTIONS.md` Q16 (закрыт в рамках Q34).

### Q35. ✅ Создание workspace и шаблоны (решено 2026-09-26)

- **Расхождение:** `notebook_ui.feature` создаёт `rules/` и `README.md`
  (после Q34/Q37 — без `tables/` и `tests/`); содержание README и шаблона
  правила нигде не задано.
- **Рекомендация:** зафиксировать шаблон (файл-пример правила; `.gitignore`
  для `.dar-notebook/` уже добавлен по Q34).
- **Обновить:** `notebook_ui.feature`, `file_management.feature`,
  `SPECIFICATION.md` §4.3.
- **✅ Решение (2026-09-26):**
  1. **Новый workspace** создаёт: `rules/`, `rules/Пример.dar`
     (канонический пример из `GRAMMAR.md` §1), `README.md` (структура,
     пример, шаги «создать → выполнить → опубликовать») и `.gitignore`
     с записями `.credo/` и `.dar-notebook/` (Q19). README ссылается на
     файл-пример, а не дублирует его.
  2. **«Создать правило»** создаёт файл `rules/{Имя}.dar` с **парсящимся**
     шаблоном (никаких плейсхолдеров вроде `Поле < Значение` — regex-парсер
     v0.1 сразу дал бы красную диагностику). Имя правила в шаблоне = имя
     файла; имя проверяется по грамматике v0.1 (буквы/цифры/`_`, включая
     кириллицу, `GRAMMAR.md` §1).

     Канонический шаблон (`{Имя}` подставляется из диалога):

     ```dar
     // Новое правило: отредактируйте условие и решение
     Правило {Имя} {
       Если (Клиент.Возраст < 21) {
         Решение = Отказ;
         Причина = "Причина отказа";
       }
     }
     ```
  3. **Черновик при создании файла не создаётся** — он появляется при
     сохранении/тестировании (draft-first, Q33); кнопка «Выполнить»
     активируется после появления черновика.
  4. Шаблон согласован с `GRAMMAR.md` v0.1: одно сравнение, `Решение = …;`,
     `Причина` — строка; допускается комментарий `//`.
  - Следствие: `notebook_ui.feature` и `file_management.feature` уточнены
    (новых сценариев нет — шаги внутри существующих), `SPECIFICATION.md`
    §4.3 дополнен деревом и заметкой; число сценариев не менялось
    (актуальные числа — в `features/README.md`).

### Q36. ✅ Конвейеры: несколько правил в одном файле? (решено 2026-09-24)

- **Расхождение:** `lsp.feature` (documentSymbol) ожидает «Конвейер
  ПотребКредит» с 3 правилами и 1 скорингом; SPEC §10 п. 14 — «одно
  правило = один файл»; `graph_view.feature` (граф конвейера) — ⏳
  отложен.
- **Вопрос:** поддерживаем ли `Конвейер`/скоринг/таблицы в MVP?
- **Рекомендация:** нет; убрать `Конвейер` из LSP-сценария (заменить на 3
  отдельных файла), граф оставить отложенным.
- **Обновить:** `lsp.feature`, `graph_view.feature`, SPEC §10.
- **✅ Решение (2026-09-24):** конвейеры, скоринги и таблицы — вне MVP
  (язык v0.1, `GRAMMAR.md` §1/§4). Сценарий documentSymbol в `lsp.feature`
  трактуется как «несколько открытых файлов» (workspace symbols), граф —
  остаётся отложенным (`graph_view.feature` ⏳). Возврат — v0.2 вместе с
  `Приоритет` (Q4).

### Q37. ✅ Реестр полей и типов для автодополнения (решено 2026-09-26)

- **Расхождение:** `lsp.feature` требует подсказки `Клиент.Возраст`,
  `Клиент.Доход` с типом поля; откуда берутся схемы — ни SPEC, ни фичи не
  говорят. Каталог `tables/` упомянут, но формат не определён.
- **Вопрос:** источник схемы полей для completion/hover: файл `tables/`,
  контракт опубликованных проверок, конфиг workspace?
- **Рекомендация:** для MVP — не подсказывать поля (только ключевые
  слова), иначе вводить `tables/` с форматом.
- **Обновить:** `lsp.feature`, `SPECIFICATION.md` §4.3.
- **✅ Решение (2026-09-26):**
  1. **Архитектурный источник схемы полей и словарей — БД**
     (пользовательские и системные таблицы). В MVP БД = `HashMap`, явной
     схемы нет. Векторная БД — отложена (после MVP).
  2. **Каталог `tables/` — рудимент, полностью исключается:** данные
     приходят из БД, а не из `.dar`-файлов таблиц. Убран из
     `SPECIFICATION.md` §4.3, `notebook_ui.feature`, `lsp.feature`;
     синтаксис таблиц в `GRAMMAR.md` не вводится.
  3. **Автодополнение MVP — два источника:**
     - ключевые слова языка: `Правило`, `Если`, `Решение`, `Причина`
       (статический список из `GRAMMAR.md`);
     - эвристика по открытым файлам: все слова с точкой (например,
       `Клиент.Возраст`), встреченные в тексте.
  4. **Типы полей в MVP не подсказываются** (явной схемы нет). Целевая
     траектория — строгая статическая типизация; схема колонок будет
     браться из БД (Q9: в MVP типы проверяются на исполнении).
  5. **Словарь решений** для автодополнения `Решение = `:
     - источник — системная таблица решений банка в БД (пользователь
       расширяет свободно, набор не ограничен);
     - в MVP — демо-конфиг, имитирующий системную таблицу решений,
       заполненную пользователем банка (включая нестандартные решения из
       реальной практики, не только «Одобрить/Отказать»);
     - фиксированный набор в коде ядра не хардкодится.
  6. **Векторная БД — отложена (после MVP).** Абстракция источника данных
     (trait) не закладывается сейчас; `evaluate_rule` работает с
     `HashMap` напрямую. Переход на БД — отдельная задача.
  - **Следствие для кода:** правок не требуется (автодополнение — задача
    LSP/`lsp-dar`, Фаза 2; каталог `tables/` в коде не используется).
  - **Обновлено:** `features/lsp.feature` (источники автодополнения,
    без типов полей, словарь решений — демо-конфиг),
    `features/notebook_ui.feature` (без `tables/`),
    `features/README.md` (заметки, строки), `SPECIFICATION.md` §4.3 и §10
    (решение №33), `DECISIONS.md` (ADR-002: БД как источник данных).

### Q38. ✅ Состав LSP сверх SPEC (решено 2026-09-24)

- **Расхождение:** `lsp.feature` требует formatting, semanticTokens,
  definition, references, documentSymbol, hover; `SPECIFICATION.md` §3.3/§5
  перечисляет только диагностику, автодополнение и символы, а модули
  `dar-core` — `completion.rs`/`explain.rs` без `format`/`tokens`.
- **Вопрос:** какое подмножество LSP обязательно для MVP и где оно
  реализуется?
- **Рекомендация:** MVP — diagnostics + completion + semantic tokens;
  остальное — целевом состоянии.
- **✅ Решение (2026-09-24):** LSP входит в MVP (пользователь подтвердил).
  Обязательный состав: initialize/handshake, синхронизация документов
  (didOpen/didChange), diagnostics, completion (ключевые слова + значения
  решений), hover по правилу, documentSymbol/workspaceSymbols (по одному
  правилу в файле — см. Q36), semanticTokens. Реализация — крейт `lsp-dar`
  поверх API `parse_rule` (Фаза 2). Вне MVP → v0.2: formatting, definition,
  references, completion полям из реестра (Q37). Сценарии `lsp.feature` с
  `Приоритет`, полями и конвейером помечаются как целевое состояние.
- **Обновить:** `SPECIFICATION.md` §3.3/§5, `lsp.feature`, §8 (Фаза 2).

### Q39. ✅ Поведение при падении LSP (решено 2026-09-26)

- **Расхождение:** SPEC §10 п. 6 — «Нет fallback при падении LSP;
  перезапуск sidecar»; `lsp_notebook.feature` — автоперезапуск + уведомление
  + «состояние открытых файлов восстанавливается».
- **Вопрос:** что значит «нет fallback» и что восстанавливается
  (диагностики? позиция курсора?).
- **Рекомендация:** переформулировать п. 6: «перезапуск и повторная
  синхронизация открытых документов; деградация — без подсветки».
- **Обновить:** `SPECIFICATION.md` (§10 п. 6),
  `lsp_notebook.feature`, `features/README.md` (строка и счётчики).
- **✅ Решение (2026-09-26):** принимается рекомендация с уточнениями.

  1. **«Нет fallback» — нет второго движка.** При недоступности
     sidecar функции LSP (диагностика, автодополнение, семантическая
     подсветка) недоступны; редактор работает как обычный текстовый.
     Встроенной в UI альтернативы нет: `dar-core` доступен только через
     LSP (согласуется с Q33 — локального исполнения в UI нет).
  2. **Восстанавливается серверная сессия, а не «состояние» файлов.**
     Буферы, курсор, скролл и undo — клиентские (CodeMirror), падение
     sidecar их не теряет. После перезапуска клиент выполняет
     `initialize` и повторный `didOpen` по всем открытым документам с
     текущим текстом (включая несохранённые правки); сервер заново
     публикует диагностику.
  3. **Ограничение автоперезапуска.** Автопопытки подряд ограничены
     (канон — три); после их исчерпания автоперезапуск прекращается,
     пользователь видит уведомление «Сервер языка недоступен» и может
     перезапустить сервер вручную. Бесконечный цикл падений исключён.
  4. **Деградация не блокирует работу.** Во время недоступности LSP
     редактирование и сохранение продолжаются, буферы и несохранённые
     правки сохраняются; выполнение правила (`check.test` через MCP,
     Q33) от LSP не зависит.

  - Следствие для кода: `lsp-dar` и Notebook не реализованы (Фаза 2–3,
    Q38), правок кода нет. Фича: сценарий «Перезапуск LSP при падении»
    уточнён, добавлены 2 поведенческих сценария (деградация без LSP;
    повторные падения → ручной перезапуск), 6 → 8 сценариев.
    `SPECIFICATION.md` §10 п. 6 переформулирован.

---

## 7. Процесс

### Q40. ✅ Исполняемые ли Gherkin-сценарии (решено 2026-09-24)

- **Расхождение:** критерий готовности SPEC §9: «Все сценарии из
  соответствующих `.feature` файлов проходят». При этом нет маппинга
  сценариев на тесты и нет раннера (Cucumber и т. п.);
  `testing.feature` перечисляет только Rust-тесты. Проверить DoD сейчас
  невозможно.
- **Вопрос:** фичи — исполняемая спецификация (cucumber-rs) или
  документация, а DoD = `cargo test`?
- **Рекомендация:** в MVP — документация, DoD привязать к конкретным
  Rust-тестам; ввести cucumber — при переходе к целевому состоянию.
- **Обновить:** `SPECIFICATION.md` §9, `testing.feature`, `ci.yml`.
- **✅ Решение (2026-09-24):** в MVP `.feature` — **документация**
  (требования и чек-листы), не исполняемая спецификация. DoD SPEC §9:
  `cargo fmt --check` + `cargo clippy -- -D warnings` + `cargo test --all`
  плюс ручной прогон UI-сценариев. Проверяемость инвентаризации даёт новый
  тест `tests/features_inventory.rs`: структура фич, полнота перечня,
  счётчики и итоги README. Cucumber-rs — v0.2, после стабилизации языка и
  API, только для backend-сценариев; UI-сценарии остаются ручными.

### Q41. ✅ Владелец и синхронизация документов (решено 2026-09-26)

- **Расхождение:** статусы и цифры дублируются в `SPECIFICATION.md` и
  `features/README.md`; `DECISIONS.md` ведётся (ADR-001, ADR-002), но
  покрывает не все решения; `AGENTS.md` актуализирован под `credo2`
  (проверено 2026-09-26).
- **Вопрос:** кто владелец каждого документа и где фиксируются решения?
- **Рекомендация:** решения — в `DECISIONS.md` (ADR-строки), статусы — только
  в `features/README.md`, SPEC не дублирует статусы; `AGENTS.md` обновить
  под `credo2` (или пометить как отдельный прототип).
- **Обновить:** `SPECIFICATION.md` (§6.1, §10), `DECISIONS.md` (ADR-003),
  `AGENTS.md` (раздел «Документы и решения», ссылки).
- **✅ Решение (2026-09-26):** принята политика **«один факт — один канон»**.

  Роли документов (при расхождении канон — из этой таблицы):

  | Документ | Канон чего |
  |---|---|
  | `prototypes/credo2/SPECIFICATION.md` | целевая архитектура; §10 — краткие формулировки решений |
  | `prototypes/credo2/OPEN_QUESTIONS.md` | полный контекст «вопрос → решение»; статусы вопросов |
  | `prototypes/credo2/features/README.md` | **единственный** канон статусов, приоритетов и счётчиков сценариев (Q5/Q40) |
  | `prototypes/credo2/GRAMMAR.md` | канон языка v0.1 (EBNF включён, Q2) |
  | `DECISIONS.md` (корень) | реестр **сквозных архитектурных** ADR (кратко + источники) |
  | `concept.md`, `SCOPE.md`, `NON_GOALS.md`, `ROADMAP.md`, `open-questions.md` | рамки и продуктовые вопросы DAR (не технические детали CREDO) |
  | `AGENTS.md` (корень) | операционная инструкция агентам |
  | `prototypes/credo2/CHANGELOG.md`, `CONFORMANCE/` | хронология прототипа; принципы тестов соответствия |
  | `prototypes/credo2/tasks/` | реестр задач по коду: сводка + папка на задачу (не норматив) |

  Правила:
  - **Статусы и числа — только в `features/README.md`.** SPEC их не
    дублирует (§6.1 ссылается на README; актуальность проверяет
    `tests/features_inventory.rs`), ручная сверка не нужна.
  - **Новое решение** при закрытии вопроса: краткая строка в SPEC §10;
    полный контекст — в `OPEN_QUESTIONS.md`; если решение
    сквозное/архитектурное — дополнительно ADR в `DECISIONS.md`.
  - **Расхождение с кодом** фиксируется явно: статус 🟡 требования в
    `features/README.md` и задача в `tasks/`; «молчаливых» расхождений
    нет.
  - **`AGENTS.md`** описывает текущую сборку прототипа (как собрать и
    вызвать); канон требований и контрактов — SPEC/OPEN_QUESTIONS; при
    расхождении приоритет у канона.
  - **Корневые DAR-документы** (`concept.md`, `SCOPE.md`, `NON_GOALS.md`,
    `ROADMAP.md`, `open-questions.md`, заглушки `GRAMMAR.md`/`EBNF.md`) —
    вне периметра текущей работы над прототипом; канон языка для
    прототипа — `prototypes/credo2/GRAMMAR.md`; корневые документы
    обновляются после демо (решение 2026-09-26).

  - Следствия (выполнено): SPEC §6.1 — числа убраны, ссылка на README +
    тест; SPEC §10 — шапка «краткий канон / контекст / ADR»;
    `DECISIONS.md` — ADR-003 «Владение документами и синхронизация»;
    `AGENTS.md` — раздел «Документы и решения» и корректные ссылки.
    Фичи и счётчики не менялись (актуальные числа — в
    `features/README.md`).

---

## Матрица: документ → что править после решений

| Документ | Вопросы для правки |
|---|---|
| `SPECIFICATION.md` §1.3, §4, §5, §6, §7, §8, §10, §11 | Q1–Q4, Q6–Q7, Q12–Q15, Q19–Q23, Q27, Q29–Q39, Q41 |
| `features/README.md` | Q1, Q5, Q6, Q14, Q17 |
| `features/lexer.feature`, `parser.feature`, `execution.feature`, `errors.feature`, `explain.feature`, `explain_full.feature` | Q2–Q4, Q8–Q11, Q42 |
| `draft.feature`, `test_draft.feature`, `publish.feature`, `publish_rules.feature`, `immutability.feature`, `storage_paths.feature`, `semver.feature`, `deferred.feature` | Q12–Q18, Q28–Q29 |
| `rest_api.feature`, `rest_auth.feature`, `evaluate.feature`, `batch.feature`, `dashboard.feature`, `import_export.feature` | Q20–Q27, Q42 |
| `notebook_ui.feature`, `editor.feature`, `inline_execution.feature`, `git_integration.feature`, `agent_minimal.feature`, `file_management.feature`, `graph_view.feature` | Q12, Q15, Q31–Q37 |
| `lsp.feature`, `lsp_notebook.feature` | Q10, Q36–Q39 |
| `GRAMMAR.md`, `EBNF.md`, `DECISIONS.md`, `AGENTS.md`, `concept.md` | Q2, Q3, Q40, Q41 |

## Минимальный набор решений до старта (если нет времени на всё)

1. ✅ Q1 — решено: эволюция `credo2` (не greenfield).
2. ✅ Q2/Q3 — решены: канон `GRAMMAR.md`, парсер MVP — regex-минимум,
   стратегия LSP-совместимости зафиксирована (GRAMMAR.md §3). Q4 также
   решён: `Приоритет` вне MVP (вернётся с конвейерами в v0.2).
3. ✅ Q8/Q9/Q42/Q10 — решены 2026-09-24: строгая ошибка при отсутствующем
   поле, проверка типов на исполнении, канон объяснения (латиница +
   русские тексты), словарь решений задаёт банк (`Pass` убран).
4. ✅ Q12/Q13 — решено: источник истины — `.dar`-файл, публикация —
   артефакт `checks/{name}/{X}/{Y}/{Z}/` (Q12, Q13; уточнение пути — Q32).
   Q14/Q15 также решены: ветка `publish/{name}-{version}`, мерж — отдельный
   шаг `credo merge`. ✅ Q32 — решено 2026-09-26: два git-репозитория,
   мультиверсионность, панель «Версии», хотфикс-линии (см. Q32).
5. ✅ Q20/Q21 — решены: канонический путь
   `/checks/{name}/versions/{version}/...` и MVP-минимум REST; схема
   `GET /checks` — манифест (`name`/`active`/`supported`/`deprecated`,
   `active` пуст при всех deprecated). ✅ Q22 — решено 2026-09-25:
   `x-api-key`, единственный серверный ключ, текст 401, открытые пути.
6. ✅ Q28 — решено 2026-09-26: контракт `check.create` — `{name, source}`
   (оба обязательны, «сохранить = обновить»). ✅ Q29 — решено 2026-09-26:
   JSON-контракты MCP-инструментов — `SPECIFICATION.md` §4.5 (коды ошибок,
   инварианты `stale`/`test_valid`, полные схемы — Q29). ✅ Q30 — решено
   2026-09-26: транспорт — локальный stdio sidecar, автозапуск Notebook,
   режим «только MCP»; HTTP+SSE/внешний сервер — v0.2+.
7. ✅ Q33/Q34 — решены 2026-09-26: единый механизм исполнения — MCP
   `check.test` на черновике (draft-first), локального исполнения нет;
   опубликованная версия — `check.run`; тесты — только локальный кэш
   `.dar-notebook/results-cache.json` (вне git), `tests/*.тест` — v0.2.
8. ✅ Q40 — решено: фичи — документация (cucumber-rs вне MVP); DoD =
   `cargo fmt --check` + `cargo clippy` + `cargo test` + ручной UI-прогон.

---

## Приложение A. Перенос из `features/README.md`

Раздел «Известные расхождения и заметки» из `features/README.md` удалён,
чтобы README не дублировал перечень проблем. Соответствие бывших заметок:

| Былая заметка | Где теперь |
|---|---|
| 1. Коллизия имени `explain.feature` | Закрыто — A1 |
| 2. Аутентификация | Q22 |
| 3. Поля манифеста | Q21 |
| 4. Отсутствующее поле | Закрыто — Q8 (решено 2026-09-24) |
| 5. Формат ветки публикации | Закрыто — Q14 (решено 2026-09-25) |
| 6. Иммутабельность | Закрыто — Q17 (решено 2026-09-26) |
| 7. Числовая сортировка prerelease | Закрыто — Q18 (решено 2026-09-26) |
| 8. Разделение LSP | Закрыто — A2; учёт в SPEC — Q6 |
| 9. Счётчик сценариев `editor.feature` | Q6 |
| 10. Синтаксис `Приоритет` | Q4 (см. также Q2, Q3) |

### A1. Коллизия имени `explain.feature` — закрыто

Изначально было два разных раздела объяснимости с одинаковым именем файла.
Первый (языковой, «Движок объясняет каждое решение») сохранён как
`features/explain.feature`, второй («Полное объяснение каждого решения») —
как `features/explain_full.feature`. Схема объяснения канонизирована
(ключи латиницей, поле `condition`, тексты русские) — Q42, решено 2026-09-24.

### A2. Разделение LSP — закрыто

LSP описано базовым файлом `features/lsp.feature` и отдельным
блоком «уточнения» про интеграцию с Notebook. Чтобы сохранить конвенцию
«один файл — одна `Функция`», уточнения вынесены в
`features/lsp_notebook.feature`. `SPECIFICATION.md` §5/§6.1
про второй файл не знает — учтено в Q6.

---

## Приложение B. Задачи по коду

Следствия принятых решений, требующие изменений в `credo2`, ведутся в
[`tasks/`](tasks/README.md): сводка задач (T-01…T-09) и папка на каждую
задачу с карточкой (`tasks/T-XX-<слаг>/README.md`) — приоритеты,
зависимости и критерии готовности. Статусы задач — только там; статусы
требований — в `features/README.md`.
