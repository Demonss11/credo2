# Открытые вопросы: `SPECIFICATION.md` ↔ `features/`

Документ фиксирует расхождения между `SPECIFICATION.md` (целевая архитектура)
и `features/*.feature` + `features/README.md` (требования), а также с текущим
кодом `credo2`. Цель — до начала разработки принять решения, чтобы каждая
формулировка жила в одном месте и в одном виде.

**Как пользоваться.** Для каждого вопроса впишите ответ в блок «Решение»
(кто и когда). После решения обновите документы из поля «Обновить».
Нумерация сквозная и не зависит от порядка разделов: Q42 добавлен позже
остальных.

Легенда приоритета:

- 🔴 **Блокер** — без ответа нельзя начинать соответствующую разработку.
- 🟡 **Важно** — можно начать, но решение нужно до конца MVP.
- ⚪ **Оформление** — не влияет на код, чинит противоречие в документации.
- ✅ **Решено** — вопрос закрыт, решение зафиксировано в блоке «Решение»,
  документы из поля «Обновить» синхронизированы.

---

## 0. Общие вопросы (определяют всё остальное)

### Q1. ✅ SPEC — план «с нуля» или доработка `credo2`? (решено 2026-09-24)

- **Расхождение:** `SPECIFICATION.md` §5/§8 описывает новый монорепозиторий
  `dar/` (`crates/dar-core`, `crates/lsp-dar`, `crates/credo-server`,
  `notebook/`) и 13-недельный план с Фазы 1. Фактически уже существует
  рабочий прототип `prototypes/credo2` (крейт `credo2`: `core.rs`, `lib.rs`,
  `mcp.rs`, `rest.rs`, `main.rs`), а `features/README.md` помечает часть
  требований как ✅ реализованные.
- **Вопрос:** SPEC — это задание на переписывание с нуля или целевая
  архитектура, к которой `credo2` эволюционирует?
- **Варианты:** (а) greenfield по SPEC, `credo2` — референс; (б) эволюция
  `credo2` до структуры SPEC; (в) `credo2` остаётся монолитом, SPEC
  переписывается под него.
- **Рекомендация:** (б) с честной пометкой в SPEC, что Фазы 1–4 частично
  закрыты прототипом.
- **Обновить:** `SPECIFICATION.md` §5, §8, §10; `features/README.md`.
- **✅ Решение (2026-09-24):** вариант **(б) — эволюция `credo2`**, не
  greenfield. Монорепозиторий `dar/` из SPEC — целевая структура, к которой
  `credo2` перестраивается поэтапно: сначала выделение `dar-core` и
  `credo-server` из текущего крейта, `lsp-dar` и `notebook/` — новые крейты
  в том же workspace. Greenfield-ветка отклонена.
  - В `SPECIFICATION.md`: добавлена служебная заметка «Статус реализации»
    после заголовка; §5 переименован («Целевая структура монорепозитория»,
    с текущим маппингом `credo2::core → crates/dar-core`,
    `credo2::{mcp,rest} → crates/credo-server`); §8 — Фазы 1 и 4 помечены
    как частично закрытые прототипом (`credo2`), план переведён в режим
    «эволюция»; §10 — добавлено решение №15 (эволюция `credo2`).
  - `features/README.md`: в шапке зафиксировано, что требования
    верифицируются на эволюционирующем `credo2`, а не на отдельном
    greenfield-проекте.
  - Следствие для Q3/Q4: поскольку база — `credo2` с regex-парсером,
    решения по минимальному DSL принимаются в контексте этой базы
    (см. Q3).

### Q2. ✅ Где канонический DSL: `concept.md`, SPEC или features? (решено 2026-09-24)

- **Расхождение:** корневой `concept.md` описывает `Тогда` (строки 91, 162,
  257, 275, 309, 342), `Приоритет` (109, 212, 220, 267, 307, 365, 385) и
  doc-блоки `:::` (520–524). `SPECIFICATION.md` §1.3 перечисляет ключевые
  слова `Правило, Если, Тогда, Решение`, а §10 п. 1 заменяет `:::` на `//`.
  Фичи (`lexer.feature`, `parser.feature`, `execution.feature`) описывают
  язык без `Тогда`, но с `Приоритет: 100;` и `Решение = ...;`.
  `GRAMMAR.md` и `EBNF.md` — пустые файлы по 13/10 байт.
- **Вопрос:** какая грамматика v0.1 канонична и где она лежит?
- **Рекомендация:** зафиксировать грамматику v0.1 в `GRAMMAR.md`/`EBNF.md`
  (одно правило — один абзац), `concept.md` пометить как «видение, не
  норматив».
- **Обновить:** `GRAMMAR.md`, `EBNF.md`, `concept.md` (шапка),
  `SPECIFICATION.md` §1.3.
- **✅ Решение (2026-09-24):** каноническая грамматика v0.1 зафиксирована в
  **`GRAMMAR.md`** (создан; включает EBNF-описание — пустой `EBNF.md` более
  не нужен, дублирование запрещено). `SPECIFICATION.md` §1.3 приведён в
  соответствие: ключевое слово `Тогда` из перечня убрано (действие в v0.1
  выражается `Решение = ...;`). Файл `concept.md` в репозитории отсутствует;
  при возврате — помечать шапкой «видение, не норматив». Статусы ключевых
  слов (`Тогда`/`Приоритет`/`:::`) — таблица GRAMMAR.md §4.

### Q3. ✅ Минимальный DSL (regex, одно сравнение) или полноценный лексер/AST? (решено 2026-09-24)

- **Расхождение:** `credo2::core::parse_rule` — регулярки: одно сравнение,
  нет `Приоритет`, нет типизированных ошибок, нет позиций. Фичи
  `lexer.feature`, `parser.feature`, `execution.feature`, `errors.feature`,
  `explain.feature` описывают токены, AST, `Приоритет`, ошибки
  «Файл пуст» / «Несовместимые типы» / «ожидалась }».
  `features/README.md` называет языковые фичи «целевым состоянием DAR».
- **Вопрос:** какой парсер обязателен для MVP — regex-минимум или лексер+AST?
- **Рекомендация:** для MVP — regex-минимум, но с явным списком «язык v0.1»
  в `GRAMMAR.md`; языковые фичи перевести в раздел «целевое состояние».
- **Обновить:** `SPECIFICATION.md` §3.1/§8 (Фаза 1), `features/README.md`,
  `GRAMMAR.md`.
- **✅ Решение (2026-09-24):** вариант **regex-минимум** (текущий
  `core::parse_rule`) — парсер MVP без отдельного лексера и публичного AST.
  Язык v0.1 формально зафиксирован в `GRAMMAR.md` (EBNF + ограничения).
  Конфликта с планированным LSP нет: принята стратегия «толстый парсер,
  тонкий интерфейс» — весь доступ к `.dar` только через `parse_rule()`,
  внутренние типы (`Rule/Condition/Action`) в v0.2 обогащаются позициями и
  типизированными ошибками без смены публичного контракта (GRAMMAR.md §3).
  Файлы `lexer.feature`/`parser.feature` остаются целевым состоянием (v0.2).

### Q4. ✅ `Приоритет` входит в MVP? (решено 2026-09-24)

- **Расхождение:** `parser.feature` (сценарий 2), `execution.feature`,
  `explain.feature`, `editor.feature`, `lsp.feature` используют
  `Приоритет: 100;`; `client_explanation.feature` требует сортировку причин
  «по приоритету правил»; `credo2` не поддерживает; `features/README.md`
  (заметка 10) признаёт расхождение.
- **Вопрос:** реализуем `Приоритет` в MVP или убираем из фич?
- **Рекомендация:** если конвейер из нескольких правил не в MVP (см. Q38),
  убрать `Приоритет` из фич MVP и вернуть вместе с конвейерами.
- **Обновить:** все фичи с `Приоритет`, `SPECIFICATION.md` §7.
- **✅ Решение (2026-09-24):** `Приоритет` убран из MVP — ключевое слово не
  входит в язык v0.1 (`GRAMMAR.md` §4). В v0.1 одно правило = один файл
  (SPEC §10 п. 14), конвейеров нет (Q36), поэтому конфликтующих правил не
  возникает и порядок применения не требуется. Реализация вернётся в v0.2
  вместе с конвейерами. Фичи, использующие `Приоритет: 100;`
  (`parser.feature`, `execution.feature`, `explain.feature`,
  `editor.feature`, `lsp.feature`, `client_explanation.feature`), остаются
  целевым состоянием до переноса синтаксиса в v0.2.

### Q5. ✅ Единая легенда статусов и приоритетов (решено 2026-09-24)

- **Расхождение:** `features/README.md` использует ✅/🟡/⬜/⏸
  (реализовано/расходится/план/отложено), `SPECIFICATION.md` §6.2 —
  🔴/🟡/🟢/⏳ (обязательные/желательные/вау/отложенные). Один и тот же файл
  получает разные метки (например, `rest_api.feature` — ✅ и 🔴).
- **Вопрос:** какая система меток одна? Что означает «сделано»?
- **Рекомендация:** оставить одну таблицу в `features/README.md`:
  «статус реализации» + «приоритет MVP» отдельными колонками.
- **Обновить:** `features/README.md`, `SPECIFICATION.md` §6.
- **✅ Решение (2026-09-24):** приняты **две независимые оси** с одним
  каноном — `features/README.md`:
  - **статус реализации** (✅/🟡/⬜/⏸) — что уже работает в `credo2`;
  - **приоритет MVP** (🔴/🟡/🟢/⏳) — что и когда нужно демо.
  У каждого файла в сводных таблицах README теперь обе колонки.
  `SPECIFICATION.md` §6.1 удалил дублирующую таблицу и ссылается на README,
  §6.2 оставляет только смысл уровней и привязку к фазам.
  - **«Сделано» (✅):** поведение реализовано, сценарии не противоречат
    коду; backend покрыт автотестом либо явно помечен «ручной прогон»
    (см. «Соответствие коду» в README), UI проверяется вручную по сценарию.
    🟡 — расхождение зафиксировано в `OPEN_QUESTIONS.md`; ⬜ — план;
    ⏸ — вне MVP.
  - **Приоритеты** файлов перенесены из SPEC §6.1; файлы, которых там не
    было, размечены по здравому смыслу: `lexer` ⏳; `parser`, `execution`,
    `explain`, `errors`, `semver`, `storage_paths`, `manifest`,
    `mcp_tools`, `testing`, `lsp_notebook` 🔴; `immutability`,
    `manifest_sync`, `deprecation` 🟡.
  - **Следствие:** Q6 закрыт попутно (см. ниже).

---

## 1. Инвентаризация и цифры

### Q6. ✅ Пересчитать сводную таблицу SPEC §6.1 (решено 2026-09-24, попутно с Q5)

- **Расхождение:** SPEC: 18 файлов / 93 сценария. Факт: 36 файлов / 191
  сценарий. Ошибки по категориям: REST — 7 против 22
  (`evaluate` 7 + `rest_api` 10 + `rest_auth` 5); объяснимость — 3 против 7
  (`explain` 4 + `explain_full` 3); редактор — 8 против 7; LSP — 6 против
  21 (`lsp` 15 + `lsp_notebook` 6). Не упомянуты вовсе: `lexer`, `parser`,
  `execution`, `errors`, `publish_rules`, `immutability`, `storage_paths`,
  `manifest`, `manifest_sync`, `deprecation`, `semver`, `mcp_tools`,
  `testing`, `deferred`, `lsp_notebook`.
- **Вопрос:** переписываем §6.1 из фактических файлов или оставляем как
  исторический срез?
- **Рекомендация:** заменить таблицу на актуальную, генерируемую
  (скрипт подсчёта сценариев).
- **Обновить:** `SPECIFICATION.md` §6.1.
- **✅ Решение (2026-09-24):** закрыт попутно с Q5. Таблица не
  пересчитывается, а **удалена** из SPEC как дублирующая: единственный
  канон инвентаризации — `features/README.md` (36 файлов / 191 сценарий;
  backend — 27/127, frontend DAR Notebook — 9/64). Актуальность счётчиков
  проверяет тест `tests/features_inventory.rs`, а не ручной пересчёт.

### Q7. ⚪ Термины в глоссарии

- **Расхождение:** §11 не содержит терминов, которыми оперируют фичи:
  «check», «active», «supported», «kind», «service_hash», «контракт»,
  «песочница», «манифест».
- **Вопрос:** дополнить глоссарий?
- **Рекомендация:** да, добавить перечисленные термины с привязкой к
  структурам (`ManifestEntry`, `CheckMeta`, `CheckContract`).
- **Обновить:** `SPECIFICATION.md` §11.

---

## 2. Язык и семантика

### Q8. ✅ Отсутствующее поле: ошибка или `0`? (решено 2026-09-24)

- **Расхождение:** `errors.feature`, `test_draft.feature`,
  `explain_full.feature`, `batch.feature` ожидают ошибку «Неизвестное
  поле». `evaluate_rule` (`core.rs`) подставлял `0.0`, из-за чего условие
  `< 21` для отсутствующего поля **срабатывало**. `AGENTS.md` фиксировал
  текущее поведение как особенность.
- **Вопрос:** какое поведение канонично для v0.1?
- **Варианты:** (а) строго — ошибка при отсутствии поля; (б) мягко — `0`,
  но с предупреждением в объяснении; (в) явный `null`/`unknown`.
- **Рекомендация:** (а) для `check.test` и REST, потому что иначе возможны
  ложные отказы; ложные срабатывания опаснее падения.
- **Обновить:** `core.rs::evaluate_rule`, `errors.feature`, фичи с этим
  ожиданием, `AGENTS.md`.
- **✅ Решение (2026-09-24):** вариант **(а) — строгая ошибка**.
  Разделение состояний «нет параметра» и «есть параметр, но пустой»
  плановое (v0.2+, вместе со строгой типизацией), но не вопрос MVP.
  - `core.rs`: `evaluate_rule` возвращает `Result<Explanation, EvalError>`;
    `EvalError::UnknownField(поле)` с русским сообщением «Неизвестное
    поле: …» (тексты согласованы с фичами, см. также Q11).
  - `rest.rs::eval_inner`: ошибка исполнения → HTTP 422
    `{ "error": "Неизвестное поле: …" }`.
  - `mcp.rs::test`: ошибка исполнения → ошибка инструмента MCP.
  - Фичи (`errors.feature`, `test_draft.feature`, `explain_full.feature`,
    `batch.feature`, `inline_execution.feature`) уже описывали строгое
    поведение — код приведён в соответствие, изменений в фичах не
    потребовалось. `AGENTS.md` в репозитории отсутствует.
  - Тесты: `core::tests::missing_field_is_strict_error_q8`,
    `missing_field_does_not_trigger_false_positive_q8`.

### Q9. ✅ Типизация и сравнение чисел (решено 2026-09-24)

- **Расхождение:** `errors.feature` требует ошибку «Несовместимые типы» при
  `Клиент.Возраст < "двадцать"`; `evaluate_rule` молча возвращал
  `matched = false`. SPEC §10 п. 13: `f64` в MVP, `Decimal` в v0.2;
  `rust_decimal` указан в §3.1 как зависимость.
- **Вопрос:** проверяем типы на этапе парсинга/исполнения? Что с `==` для
  `f64` (точное равенство или эпсилон)?
- **Рекомендация:** ошибка типов — в MVP; для `==` зафиксировать допуск
  либо запретить `==` для чисел вне целых.
- **Обновить:** `core.rs`, `errors.feature`, `SPECIFICATION.md` §10.
- **✅ Решение (2026-09-24):** строгая статическая типизация — плановая
  (v0.2), это не вопрос MVP. В MVP принято «как в рекомендации»:
  проверка типов **на исполнении**.
  - `core.rs`: сравнение значений разных типов →
    `EvalError::TypeMismatch` («Несовместимые типы: поле … имеет тип …,
    ожидался …»); строки поддерживают только `==`/`!=`, `<`/`>` для строк
    — та же ошибка типов.
  - Равенство `f64` (`==`/`!=`) остаётся точным; эпсилон не вводится
    (риски ложных срабатываний выше). Переход на `Decimal` снимет вопрос
    в v0.2 (SPEC §10 п. 13).
  - Замечание: `errors.feature` ожидает ошибку типов на этапе парсера —
    это целевое поведение лексера/AST (Q3: regex-минимум в MVP ловит
    ошибку на исполнении); сценарий остаётся целевым состоянием v0.2.
  - Тест: `core::tests::type_mismatch_is_error_q9`.

### Q10. ✅ Словарь решений и «не сработало» (решено 2026-09-24)

- **Расхождение:** `contract_from_rule` добавлял решение `"Pass"`;
  `lsp.feature` предлагает «Отказ», «Одобрено», «РучноеРассмотрение»;
  `client_explanation.feature` — «Заявка одобрена»; фичи используют «пусто»
  (`test_draft.feature`), «решение не установлено» (`execution.feature`),
  пустую строку (код).
- **Вопрос:** канонический набор решений и представление результата
  «правило не сработало»?
- **Рекомендация:** зафиксировать enum решений в грамматике + одно
  представление «нет решения» (`null` в JSON, пустая строка в ядре — как
  сейчас), убрать `"Pass"` из контракта или объяснить его.
- **Обновить:** `core.rs`, `lsp.feature`, `client_explanation.feature`,
  `GRAMMAR.md`.
- **✅ Решение (2026-09-24):** жёсткого перечня решений в ядре **нет** —
  словарь решений определяет банк, в каждом банке он свой. Ядро
  оперирует решениями как свободными идентификаторами (`Решение = <Имя>`
  в `GRAMMAR.md` уже так и определено).
  - Форму для заполнения словаря заложим при разработке БД/справочников
    (per-bank таблица/HashMap) — вне текущего кода, не блок MVP-ядра.
  - `contract_from_rule` больше **не** добавляет `"Pass"`: в контракт
    попадают только решения, реально используемые в правиле.
  - «Правило не сработало»: единое представление — пустая строка
    `decision`/`reason` в ядре, `""` в JSON объяснения (сценарии
    «пусто»/«решение не установлено» в фичах трактуются как «пустое
    значение», код менять не нужно).
  - Подсказки LSP (`lsp.feature`) смогут брать словарь из конфигурации
    банка — учтено в плане Q36–Q39.
  - Тесты: `contract_decisions_follow_rule_vocabulary_q10`,
    `unmatched_rule_has_empty_decision_q10`.

### Q11. ✅ Язык сообщений об ошибках (решено 2026-09-25)

- **Расхождение:** фичи ожидают русские тексты («ожидалась }», «Файл пуст»,
  «Неизвестное поле», «черновик не найден»), код частично английский
  (`not found`, `missing or invalid x-api-key`, `unknown tool`).
- **Рекомендация:** русский текст + стабильный `code` в JSON ошибки.
- **Обновить:** `core.rs`, `rest.rs`, `mcp.rs`, фичи.
- **✅ Решение (2026-09-25):** канон — **все пользовательские сообщения
  по-русски**; латиницей остаются только идентификаторы (имена полей,
  заголовок `x-api-key`, имена инструментов). Машиночитаемый `code` и форма
  конверта в Q11 не входят — вынесены в Q23 (REST) и Q28/Q29 (MCP).
  - Ядро (`core.rs`: парсер, semver, `EvalError`) и публикация (`lib.rs`)
    уже соответствуют канону — приведены при Q8/Q9.
  - Транспорт приведён к канону:
    - 401 — «ошибка аутентификации: неверный или отсутствующий x-api-key»;
    - 404 — «версия не найдена: {name}@{version}» и
      «проверка '{name}' не найдена»;
    - MCP — «неизвестный инструмент: {name}».
  - Фичи согласованы: в `rest_auth.feature` английское тело ответа заменено
    русским; `evaluate.feature` уже ожидала «ошибка аутентификации» и
    «версия не найдена» — правок не потребовалось.
  - Исключения канона: служебные/диагностические сообщения (ошибки git в
    `lib.rs`, описания в OpenAPI-документе) остаются техническими.

### Q42. ✅ Каноническая схема объяснения и язык полей (решено 2026-09-24)

- **Расхождение:** `explain.feature` описывала поля по-русски («Правило»,
  «Приоритет», «Условие», «Значение»); `explain_full.feature` и код —
  латиницей (`rule_name`, `condition`, `actual_value`, `matched`,
  `decision`, `reason`, `priority`). При этом в `Explanation` из `core.rs`
  не было `condition`, а `matched`/`decision`/`reason` есть.
- **Вопрос:** один контракт объяснения: какие поля, на каком языке ключей,
  где живут `condition` и `priority`?
- **Рекомендация:** канон — латиница (`explain_full.feature`), добавить
  `condition`; `priority` — только если Q4 решится в пользу `Приоритет`;
  `explain.feature` переписать под тот же контракт.
- **Обновить:** `core.rs::Explanation`, `explain.feature`,
  `explain_full.feature`, SPEC (схема объяснения).
- **✅ Решение (2026-09-24):** Q4 решён не в пользу `Приоритет` (см. решение Q4), поэтому
  **канон — латиница**: ключи `snake_case` (`rule_name`, `condition`,
  `actual_value`, `matched`, `decision`, `reason`), **тексты полей
  (`decision`, `reason`) — русские** (предложение пользователя: канон —
  русский язык для содержательных значений; ключи остаются латиницей ради
  стабильного JSON-контракта; текст ошибки — канонический русский
  (Q11)).
  - `core.rs::Explanation` дополнен полем `condition` (каноническая
    строковая форма `<поле> <оператор> <значение>`,
    `condition_to_string`).
  - `priority` в схему **не входит** — вне v0.1 (Q4), вернётся в v0.2 с
    конвейерами.
  - Ошибки исполнения (Q8/Q9) — вне `Explanation` (возвращаются через
    `Result`): отдельного варианта «объяснение с ошибкой» в схеме нет.
  - `explain.feature` переписана под канон (ключи латиницей, сценарий с
    приоритетом удалён); в `explain_full.feature` убраны упоминания
    `priority`, добавлен шапочный комментарий о каноне.
  - `SPECIFICATION.md` §10: добавлено решение №21 (схема объяснения).
  - Тест: `explanation_has_latin_keys_and_condition_q42`.

---

## 3. Хранилище, публикация, git

### Q12. ✅ Единственный источник истины: `.dar`-файлы или песочница+JSON? (решено 2026-09-25)

- **Расхождение:** SPEC §1.3/§4.3: «файл на диске — источник истины»
  (`rules/*.dar`). Но жизненный цикл §7 начинается с MCP-черновика в
  `.credo/sandbox.json`, а публикация пишет `rule.json`/`contract.json`/
  `meta.json` в bare-репозиторий; `.dar`-файлы в этом цикле не участвуют
  вовсе.
- **Вопрос:** как связаны файлы workspace, черновики и опубликованные
  артефакты? Что происходит при расхождении?
- **Варианты:** (а) `.dar` — истина, черновик/публикация — производные;
  (б) песочница — истина для черновиков, `.dar` — только UI-представление;
  (в) два независимых контура в MVP.
- **Рекомендация:** определить один направленный поток
  (`файл → черновик → публикация`) и запретить обход.
- **Обновить:** `SPECIFICATION.md` §1.3, §4.3, §7; `notebook_ui.feature`,
  `editor.feature`, `file_management.feature`.
- **✅ Решение (2026-09-25):** источник истины — `.dar`-файл рабочего контура
  (`rules/`, под git). `.credo/sandbox.json` — только черновик: производный
  от файла или созданный агентом с нуля; временное состояние, которое можно
  удалить/пересоздать без потери данных; черновики не участвуют в REST
  `evaluate`. Поток строго направленный:
  `rules/*.dar → черновик → артефакт публикации`.
  - Обход потока запрещён: нельзя опубликовать то, чего нет в песочнице/файле;
    `evaluate` читает только опубликованное.
  - При расхождении (файл изменён вручную, черновик устарел) выигрывает файл;
    черновик помечается `stale` при первой операции над ним (сверка
    `source_hash` исходного `.dar`).
  - Черновик без привязанного `.dar`-файла при публикации обязан сначала
    сохранить текст в `rules/{name}.dar` — сохраняется инвариант «у каждого
    опубликованного артефакта есть исходник».
  - Точное хранение опубликованных правил для `evaluate` в MVP —
    `HashMap`/`Vec` в памяти; будущая векторная БД — только производный
    поисковый индекс, не источник истины для исполнения.

### Q13. ✅ Что именно публикуется и по какому пути? (решено 2026-09-25)

- **Расхождение:** `publish.feature`: «в ветке содержится файл
  „Правило МинимальныйВозраст 1.0.0“». `storage_paths.feature`:
  `checks/{name}/{version}/` с `rule.json`, `contract.json`, `meta.json`.
  `publish_rules.feature`: путь `checks/CreditAgeMin/2.0.0/`. SPEC §10 п. 14:
  «одно правило = один файл». Код: JSON-тройка. Всё разное.
- **Вопрос:** канонический артефакт публикации — JSON или `.dar`-текст?
  Если JSON — то где лежит исходный `.dar`?
- **Рекомендация:** оставить JSON-тройку (как в коде) и переписать
  `publish.feature`; «один файл» уточнить как «один check = один каталог
  версии».
- **Обновить:** `publish.feature`, `storage_paths.feature`,
  `SPECIFICATION.md` §10 п. 14.
- **✅ Решение (2026-09-25):** публикация — неизменяемый скомпилированный
  артефакт, а не исходный DSL-текст. Каталог `checks/{name}/{version}/`
  содержит три файла:
  - `rule.json` — исполняемое представление правила (AST или иной
    машинно-читаемый формат);
  - `contract.json` — контракт проверки: входные поля с типами и формат
    результата;
  - `meta.json` — метаданные издания: `name`, `version`, `display_name`,
    `published_at`, `published_by`, `checksum`, `source_hash`,
    `compiler_version`.
  - Исходный `.dar` остаётся источником истины в рабочем контуре; в v0.1 в
    реестр публикаций не попадает. Добавление `source.dar` в каталог версии —
    v0.2; связь артефакта с исходником обеспечивает `source_hash`.
  - `name` — машиночитаемый латинский идентификатор (например,
    `CreditAgeMin`); человекочитаемое имя — `display_name` в `meta.json`.
  - Каталог версии неизменяем: изменение правила требует новой версии.
    Формулировка «одно правило = один файл» устарела; канон — «один check =
    один каталог версии».

### Q14. ✅ Имя ветки публикации (решено 2026-09-25)

- **Расхождение:** `publish.feature` — `checks/{name}/{version}`;
  `publish_rules.feature` и код — `publish/{name}-{version}`.
- **Вопрос:** какое имя канонично?
- **Рекомендация:** `publish/{name}-{version}` (уже реализовано и описано
  в `publish_rules.feature`).
- **Обновить:** `publish.feature`, `features/README.md` (заметка 5).
- **✅ Решение (2026-09-25):** каноническое имя ветки —
  `publish/{name}-{version}` (например, `publish/CreditAgeMin-2.0.0`), как
  в коде и `publish_rules.feature`. `checks/{name}/{version}/` — путь
  артефакта внутри реестра, `publish/{name}-{version}` — имя ветки доставки;
  использовать `checks/...` как имя ветки запрещено, чтобы не смешивать
  пространства артефактов и доставки.

### Q15. ✅ Кто и как мержит ветку в `main` (решено 2026-09-25)

- **Расхождение:** SPEC §7 показывает «git commit+branch» и сразу
  `POST /evaluate`, без шага merge. MCP-ответ предлагает ручную команду
  `git update-ref refs/heads/main ...`. `deferred.feature` откладывает
  настоящий PR через GitHub API. UI (`git_integration.feature`) содержит
  кнопку «Опубликовать» и «Commit», но непонятно, к какому репозиторию они
  применяются.
- **Вопрос:** минимальный цикл публикации в MVP: кто переводит ветку в
  `main` — человек командой, Notebook или сервер?
- **Рекомендация:** для MVP — полуручной цикл с одной документированной
  командой и кнопкой «Смержить» в UI.
- **Обновить:** `SPECIFICATION.md` §7, `git_integration.feature`,
  `deferred.feature`.
- **✅ Решение (2026-09-25):** минимальный цикл публикации — двухшаговый:
  1. `check.publish` создаёт ветку `publish/{name}-{version}` от `main`;
  2. отдельный явный шаг слияния в `main` — команда
     `credo merge {name} {version}`.
  Правило становится видимым для `evaluate`, MCP и UI **только после слияния
  в `main`**; до этого ветка — заготовка, не доступная потребителям.
  - Канон MVP — «человек командой». Кнопка «Смержить» в UI и MCP-инструмент
    `check.merge` — в планах; команда `credo merge` проектируется так, чтобы
    позже её мог вызывать MCP-инструмент без изменения семантики.
  - Механика мержа в bare-репозитории: ветка обязана быть потомком `main`
    (`git merge-base --is-ancestor`); если нет — отказ **без авто-ребейза**
    с предложением перепубликовать. Иначе атомарное обновление `main` через
    `git update-ref refs/heads/main <sha-ветки> <ожидаемый-sha-main>`
    (CAS-проверка против параллельных мержей). После успеха ветка
    `publish/{name}-{version}` удаляется; артефакт неизменяемо живёт в `main`.
  - Принцип «четырёх глаз» (публикует один, мержит другой) в MVP не
    требуется, но закладывается архитектурно: шаг мержа отделён от шага
    публикации и допускает другого исполнителя.
  - Настоящий PR через GitHub API остаётся отложенным (`deferred.feature`).

### Q16. 🟡 Публикация без теста и контрольная сумма

- **Расхождение:** `publish.feature` требует отклонять публикацию без
  тестирования и при несовпадении контрольной суммы после последнего теста.
  В коде нет ни учёта тестов, ни такой проверки. SPEC это не описывает.
- **Вопрос:** оставляем требование? Если да — где хранится
  «checksum последнего теста»?
- **Рекомендация:** реализовать: хранить в черновике
  `last_test_checksum`/`tested_at` (одно поле в `sandbox.json`).
- **Обновить:** `draft.feature`, `publish.feature`, `lib.rs`, SPEC §4.2.

### Q17. 🟡 Иммутабельность: файловая система или bare-git?

- **Расхождение:** `immutability.feature` описывает защиту через
  `create_dir_all`/`AlreadyExists`; в `credo2` хранилище — bare git, защита
  — проверка дубликата + атомарный `create_ref`.
- **Вопрос:** какой механизм считаем нормативным (и нужно ли переносить
  хранилище на рабочее дерево)?
- **Рекомендация:** оставить bare-git, переписать сценарии фичи.
- **Обновить:** `immutability.feature`, `features/README.md` (заметка 6).

### Q18. 🟡 Сортировка pre-release и build-метаданные

- **Расхождение:** `deferred.feature` фиксирует лексикографическое
  сравнение (`rc2` vs `rc10`) как текущее; `core.rs` уже сравнивает
  числово; `semver.feature` ожидает `1.2.3+build` как валидную версию, а
  `storage_paths.feature` и код убирают build из пути.
- **Вопрос:** числовое сравнение — норматив? Что с двумя публикациями
  `1.0.0+a` и `1.0.0+b` (дубликат)?
- **Рекомендация:** числовое сравнение как в semver; build отбрасывать и
  явно запрещать повторную публикацию базы.
- **Обновить:** `deferred.feature`, `semver.feature`, `SPECIFICATION.md`.

### Q19. ⚪ Где живут данные: `.credo/` и `.dar-notebook/` в одном месте

- **Расхождение:** SPEC §4.2 упоминает `.credo/sandbox.json` и
  `.credo/published-repo/`, §4.3 — `.dar-notebook/` (`layout.json`,
  `results-cache.json`, `agent-history.json`). В фичах фигурирует только
  `.dar-notebook/`.
- **Вопрос:** описать оба каталога и их владельцев?
- **Рекомендация:** да, добавить в SPEC §4.3 таблицу «каталог → что
  хранит → попадает ли в git».
- **Обновить:** `SPECIFICATION.md` §4.3.

---

## 4. REST API

### Q20. 🔴 Канонические пути REST

- **Расхождение:** `rest_api.feature` — `/checks/:name/versions/:version/...`;
  `evaluate.feature` — `/checks/:name/:version/evaluate` и
  `GET /checks/:name/9.9.9`; `import_export.feature` —
  `/checks/:name/1.0.0/export`. Реализация — вариант с `/versions/`.
- **Вопрос:** какой путь нормативен?
- **Рекомендация:** `/checks/{name}/versions/{version}/...` (как в коде);
  переписать `evaluate.feature` и `import_export.feature`.
- **Обновить:** `evaluate.feature`, `import_export.feature`, `rest_api.feature`.

### Q21. 🔴 Схема ответа `GET /checks`

- **Расхождение:** `rest_api.feature` — `count`/`service_hash`/`checks`
  (`ManifestEntry`: `name`/`active`/`supported`/`deprecated`);
  `dashboard.feature` — «три записи со статусами активна/deprecated»;
  `evaluate.feature` — `name`/`kind`/`versions`/`latest`.
- **Вопрос:** одна схема ответа — какая? Входит ли `kind` в манифест?
- **Рекомендация:** канон — `ManifestEntry` из кода; `kind` добавить,
  только если он нужен публикации (сейчас есть только в фичах).
- **Обновить:** `dashboard.feature`, `evaluate.feature`,
  `SPECIFICATION.md` (схема API).

### Q22. 🔴 Аутентификация: `x-api-key` или `Authorization`

- **Расхождение:** `rest_auth.feature` и код — `x-api-key`;
  `evaluate.feature` — `Authorization` и текст «ошибка аутентификации».
- **Рекомендация:** `x-api-key`; путь `/openapi.json` тоже открыт (как в
  коде) и зафиксировать это в `rest_auth.feature`.
- **Обновить:** `evaluate.feature`, `rest_auth.feature`.

### Q23. 🟡 Формат ошибок REST

- **Расхождение:** тексты ошибок унифицированы русским каноном (Q11), но
  конверт не определён: код возвращает `{"error": "<сообщение>"}` без
  машиночитаемого `code`, фичи структуру не описывают. Добавление `code` и
  конверта — остаток, переданный из Q11.
- **Вопрос:** единый конверт ошибки и набор стабильных кодов?
- **Рекомендация:** конверт `{"error": {"code", "message"}}`, тексты
  по-русски, коды латиницей (`not_found`, `unauthorized`).
- **Обновить:** `rest.rs`, `rest_api.feature`, `evaluate.feature`.

### Q24. 🟡 Массовый прогон: отдельный эндпоинт?

- **Расхождение:** `batch.feature` шлёт `{"заявки": [...]}` на тот же
  `POST .../evaluate` и ждёт «статистика/всего/отказано/одобрено». Схема
  запроса/ответа нигде не описана, ключи русские — вразрез с остальным
  API.
- **Вопрос:** отдельный `POST /checks/{name}/versions/{v}/batch` или
  перегрузка `evaluate`? Ключи латиницей?
- **Рекомендация:** отдельный эндпоинт, ключи латиницей (`applications`,
  `stats`), русские — только в текстовых значениях.
- **Обновить:** `batch.feature`, SPEC (схема API).

### Q25. 🟡 Объяснение для клиента: транспорт и схема

- **Расхождение:** `client_explanation.feature` не говорит, куда
  обращаться (REST? MCP?), какие поля, откуда берётся «основание — политика
  кредитования» и порядок причин «по приоритету».
- **Вопрос:** определить эндпоинт/инструмент и схему.
- **Рекомендация:** MCP-инструмент `check.explain_client` для v0.1,
  шаблон текста — из контракта.
- **Обновить:** `client_explanation.feature`, SPEC §6.1.

### Q26. 🟡 Импорт/экспорт: контракт

- **Расхождение:** `import_export.feature` использует инструменты
  `check.export_draft` и `check.import`, которых нет в коде;
  `Content-Type: text/dar`; флаг «импортированный» нигде не хранится.
- **Вопрос:** реализуем в MVP или переносим в `deferred.feature`?
- **Рекомендация:** сначала решить Q12 (истина в файлах), затем решать
  формат обмена.
- **Обновить:** `import_export.feature`, `mcp.rs`.

### Q27. ⚪ Порт REST

- **Расхождение:** SPEC §4.2 — порт 8080; код — `127.0.0.1:8080` по
  умолчанию и выключен, если не передан `--rest`/`--addr`. Фичи не
  описывают запуск.
- **Рекомендация:** зафиксировать в SPEC и README запуска.
- **Обновить:** `SPECIFICATION.md` §4.2, README запуска.

---

## 5. MCP: контракт для агента

### Q28. 🔴 Параметры `check.create`

- **Расхождение:** `draft.feature` — `name`, `text`, `expected_kind`;
  ответы `list_drafts` — размер в байтах и формат `"text"`; код —
  `source`, хранит распарсенное правило, отдаёт `sandbox_file`. Фичи
  `agent_minimal.feature` полагаются на результат («Черновик создан»), но
  параметров не показывают.
- **Вопрос:** канонический контракт `check.create`?
- **Рекомендация:** один вызов — `source: string` (как в коде),
  `expected_kind`/`name` убрать из MVP; переписать `draft.feature`.
- **Обновить:** `draft.feature`, `mcp.rs`, README инструментов.

### Q29. 🟡 Ответы `check.test`, `check.list_published`, `check.deprecate`

- **Расхождение:** фичи ждут поля `matched`, `decision`, `reason`,
  `actual_value`, `deprecated_at` (это есть), но также «размер» и
  «формат» черновика; `check.deprecate` в коде требует `reason`
  обязательным.
- **Вопрос:** зафиксировать JSON-схемы всех MCP-инструментов в одном
  месте.
- **Рекомендация:** добавить в SPEC раздел «MCP tools» с таблицей
  параметров/ответов.
- **Обновить:** `SPECIFICATION.md`, `mcp_tools.feature`, `draft.feature`.

### Q30. 🔴 Транспорт MCP в Notebook

- **Расхождение:** SPEC §2.3/§4.1: «MCP-клиент (подключение к
  credo-server)», транспорт MCP — «stdio / HTTP+SSE». `agent_minimal.feature`
  — «панель подключена к MCP-серверу через stdio». Не сказано, кто
  запускает сервер: Notebook сам спавнит процесс или подключается к
  развёрнутому.
- **Вопрос:** в MVP Notebook сам запускает локальный `credo2` (sandbox
  локальный) или ходит на общий сервер?
- **Рекомендация:** MVP — локальный stdio-процесс на workspace; общий
  сервер — следующий шаг.
- **Обновить:** `SPECIFICATION.md` §2.3/§4.1, `agent_minimal.feature`.

---

## 6. Notebook, LSP, UI

### Q31. 🔴 Где живёт чат агента

- **Расхождение:** SPEC §10 п. 12: «Чат-интерфейс: отдельно от основного
  UI | Обсуждается отдельно». `notebook_ui.feature`: правая панель
  «содержит чат с агентом», `agent_minimal.feature` описывает панель в
  приложении.
- **Рекомендация:** в MVP — панель в основном окне; удалить п. 12 или
  переформулировать.
- **Обновить:** `SPECIFICATION.md` §10, `notebook_ui.feature`.

### Q32. 🔴 Два git-репозитория: workspace и published-repo

- **Расхождение:** SPEC §4.1 — у Notebook локальный `git CLI` и
  «локальный репозиторий»; §4.2 — у сервера свой bare-репозиторий
  публикаций. `git_integration.feature` показывает статус ветки, diff,
  commit и «Опубликовать» — непонятно, к какому репозиторию.
- **Вопрос:** какие операции UI относятся к workspace-репозиторию, а какие
  — к публикациям?
- **Рекомендация:** UI работает с workspace-репо; «Опубликовать» —
  отдельная операция MCP, результат виден в панели публикаций.
- **Обновить:** `git_integration.feature`, `SPECIFICATION.md` §4.1/§4.2.

### Q33. 🔴 Чем исполняется правило в редакторе

- **Расхождение:** `inline_execution.feature` — кнопка «Выполнить» прямо
  под правилом; `editor.feature` — диагностика из LSP; `agent_minimal` —
  `check.test` для черновика. LSP не исполняет правила, а источник данных
  для inline-запуска не определён (локальное ядро Tauri? MCP?).
- **Вопрос:** какой движок исполняет код из редактора и как туда попадает
  правило (сохранённый файл, черновик, несохранённый буфер)?
- **Рекомендация:** исполнять **буфер** через локальный `dar-core` (Tauri),
  без обязательного создания черновика; кнопку из `agent_minimal` не
  смешивать.
- **Обновить:** `inline_execution.feature`, `SPECIFICATION.md` §2.2/§4.1.

### Q34. 🔴 Тесты: внутри `.dar` или в `.dar-notebook/`

- **Расхождение:** SPEC §1.2: `.dar` — одновременно тест; §4.3: каталог
  `tests/` с `*.тест`; `inline_execution.feature`: последние входные данные
  и несколько наборов тестов сохраняются в `.dar-notebook/`
  (`results-cache.json`), то есть вне git.
- **Вопрос:** где канонические тесты и можно ли их версионировать?
- **Рекомендация:** для MVP — тесты в `.dar-notebook/` (локально), формат
  `tests/*.тест` не вводить; принцип «тест в файле» перенести в целевую
  версию.
- **Обновить:** `SPECIFICATION.md` §1.2/§4.3, `inline_execution.feature`.

### Q35. ⚪ Создание workspace и шаблоны

- **Расхождение:** `notebook_ui.feature` создаёт `rules/`, `tables/`,
  `tests/` и `README.md`; содержание README и шаблонов нигде не задано.
- **Рекомендация:** зафиксировать шаблон (файл-пример правила, `.gitignore`
  для `.dar-notebook/`).
- **Обновить:** `notebook_ui.feature`, `SPECIFICATION.md` §4.3.

### Q36. ✅ Конвейеры: несколько правил в одном файле? (решено 2026-09-24)

- **Расхождение:** `lsp.feature` (documentSymbol) ожидает «Конвейер
  ПотребКредит» с 3 правилами и 1 скорингом; SPEC §10 п. 14 — «одно
  правило = один файл»; `graph_view.feature` (граф конвейера) — ⏳
  отложен.
- **Вопрос:** поддерживаем ли `Конвейер`/скоринг/таблицы в MVP?
- **Рекомендация:** нет; убрать `Конвейер` из LSP-сценария (заменить на 3
  отдельных файла), граф оставить отложенным.
- **Обновить:** `lsp.feature`, `graph_view.feature`, SPEC §10.
- **✅ Решение (2026-09-24):** конвейеры, скоринги и таблицы — вне MVP
  (язык v0.1, `GRAMMAR.md` §1/§4). Сценарий documentSymbol в `lsp.feature`
  трактуется как «несколько открытых файлов» (workspace symbols), граф —
  остаётся отложенным (`graph_view.feature` ⏳). Возврат — v0.2 вместе с
  `Приоритет` (Q4).

### Q37. 🔴 Реестр полей и типов для автодополнения

- **Расхождение:** `lsp.feature` требует подсказки `Клиент.Возраст`,
  `Клиент.Доход` с типом поля; откуда берутся схемы — ни SPEC, ни фичи не
  говорят. Каталог `tables/` упомянут, но формат не определён.
- **Вопрос:** источник схемы полей для completion/hover: файл `tables/`,
  контракт опубликованных проверок, конфиг workspace?
- **Рекомендация:** для MVP — не подсказывать поля (только ключевые
  слова), иначе вводить `tables/` с форматом.
- **Обновить:** `lsp.feature`, `SPECIFICATION.md` §4.3.

### Q38. ✅ Состав LSP сверх SPEC (решено 2026-09-24)

- **Расхождение:** `lsp.feature` требует formatting, semanticTokens,
  definition, references, documentSymbol, hover; `SPECIFICATION.md` §3.3/§5
  перечисляет только диагностику, автодополнение и символы, а модули
  `dar-core` — `completion.rs`/`explain.rs` без `format`/`tokens`.
- **Вопрос:** какое подмножество LSP обязательно для MVP и где оно
  реализуется?
- **Рекомендация:** MVP — diagnostics + completion + semantic tokens;
  остальное — целевом состоянии.
- **✅ Решение (2026-09-24):** LSP входит в MVP (пользователь подтвердил).
  Обязательный состав: initialize/handshake, синхронизация документов
  (didOpen/didChange), diagnostics, completion (ключевые слова + значения
  решений), hover по правилу, documentSymbol/workspaceSymbols (по одному
  правилу в файле — см. Q36), semanticTokens. Реализация — крейт `lsp-dar`
  поверх API `parse_rule` (Фаза 2). Вне MVP → v0.2: formatting, definition,
  references, completion полям из реестра (Q37). Сценарии `lsp.feature` с
  `Приоритет`, полями и конвейером помечаются как целевое состояние.
- **Обновить:** `SPECIFICATION.md` §3.3/§5, `lsp.feature`, §8 (Фаза 2).

### Q39. 🟡 Поведение при падении LSP

- **Расхождение:** SPEC §10 п. 6 — «Нет fallback при падении LSP;
  перезапуск sidecar»; `lsp_notebook.feature` — автоперезапуск + уведомление
  + «состояние открытых файлов восстанавливается».
- **Вопрос:** что значит «нет fallback» и что восстанавливается
  (диагностики? позиция курсора?).
- **Рекомендация:** переформулировать п. 6: «перезапуск и повторная
  синхронизация открытых документов; деградация — без подсветки».
- **Обновить:** `SPECIFICATION.md` §10, `lsp_notebook.feature`.

---

## 7. Процесс

### Q40. ✅ Исполняемые ли Gherkin-сценарии (решено 2026-09-24)

- **Расхождение:** критерий готовности SPEC §9: «Все сценарии из
  соответствующих `.feature` файлов проходят». При этом нет маппинга
  сценариев на тесты и нет раннера (Cucumber и т. п.);
  `testing.feature` перечисляет только Rust-тесты. Проверить DoD сейчас
  невозможно.
- **Вопрос:** фичи — исполняемая спецификация (cucumber-rs) или
  документация, а DoD = `cargo test`?
- **Рекомендация:** в MVP — документация, DoD привязать к конкретным
  Rust-тестам; ввести cucumber — при переходе к целевому состоянию.
- **Обновить:** `SPECIFICATION.md` §9, `testing.feature`, `ci.yml`.
- **✅ Решение (2026-09-24):** в MVP `.feature` — **документация**
  (требования и чек-листы), не исполняемая спецификация. DoD SPEC §9:
  `cargo fmt --check` + `cargo clippy -- -D warnings` + `cargo test --all`
  плюс ручной прогон UI-сценариев. Проверяемость инвентаризации даёт новый
  тест `tests/features_inventory.rs`: структура фич, полнота перечня,
  счётчики и итоги README. Cucumber-rs — v0.2, после стабилизации языка и
  API, только для backend-сценариев; UI-сценарии остаются ручными.

### Q41. 🟡 Владелец и синхронизация документов

- **Расхождение:** статусы и цифры дублируются в `SPECIFICATION.md` и
  `features/README.md`; `DECISIONS.md` пуст (15 байт);
  `AGENTS.md` описывает другой прототип (`prototypes/credo`), а не
  `credo2`.
- **Вопрос:** кто владелец каждого документа и где фиксируются решения?
- **Рекомендация:** решения — в `DECISIONS.md` (ADR-строки), статусы — только
  в `features/README.md`, SPEC не дублирует статусы; `AGENTS.md` обновить
  под `credo2` (или пометить как отдельный прототип).
- **Обновить:** `DECISIONS.md`, `AGENTS.md`, `SPECIFICATION.md`.

---

## Матрица: документ → что править после решений

| Документ | Вопросы для правки |
|---|---|
| `SPECIFICATION.md` §1.3, §4, §5, §6, §7, §8, §10, §11 | Q1–Q4, Q6–Q7, Q12–Q15, Q19–Q23, Q27, Q29–Q39, Q41 |
| `features/README.md` | Q1, Q5, Q6, Q14, Q17 |
| `features/lexer.feature`, `parser.feature`, `execution.feature`, `errors.feature`, `explain.feature`, `explain_full.feature` | Q2–Q4, Q8–Q11, Q42 |
| `draft.feature`, `test_draft.feature`, `publish.feature`, `publish_rules.feature`, `immutability.feature`, `storage_paths.feature`, `semver.feature`, `deferred.feature` | Q12–Q18, Q28–Q29 |
| `rest_api.feature`, `rest_auth.feature`, `evaluate.feature`, `batch.feature`, `dashboard.feature`, `import_export.feature` | Q20–Q27, Q42 |
| `notebook_ui.feature`, `editor.feature`, `inline_execution.feature`, `git_integration.feature`, `agent_minimal.feature`, `file_management.feature`, `graph_view.feature` | Q12, Q15, Q31–Q37 |
| `lsp.feature`, `lsp_notebook.feature` | Q10, Q36–Q39 |
| `GRAMMAR.md`, `EBNF.md`, `DECISIONS.md`, `AGENTS.md`, `concept.md` | Q2, Q3, Q40, Q41 |

## Минимальный набор решений до старта (если нет времени на всё)

1. ✅ Q1 — решено: эволюция `credo2` (не greenfield).
2. ✅ Q2/Q3 — решены: канон `GRAMMAR.md`, парсер MVP — regex-минимум,
   стратегия LSP-совместимости зафиксирована (GRAMMAR.md §3). Q4 также
   решён: `Приоритет` вне MVP (вернётся с конвейерами в v0.2).
3. ✅ Q8/Q9/Q42/Q10 — решены 2026-09-24: строгая ошибка при отсутствующем
   поле, проверка типов на исполнении, канон объяснения (латиница +
   русские тексты), словарь решений задаёт банк (`Pass` убран).
4. ✅ Q12/Q13 — решено: источник истины — `.dar`-файл, публикация —
   артефакт `checks/{name}/{version}/` (Q12, Q13). Q14/Q15 также решены:
   ветка `publish/{name}-{version}`, мерж — отдельный шаг `credo merge`.
5. Q20/Q22 — канонические REST-путь и аутентификация.
6. Q28/Q30 — канонический MCP-контракт и транспорт.
7. Q33/Q34 — где исполняется код и где живут тесты.
8. ✅ Q40 — решено: фичи — документация (cucumber-rs вне MVP); DoD =
   `cargo fmt --check` + `cargo clippy` + `cargo test` + ручной UI-прогон.

---

## Приложение A. Перенос из `features/README.md`

Раздел «Известные расхождения и заметки» из `features/README.md` удалён,
чтобы README не дублировал перечень проблем. Соответствие бывших заметок:

| Былая заметка | Где теперь |
|---|---|
| 1. Коллизия имени `explain.feature` | Закрыто — A1 |
| 2. Аутентификация | Q22 |
| 3. Поля манифеста | Q21 |
| 4. Отсутствующее поле | Закрыто — Q8 (решено 2026-09-24) |
| 5. Формат ветки публикации | Закрыто — Q14 (решено 2026-09-25) |
| 6. Иммутабельность | Q17 |
| 7. Числовая сортировка prerelease | Q18 |
| 8. Разделение LSP | Закрыто — A2; учёт в SPEC — Q6 |
| 9. Счётчик сценариев `editor.feature` | Q6 |
| 10. Синтаксис `Приоритет` | Q4 (см. также Q2, Q3) |

### A1. Коллизия имени `explain.feature` — закрыто

Изначально было два разных раздела объяснимости с одинаковым именем файла.
Первый (языковой, «Движок объясняет каждое решение») сохранён как
`features/explain.feature`, второй («Полное объяснение каждого решения») —
как `features/explain_full.feature`. Схема объяснения канонизирована
(ключи латиницей, поле `condition`, тексты русские) — Q42, решено 2026-09-24.

### A2. Разделение LSP — закрыто

LSP описано базовым файлом `features/lsp.feature` (15 сценариев) и отдельным
блоком «уточнения» про интеграцию с Notebook. Чтобы сохранить конвенцию
«один файл — одна `Функция`», уточнения вынесены в
`features/lsp_notebook.feature` (6 сценариев). `SPECIFICATION.md` §5/§6.1
про второй файл не знает — учтено в Q6.
