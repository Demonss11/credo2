# T-11. Цикл агентов: Agile-петля, единый тестировщик, память и почта

- **Ветка эксперимента:** `exp/agent-cycle-rerun` (база `e1b2550`).
- **Карточка:** [`docs/tasks/T-11-agent-cycle/README.md`](../../docs/tasks/T-11-agent-cycle/README.md)
- **Решение журнала:** [`docs/decisions/D38-agent-cycle.md`](../../docs/decisions/D38-agent-cycle.md) (Q43)
- **Формат отчётов:** `AGENTS.md` §Рабочая группа агентов (один писатель на файл, append).

## lead · 2026-09-26 · лента открыта

- Сделано: открыта лента задачи T-11; размер и маршрут фиксирует `lead`.
- Дальше / риски: первая запись — перенос решения владельца в журнал (`migrator`).

## migrator · 2026-09-26 · готово

- Сделано: новая запись журнала — [Q43](../../docs/questions/Q43.md) →
  [D38](../../docs/decisions/D38-agent-cycle.md) (не перенос из архива);
  строка §10 №38, строка в [TRACEABILITY](../../docs/TRACEABILITY.md), «Источник»
  карточки T-11 и сводки `tasks/README.md` → D38 (Q43).
- Проверки: ID свободны (последний Q — Q42, последнее решение §10 — №37);
  относительные ссылки сверены вручную (BRIEF §9); `cargo` не запускался —
  решение процессное, вердикт «Сверка с кодом: ⚪ не применимо».
- Дальше / риски: в `features/agents-*.feature` нет блока обратных ссылок
  `Decisions: D38` (BRIEF §5.6, шаг 2) — правка зоны `docs-writer`, нужен вызов
  через `lead`. Судейская часть — `validator`.

## docs-writer · 2026-09-26 · готово

- Сделано: обратные ссылки `# D38 (Q43): цикл агентов — Agile-петля, единый
  тестировщик, память и почта` в шапках `docs/features/agents-*.feature`
  (6 файлов); уточнения процесса — `docs/BRIEF.md` §5.3/§5.7,
  `docs/tasks/README.md` (DoD и T-11 → 🚧), `docs/README.md` («Куда за чем»);
  запись «Цикл агентов v2 (T-11)» в `docs/CHANGELOG.md`; карточка
  `docs/tasks/T-11-agent-cycle/README.md` — статус 🚧, раздел «Доработка v2
  (2026-09-26)», обновлённые «Примечания».
- Проверки: ссылки — файловыми инструментами (`D38`, `Q43`,
  `docs/features/README.md`, карточка, `.opencode/mail/T-11.md`); счётчики
  фич — 6 файлов / 27 сценариев (сверено с `docs/features/README.md`); `cargo`
  не запускался.
- Дальше / риски: полный `cargo test --all` и `features_inventory` — за
  `validator`; пилот v2 (T-03) — отдельной сессией; замечание `migrator`
  об обратных ссылках закрыто.

## lead · 2026-09-26 · пакет подтверждён пользователем

- Подтверждение (одно, `question`, 2026-09-26): два коммита + push ветки.
- Пакет:
  1. `docs(Q43): цикл агентов — решение владельца (v2)` — Q43/D38, SPEC §10 №38,
     TRACEABILITY, карточка T-11, сводка задач;
  2. `chore(T-11): цикл агентов v2 — роли, права, память и почта, фичи` —
     `AGENTS.md`, 10 ролей, rules, память/почта, фичи `agents-*.feature` (6/27),
     `BRIEF`/`CHANGELOG`/`README`, отчёты r1/r2;
  3. `git push -u origin exp/agent-cycle-rerun`.
- Дальше / риски: коммиты исполняет `git` идемпотентно; после — пилот T-03
  отдельной сессией.

## validator · 2026-09-26 · принято

- Сделано: приёмка доработки v2 T-11 (журнал Q43→D38, канон/роли, права R2,
  память/почта, 6 фич `agents-*`, документация). Отчёт —
  [`docs/reviews/T-11-2026-09-26.md`](../../docs/reviews/T-11-2026-09-26.md).
- Проверки: `cargo fmt --check` — чисто; `cargo clippy --all-targets -- -D warnings`
  — ok; `cargo test --all` — **78 passed / 0 failed** (incl. `features_inventory`
  4/4: счётчики 42 файла / 250 сценариев сходятся).
- Вердикт: **принято**; P1/P2 нет. P3 (не блокеры): `validator.md:53` «см. „Код“»
  (в `AGENTS.md` нет раздела «Код»); `agents-audit.feature:35` «Primary-агент»
  (у `auditor` `mode: all`).
- Дальше / риски: аудит `auditor` (отдельной сессией), коммит v2 (`git`),
  пилот T-03 — отдельными сессиями.

## docs-writer · 2026-09-26 · готово (замечание аудита P3)

- Сделано: `docs/BRIEF.md` §5.6 п.2 приведён к фактической форме — в шапке
  требования комментарий со ссылкой на решение `# Dn (Qx): краткая
  формулировка`; заявлено, что отдельного блока `Decisions:` в фичах нет.
- Проверки: форма сверена с `agents-*.feature` (5 файлов, `# D38 (Q43): …`);
  `rg "Decisions:"` по `docs/**` — нормативных вхождений не осталось
  (историческое упоминание есть в `docs/decisions/D38-agent-cycle.md:98`,
  зона `migrator`); ссылок не добавлял; `cargo` не запускался.
- Дальше / риски: CHANGELOG не обновлялся (правка формулировки, смысл §5.6
  и поведение не меняются); при желании `migrator` поправит формулировку
  в D38-agent-cycle.md:98.

## validator · 2026-09-26 · принято (r2)

- Сделано: повторная приёмка после правок (r1 «принято», 2×P3 + замечание по
  BRIEF §5.6). Отчёт —
  [`docs/reviews/T-11-2026-09-26-r2.md`](../../docs/reviews/T-11-2026-09-26-r2.md).
- Проверки: `git status --porcelain -- src tests Cargo.toml Cargo.lock` — пусто
  (код не менялся); правки `git.md`/`git-workflow.md`/`validator.md`/
  `agents-audit.feature`/`BRIEF.md §5.6` — на месте, ничего лишнего; `question`
  у `git` нет (осталось `ask`); подтверждение git — одно, у `lead`;
  `validator.md` отсылает на главу «Код» (существует); фичи 6/27, «Итого
  42/250» не поехали; DoD полный не запускался — обоснованно (src/tests и
  счётчики сценариев не менялись, на r1 — 78/78).
- Вердикт: **принято**; P1/P2/P3 нет.
- Дальше / риски: коммит v2 (`git`), аудит `auditor`, пилот T-03 — отдельными
  сессиями.

## git · 2026-09-26 · ошибка (нужно подтверждение)

- Сделано: инспекция пакета v2 (только read-only). `git status -sb` — ветка
  `exp/agent-cycle-rerun`, upstream нет; `git log -3 --oneline` — HEAD `e1b2550`
  (база из шапки ленты), коммитов пакета ещё нет; `git remote -v` — `origin`
  `https://github.com/Demonss11/credo2.git`. Сверка пакета со `git status`:
  сходится 1:1 — 44 пути (21 изменённый + 23 новых), расхождений и лишнего нет.
- Проверки: запись `lead` о подтверждении пользователя искалась файловыми
  инструментами (`.opencode/mail/T-11.md`, `.opencode/memory/lead.md`,
  `docs/**`, `rg "подтвержд"`) — не найдена. Изменяющих команд (`add`/`commit`/
  `push`) не выполнял: `git-workflow.md` §Пакет и подтверждение п.3 —
  «„Подтверждено“ без записи в ленте не бывает».
- Дальше / риски: нужна запись `lead` в ленте: пакет подтверждён пользователем
  (одно подтверждение, до вызова `git`) — коммит 1 «журнал», коммит 2
  «перестройка v2», `git push -u origin exp/agent-cycle-rerun`. После записи
  пакет выполняется этим же вызовом `git` идемпотентно; пользователя `git`
  не спрашивает.
