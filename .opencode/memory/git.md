# Память: git (git-операции)

- **Канон:** `.opencode/rules/git-workflow.md`.
- **Правило:** чекпойнт — пакет, выполненные шаги и хеши, что осталось
  (идемпотентность при обрыве). Кратко.

## Чекпойнты

- 2026-09-26 · T-11 v2 (пакет: коммит 1 «журнал», коммит 2 «перестройка v2»,
  `push -u origin exp/agent-cycle-rerun`) — **не выполнен**: в ленте
  `.opencode/mail/T-11.md` нет записи `lead` о подтверждении пользователя
  (`git-workflow.md` §Пакет и подтверждение п.3; `git.md` п.2).
  Инспекция: ветка `exp/agent-cycle-rerun`, HEAD `e1b2550`, upstream нет,
  `origin` = Demonss11/credo2; пакет сверен со `git status` — 44 пути
  (21 M + 23 новых), сходится 1:1. Следующее действие: после появления записи
  `lead` о подтверждении выполнить пакет идемпотентно (сверяя `git log -1`),
  затем отчёт в ленту и чекпойнт с хешами.
- 2026-09-26 · T-11 v2 — **выполнено** (пакет подтверждён: `lead` в
  `.opencode/mail/T-11.md:43`). Идемпотентные шаги: `git log -1` (`e1b2550`) →
  `git add` 6 путей → коммит 1 **`0604db6`** `docs(Q43)` (6 файлов, +220/−12);
  `git status --short` → `git add` 38 путей → коммит 2 **`0a5832f`**
  `chore(T-11)` (38 файлов, +1073/−267); `git push -u origin
  exp/agent-cycle-rerun` — ok, upstream `origin/exp/agent-cycle-rerun`.
  Проверки: `git status` clean до отчёта, `git log -3 --oneline`,
  `git status -sb` — в синхроне. Следующее действие: merge ветки в `master` —
  владелец; при обрыве/повторе — сверять `git log -1` и `git status -sb`
  (не переписывать историю).
- 2026-09-26 · служебные записи (пакет подтверждён: `.opencode/mail/T-11.md:137`) —
  **выполнено**: `git add` mail/T-11.md + memory/git.md → коммит **`2901001`**
  `chore(T-11): служебные записи ленты и памяти после v2` (2 файла, +39) →
  `git push` ok (`0a5832f..2901001`), upstream `origin/exp/agent-cycle-rerun`,
  `git status -sb` — в синхроне. Хеши ветки: `e1b2550` → `0604db6` → `0a5832f`
  → `2901001`. Следующее действие: merge в `master` — владелец; новые записи
  ленты/памяти снова копятся как рабочие данные (коммит — по новому
  подтверждению `lead`).
- 2026-09-26 · патч v2.1 (пакет подтверждён: `lead` в `.opencode/mail/T-11.md:162`) —
  **выполнено**: `git log -2` (`2901001`) → `git add` 22 пути (пакет совпал со
  `git status` 1:1) → коммит **`5019c45`** `chore(T-11): патч v2.1 — схема A,
  порог H5, маршруты и чекпойнты` (22 files, +318/−98) → `git push`
  (timeout 600 000 мс) ok `2901001..5019c45 exp/agent-cycle-rerun`. Проверки:
  `git status` clean, `git log -2 --oneline`, `git status -sb` — в синхроне.
  Следующее действие: merge `exp/agent-cycle-rerun` в `master` — владелец;
  остаток в дереве — ` M .opencode/mail/T-11.md`, ` M .opencode/memory/git.md`
  (рабочие данные, нужен отдельный пакет).
- 2026-09-26 · служебные записи v2.1 (пакет подтверждён: `lead` в
  `.opencode/mail/T-11.md:235`) — **выполнено**: отчёт в ленту и этот чекпойнт
  дописаны до коммита; `git add` mail/T-11.md + memory/git.md → коммит
  `chore(T-11): служебные записи ленты и памяти после v2.1` → `git push`
  (timeout 600 000 мс); `git status` — clean, `git status -sb` — в синхроне.
  Следующее действие: merge `exp/agent-cycle-rerun` в `master` — владелец;
  пилот T-03 — отдельной сессией.
