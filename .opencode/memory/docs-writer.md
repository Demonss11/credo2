# Память: docs-writer (документация и статусы)

- **Канон:** `docs/tasks/README.md`, `docs/features/README.md`,
  `docs/BRIEF.md` §9.
- **Правило:** чекпойнт — какие файлы/статусы менялись, что осталось, ссылки.
  Кратко.

## Чекпойнты

- **2026-09-26 · T-11 v2 (ветка `exp/agent-cycle-rerun`)** — готово.
  - Изменено: обратная ссылка `# D38 (Q43): цикл агентов — …` в шапках
    `docs/features/agents-{cycle,rework,memory-mail,git-approval,sized-routes,audit}.feature`;
    `docs/BRIEF.md` §5.3 (адресный прогон — `validator` через `lead`), §5.7
    (полный `cargo test` — только `validator`); `docs/tasks/README.md`
    (DoD-уточнение, T-11 → 🚧); `docs/README.md` («Куда за чем»);
    `docs/CHANGELOG.md` (запись «Цикл агентов v2»); карточка T-11 (🚧, раздел
    «Доработка v2», «Примечания»).
  - Проверки: ссылки — файловыми инструментами (D38, Q43, `features/README.md`,
    карточка, `.opencode/mail/T-11.md`); счётчики фич — 6 файлов / 27 сценариев
    (совпадает и с инвентарём); `cargo` не запускался.
  - Осталось: прогон `cargo test --all` и инвентарь — за `validator`; пилот v2
    (T-03) — отдельной сессией. Блок «Процесс агентов» в `features/README.md`
    не правился (строки уже были, счётчики сверены).
- **2026-09-26 · T-11, правка по замечанию аудита (P3)** — готово.
  - Изменено: `docs/BRIEF.md` §5.6 п.2 — «блок обратных ссылок `Decisions: Dn`»
    заменён на фактическую форму: комментарий в шапке требования
    `# Dn (Qx): краткая формулировка`, отдельного блока `Decisions:` нет.
  - Проверки: форма сверена с `agents-*.feature` (5 файлов, `# D38 (Q43): …`);
    `rg "Decisions:"` — нормативных вхождений в `docs/**` нет (осталось
    историческое упоминание в `docs/decisions/D38-agent-cycle.md:98` — зона
    `migrator`); ссылок не добавлял; `cargo` не запускался.
  - Осталось: CHANGELOG не обновлялся (правка формулировки, смысл §5.6 и
    поведение не меняются); D38-agent-cycle.md:98 при необходимости — `migrator`.
