# Приёмка (адресная, r5): правки правила очистки по P2/P3 аудита

**Проверка:** адресная повторная проверка `.opencode/rules/git-workflow.md` после
аудита r4 (P2/P3). Исправлено ровно по рекомендациям:
- §«Подтверждение и очистка логов»: в пункте о сверке из истории добавлен момент
  проверки («после коммита подтверждения и до коммита очистки»); пункт о
  служебном коммите в `develop` дедуплицирован — вместо повтора протокола ссылка
  на пункт о сверке; исключение сформулировано как «исключение из „Прямые
  коммиты в `master` и `develop` запрещены“ (см. „Модель ветвления“)»;
- §«Модель ветвления»: в запрет прямых коммитов добавлена отсылка «(кроме
  служебной очистки логов — см. „Подтверждение и очистка логов“)».
**Версия:** HEAD `f31d5a3` + рабочее дерево (изменения не закоммичены); снимок
2026-09-26.
**Вердикт:** принято.

## Находки

**P1:** — критичных проблем нет.

**P2:** — нет; P2/P3 аудита r4 устранены.

**P3:** — нет.

## Проверки

- `git diff .opencode/rules/git-workflow.md` → ровно две правки, других
  изменений нет:
  1. §«Модель ветвления» (строки 26–28) — отсылка к исключению в запрете прямых
     коммитов;
  2. §«Подтверждение и очистка логов» (строки 113–114 и 119–121) — момент
     сверки «после коммита подтверждения и до коммита очистки»; новый пункт
     ссылается на пункт о сверке, а не повторяет протокол.
- Протокол не дублируется: двухшаговость описана один раз — ссылкой «удаление
  ленты оформляется двухшаговым пакетом — см. пункт о сверке подтверждения из
  истории выше» (строка 121), а сам момент сверки — в пункте 112–114
  (`git show HEAD:<лента>`).
- `rg -n "ветка на изменение" .opencode AGENTS.md` → пусто (формулировка
  удалена).
- Ссылки-якоря живые: заголовки `## Модель ветвления (git-flow)` (строка 13) и
  `## Подтверждение и очистка логов` (строка 109) существуют; перекрёстные
  отсылки взаимны и однозначны.
- `git status --porcelain` → прежний периметр: `M git-workflow.md`,
  `M mail/service-git-flow.md`, `M memory/git.md`, `M memory/validator.md`;
  `?? docs/reviews/service-git-flow-2026-09-26-r4.md` (отчёт r4) — новых
  посторонних правок нет.
- `git status --porcelain -- src tests Cargo.toml Cargo.lock` → пусто.

## Согласованность

- §«Модель ветвления» и §«Подтверждение и очистка логов» теперь ссылаются друг
  на друга; запрет прямых коммитов и его единственное исключение читаются
  вместе, без «тихой» отмены правила.
- `AGENTS.md` §Git (290–297) делегирует ограничения `git-workflow.md` — дубля
  нет; §Память и почта (112–113) — сервисные ленты и `clean-logs.mjs` —
  согласованы; путь бэкапа `clean-logs.mjs:117` совпадает с
  `<temp>/opencode/logs-backup-<ts>/` правила.
- Двухшаговый протокол правила совпадает с подтверждённым пакетом ленты
  (`service-git-flow.md:111–116`).

## Что проверено и ок

- Правка — ровно по рекомендациям аудита, без сопутствующих изменений.
- Дублирование протокола устранено (Q41), формулировка исключения явная и
  со ссылками в обе стороны.
- Периметр и `src`/`tests`/`Cargo.*` — без отклонений.

**Почему без полного DoD и `features_inventory`:** изменение — только текст
правила и рабочие данные ролей; `src/**`, `tests/**`, `Cargo.*`,
`docs/features/**` не менялись (`git status --porcelain -- src tests Cargo.toml
Cargo.lock` → пусто).

**Примечание (процесс, не находка):** отчёты `…-r4.md` и `…-r5.md` не входят в
подтверждённый двухкоммитный пакет и останутся untracked. Решение за `lead`:
включить отчёты в коммит 1 либо оставить вне пакета (на содержимое коммитов не
влияет).
