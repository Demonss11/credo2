# Повторная приёмка T-03 (H5, r2)

**Проверка:** повторная приёмка после закрытия `docs-writer` (Н5). Периметр —
только документация, внесённая после приёмки r1: `docs/GRAMMAR.md` §2.7
(сведение с `src/core.rs:444`), статусы T-03/T-11 → ✅, сводка
`docs/tasks/README.md`, ссылки на отчёты, `docs/CHANGELOG.md` (T-03
«Добавлено»/«Ломающие», итог пилота T-11).

**Версия:** рабочее дерево поверх `e0ada18` (дата снимка 2026-09-26);
HEAD не менялся с r1 — правки r2 не закоммичены, как и весь дифф T-03.

**Вердикт:** принято

**P1:** —
**P2:** —
**P3:** —

**Проверки:**
- `git log --oneline -5` → HEAD = `e0ada18` (тот же, что на r1) — коммитов
  между r1 и r2 не было.
- `git status --short` → изменены `docs/CHANGELOG.md`, `docs/GRAMMAR.md`,
  `docs/tasks/README.md`, `docs/tasks/T-03-check-create/README.md`,
  `docs/tasks/T-11-agent-cycle/README.md` + `src/core.rs`, `src/mcp.rs`,
  `tests/mcp_draft.rs`; новый `docs/reviews/T-03-2026-09-26.md`.
- `git status --short -- src tests Cargo.toml Cargo.lock` → только
  `src/core.rs`, `src/mcp.rs`, `tests/mcp_draft.rs` (дифф T-03 из r1),
  `Cargo.toml`/`Cargo.lock` не тронуты.
- `git diff --stat -- src tests Cargo.toml Cargo.lock` → `core.rs 2 +-`,
  `mcp.rs 236`, `mcp_draft.rs 193` — **построчно совпадает с диффом r1**
  (та же правка в `core.rs` — 1 вставка/1 удаление; те же объёмы `mcp.rs` и
  `mcp_draft.rs`). Код после r1 не менялся.
- `git diff -- docs/...` → правки r2 только в 5 документах; счётчики
  `features_inventory` и `docs/features/README.md` не затронуты.

**Что проверено и ок:**
- **GRAMMAR §2.7** — строка 7 таблицы «Ограничения v0.1» теперь
  «отсутствует заголовок правила (Не найдено имя правила)», «Не найдено
  условие», «Не найдено решение»; сверено с `src/core.rs:444/445/460` —
  построчно совпадает. P3 из r1 снят.
- **Статусы:** T-03 → ✅ (карточка + сводка), T-11 → ✅ (карточка + сводка);
  поле «Отчёт приёмки» в карточке T-03 → `../../reviews/T-03-2026-09-26.md`
  (путь существует).
- **Ссылки живые:** `docs/reviews/T-03-2026-09-26.md`,
  `T-11-2026-09-26.md`, `T-11-2026-09-26-r2.md` существуют; относительные
  пути из карточек и CHANGELOG разрешаются.
- **CHANGELOG не искажает факты:** «Добавлено» T-03 — обязательные
  `{name, source}`, сверка с заголовком, конверт `validation_failed`, +14
  тестов, DoD 92/92 (совпадает с r1: 92 passed / 0 failed); «Ломающие» —
  конверт ошибок `check.create` (соответствует §4.5/Q29). Итог пилота T-11
  ссылается на отчёты T-11 (r1/r2), не на будущий вердикт T-03.
- **`docs/features/README.md`** не менялся — `draft.feature` остаётся 🟡
  (открыты T-02/T-05), счётчик 12 не тронут → `features_inventory` согласован.
- **DoD не прогонялся** — обоснованно: `src/**`, `tests/**`, `Cargo.toml`,
  `features/**` после приёмки r1 не менялись (дифф r2 — только `docs/**`);
  тесты на r1 зелёные (92/92), повторный прогон не добавляет информации
  (аналог r2 T-11).
