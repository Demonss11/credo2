# Приёмка T-03 — `check.create`: `{name, source}`

**Проверка:** задача T-03 (`docs/tasks/T-03-check-create/README.md`) — контракт
`check.create` `{name, source}` (Q28, SPEC §10 №31, §4.5); код, покрытие,
DoD, согласованность с карточкой и сценариями.

**Версия:** незакоммиченное рабочее дерево поверх `e0ada18` (снимок 2026-09-26);
изменения — `src/mcp.rs`, `src/core.rs`, `tests/mcp_draft.rs`
(+ статус 🚧 в `docs/tasks/*`, от `docs-writer`).

**Вердикт:** принято с замечаниями (1×P3 — док-разночтение, не блокер кода).

**P1:** —
**P2:** —
**P3:** `src/core.rs:444` ↔ `docs/GRAMMAR.md:56` — `parse_rule` теперь отдаёт
«отсутствует заголовок правила (Не найдено имя правила)», а GRAMMAR §2.7
(таблица «Ограничения v0.1», строка 7) фиксирует строку «Не найдено имя
правила». Документированная строка остаётся подстрокой нового сообщения
(сценарий `draft.feature:69` требует «отсутствует заголовок правила»), поэтому
машинный контракт не затронут; расхождение — текст документации. → кандидат
для `docs-writer` при закрытии: свести GRAMMAR §2.7 с фактическим сообщением
(или перенести обёртку из `parse_rule` на уровень `check.create`).

**Проверки:**
- `cargo fmt --check` → ok (без расхождений).
- `cargo clippy --all-targets -- -D warnings` → ok (0 warnings).
- `cargo test --all` → 92 passed / 0 failed (50 lib + 4 features_inventory +
  15 mcp_draft + 12 publish + 11 rest).

**Что проверено и ок:**
- Карточка T-03: статус 🚧, приоритет P1, Источник Q28 (SPEC §10 №31) — путь
  и ссылка верны; SPEC §10 строка 31 и §4.5 (контракт `{name, source}`,
  `validation_failed`) согласованы.
- Сценарии `docs/features/draft.feature` («Создание черновика через MCP»,
  «Повторный check.create перезаписывает черновик», «check.create с невалидным
  source отклоняется») и `docs/features/agent_minimal.feature` (~34–43) — не
  противоречат коду.
- Код `src/mcp.rs`: `required_str` (отсутствие/не-строка/пустая строка →
  `validation_failed`), сверка `rule.name != name` (сообщение называет оба
  имени), ответ ровно `{status, name}`; прочие инструменты через
  `map_err(ToolError::from)` сохраняют плоский `{"error":"<текст>"}` —
  поведение не менялось.
- Покрытие тестами (`src/mcp.rs` юнит + `tests/mcp_draft.rs` stdio): имя
  совпало/не совпало, отсутствие/пустой/не-строка `name`, отсутствие `source`,
  невалидный `source` (черновик не создан, существующий не тронут), upsert
  (`count=1`, старый текст не остаётся), сохранность текста побайтово (без
  trim), форма конверта. Тесты проверяют заявленное, не «зеленеют сами».
- `tests/features_inventory.rs` — счётчики фич и README согласованы (не менялись
  в этом диффе).
