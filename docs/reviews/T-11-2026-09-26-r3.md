# Приёмка T-11 · r3 (патч v2.1, после синхронизации схемы A)

**Проверка:** повторная приёмка патча v2.1 T-11 (порог существенности H5, схема A
авторства канона, риск/guard размерных маршрутов, периодический чекпойнт) после
закрытия находок r3 (P1 журнал↔схема A; P2 порог в фиче; P3 CHANGELOG/чекпойнт/`--auto`).
**Версия:** база HEAD `2901001`, незакоммиченное рабочее дерево (снимок 2026-09-26).
**Вердикт:** принято

**P1:** — (закрыто: `D38-agent-cycle.md:53-57`, `SPECIFICATION.md:859` §10 №38,
`Q43.md:62-65` — «правки канона вносит сервисная сессия владельца по протоколу;
`auditor` (`mode: all`) — независимая приёмка, канон не правит», пометка
«уточнено 2026-09-26: схема A»).
**P2:** — (закрыто: `agents-rework.feature:31-33` — порог приведён к `review.md`:
`src/tests` — полный прогон и новый отчёт; канон/права — новый отчёт с машинной
сверкой прав и адресными проверками; механические docs — адресная проверка).
**P3:** — (закрыто: `CHANGELOG.md:45-49` «Primary-агент» помечен «пересмотрено:
`mode: all`, канон не правит — схема A»; `AGENTS.md:89-90` периодический чекпойнт
`K ≈ steps/3` ≤ 8; `AGENTS.md:127` headless `--auto`).

**Проверки:**
- `git status --porcelain -- src tests Cargo.toml Cargo.lock` — пусто (код не менялся);
- `opencode debug agents` — 17 агентов; `steps` у всех 10 ролей; `cargo test *`
  только у `validator`; `opencode debug agents` — у `validator` и `auditor`;
  edit у `auditor` — только своя память; `question` у `lead`, у `git` — только `ask`;
- `cargo fmt --check` — чисто;
- `cargo test --test features_inventory` — 4 passed / 0 failed (счётчики 42/253,
  процесс агентов 6/30 сходятся).

**Что проверено и ок:** журнал Q43/D38/SPEC §10 №38 — синхронизированы со схемой A;
`review.md` ↔ `validator.md` ↔ `agents-rework.feature` — порог H5 согласован;
`AGENTS.md` ↔ `auditor.md` ↔ `lead.md` ↔ `agents-audit.feature` — схема A без
расхождений; размерные маршруты S/M/L ↔ `agents-sized-routes.feature` (риск-максимум
и guard git); периодический чекпойнт и `--auto` — в каноне; счётчики `README.md`
согласованы с `features_inventory`; изменения ограничены периметром патча.

Полный DoD (`clippy`, `cargo test --all`) не запускался обоснованно: `src/**`,
`tests/**` не менялись с r1 (78/78), правки — только `.opencode/**`, `AGENTS.md`
и `docs/**`; фичи проверены адресно через `features_inventory`.
