# Приёмка r2: права shell и инструкции (service-permissions)

**Проверка:** (1) пробы прав shell — форма без `--`, отказ `--` + скрытый путь,
право `clean-logs` только у `git`; (2) правки инструкций: нет `--`-шаблонов,
нормы о техпроблемах — короткие ссылки, `git.md` согласован с правом и
`git-workflow.md`, `review.md` — рабочая форма; (3) машинные проверки: YAML,
`cargo test` только у `validator`, чистота кода.
**Версия:** HEAD `2706f4c chore: git-workflow — служебный коммит очистки логов`
+ рабочее дерево; снимок 2026-09-26.
**Вердикт:** принято.

## 1. Пробы прав (одиночные команды)

| № | Команда | Ожидание | Результат | Вывод |
|---|---|---|---|---|
| 1 | `git diff .opencode/rules/review.md` | выполнена | **выполнена** | форма без `--` для скрытого пути работает |
| 2 | `git status .opencode` | выполнена | **выполнена** | периметр = 7 файлов (см. ниже) |
| 3 | `git diff -- .opencode/rules/review.md` | отклонена | **отклонена** (`permission.rejected`) | подтверждает документированный отказ `--` + скрытый путь |
| 4 | `node .opencode/scripts/clean-logs.mjs --dry-run` | отклонена | **отклонена** (`permission.rejected`) | право очистки — только у `git` |
| 5 | `git status --porcelain -- src tests Cargo.toml Cargo.lock` | пусто | **выполнена, пусто** | `--` перед не-скрытыми путями проходит — согласуется с формулировкой `review.md` |

Пробы 1–2 идут с `opencode reload`-конфигом текущей сессии; проба 3 —
избирательный отказ движка по признаку «`--` + скрытый путь» (не снят), проба 4
— отсутствие права у `validator`. Оба отклонения совпадают с ожиданием задания.

## 2. Правки

- **`--`-шаблонов нет.** `rg "(diff|status) --"` по `.opencode/agents` —
  совпадений нет; в YAML всех трёх ролей формы `git status *`, `git diff *`,
  `git log *`, `git show *` (+ `git grep *` у `validator`).
  *Уточнение по периметру:* относительно HEAD `2706f4c` правка невидима —
  `git grep "git diff" HEAD .opencode/agents` даёт там те же `git diff *`.
  Удаление относится к состоянию, проверенному в r1
  (`service-permissions-2026-09-26.md`, P2), где стоял `git diff -- *`;
  итоговое состояние — без `--`, то есть требование выполнено.
- **Право очистки только у `git`.** `rg clean-logs` по `.opencode` — shell-права
  лишь в `git.md:27–28` (`node .opencode/scripts/clean-logs.mjs` и `… *`);
  у `validator`/`auditor` таких прав нет (проба 4).
- **Нормы о техпроблемах — короткие ссылки, без дубля.** Канон —
  `review.md:93`; в ролях: `validator.md:106`, `auditor.md:122` — одна строка со
  ссылкой; `git.md:95–101` — git-специфика (остановка, состояние, запрет
  частичного завершения) + ссылка на `review.md`. Дословных повторов нормы нет.
- **`git.md` согласован с правом и правилом.** Тело (:49–51) «git-операции и
  очистку логов» → якорь `git-workflow.md` §«Подтверждение и очистка логов»
  (:109–122) существует; там же (:115) «Очистку в пакете выполняет `git`» —
  совпадает с правом `git.md:27`.
- **`review.md` — рабочая форма без `--`.** `:87–89` фиксирует отказ движка на
  `--` + скрытый путь и рабочие формы `git diff .opencode/…`,
  `git status .opencode` — подтверждено пробами 1–2.

## 3. Машинные проверки

- `opencode debug agents` — **выполняется без ошибок, YAML валиден**; 17 агентов
  (auditor, build, coder, compaction, docs-writer, explore, general, git, lead,
  migrator, plan, researcher, rust-expert, summary, tester, title, validator).
  Блок `git` (id на строке 887 вывода) содержит `node .opencode/scripts/clean-logs.mjs`;
  у `validator` (id 2205) — `git diff * без `--`.
- `cargo test *` в правах — **только** `validator.md:19` (`rg "cargo test"` по
  `.opencode/agents`: в остальных ролях — лишь текст инструкций). В выводе
  `debug agents` единственное право `cargo test *` — в блоке `validator`.
- `git status --porcelain -- src tests Cargo.toml Cargo.lock` — **пусто**.

## DoD не прогонялся (обоснование)

Периметр изменения — только `.opencode/**` (права и инструкции ролей) и рабочие
данные памяти: `M .opencode/agents/{auditor,git,validator}.md`,
`M .opencode/rules/{git-workflow,review}.md`,
`M .opencode/memory/{git,validator}.md`. `src/`, `tests/`, `Cargo.toml`,
`Cargo.lock` — без изменений (проверка выше). Код и фичи не затронуты, поэтому
`cargo fmt --check` / `clippy` / `test` неприменимы — как и в r1.

## Находки

**P1:** — критичных проблем нет.
**P2:** —
**P3:** —

**Примечание (не находка).** `memory/git.md` (рабочие данные, не канон) в
чекпойнте ссылается на «HEAD старта `f31d5a3`», тогда как фактический HEAD —
`2706f4c` (его ребёнок); текст описывает уже сделанный служебный коммит. Память
входит в пакет очистки и на права не влияет; фиксирую для сведения `lead`.

## Что проверено и ок

- пробы прав 1–5 — совпали с ожиданием задания;
- YAML трёх ролей валиден (`opencode debug agents`), сужение прав не расширено
  (`clean-logs` только у `git`; `cargo test` только у `validator`);
- `--`-шаблоны в правах ролей отсутствуют; `review.md` документирует рабочую
  форму и причину (пробы совпадают);
- `git.md` ↔ `git-workflow.md` §«Подтверждение и очистка логов» ↔ право
  `git.md:27` — согласованы;
- нормы о техпроблемах не дублируются (канон — `review.md`);
- `src`/`tests`/`Cargo.*` и `docs/features/**` не затронуты.

**Примечание (процесс):** память и лента не изменялись (по указанию задания —
входят в пакет очистки); настоящий отчёт — untracked, решение о включении в
пакет — за `lead`.
