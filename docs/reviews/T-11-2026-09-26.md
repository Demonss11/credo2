# Приёмка T-11 «Цикл агентов» — 2026-09-26

**Проверка:** T-11 «Цикл агентов» — журнал (Q43/D38), канон и роли
(`AGENTS.md` ↔ `.opencode/agents/**`, `review.md`, `git-workflow.md`), R2,
память/почта, docs-правки, полный DoD.

**Версия:** рабочее дерево поверх `e1b2550` (ветка `master`), снимок 2026-09-26.
Незакоммиченные правки: 10 ролей + `AGENTS.md`, `rules/*`, `docs/*`; новые
файлы `docs/questions/Q43.md`, `docs/decisions/D38-agent-cycle.md`.

**Вердикт:** принято с замечаниями

**P1:** —

**P2:**

- `.opencode/rules/review.md:67` — «Доступные команды» приписывает `validator`
  команду `cargo check`, которой нет во фронтматтере `validator.md` (только
  `cargo fmt|clippy|test`). Последствие: `validator`, следуя канону, упрётся в
  `permission.rejected` (класс Н2). Правка — убрать `check` из списка validator.
- `.opencode/rules/review.md:68` — «Доступные команды» приписывает группе
  `coder, rust-expert` команду `cargo build`, которой нет у `rust-expert`
  (только `cargo check|clippy|fmt`; тело `rust-expert.md:73` прямо запрещает
  `cargo build --release`). Последствие: `rust-expert` по канону упрётся в
  `permission.rejected` (класс Н2). Правка — развести группу / убрать `build`
  у rust-expert.

Обе находки — в `.opencode/rules/review.md`, зоне `auditor` (не `coder`);
фиксируются здесь как «инструкция ↔ права» для аудита, не блокируют R2.

**P3:** —

**Проверки:**

- `cargo fmt --check` → ok (без вывода).
- `cargo clippy --all-targets -- -D warnings` → ok, 0 предупреждений.
- `cargo test --all` → ok, 78 passed / 0 failed
  (unit 43 + features_inventory 4 + mcp_draft 8 + publish 12 + rest 11).

**Что проверено и ок:**

- **Журнал (BRIEF §5.7):** `docs/questions/Q43.md` и
  `docs/decisions/D38-agent-cycle.md` существуют; ID уникальны (Q43/D38/T-11);
  поля D верны (`Resolves`→Q43, `Спека`→SPEC §10 №38, `Affects`, `Tasks`→T-11);
  вердикт «Сверка с кодом» = ⚪ не применимо + явное «Задач не требуется»;
  строка №38 есть в `SPECIFICATION.md` §10; `TRACEABILITY.md` согласован
  (Q43 → D38 → T-11, `resolved`).
- **Канон и роли:** `AGENTS.md` §Рабочая группа агентов ↔ 10 ролей
  `.opencode/agents/*.md` — маршрут R1, память/почта R3–R5, `steps`/лимиты
  согласованы; дублей маршрута нет (роли ссылаются на AGENTS.md); фронтматтер
  каждой роли согласован с телом; `git-workflow.md` (пакетное подтверждение,
  идемпотентность, таймаут push) на месте.
- **R2:** `cargo test` разрешён только у `validator` (фронтматтер); у
  `coder`/`rust-expert`/`tester` — check/clippy/fmt без тестов, у
  `docs-writer`/`migrator`/`lead` — без cargo; вывод `opencode debug agents`
  приложен записью `lead` в ленте.
- **Память и почта:** 10 файлов `.opencode/memory/<роль>.md` существуют; каждая
  роль пишет только свой файл памяти (плюс append в ленту); лента
  `.opencode/mail/T-11.md` ведётся append по формату `AGENTS.md`; память/почта
  не в `.opencode/agents/**`.
- **Документы:** правки `BRIEF.md` §5.3/§5.7 (R2-акцент), `CHANGELOG.md`,
  `README.md` («Куда за чем», «Состояние миграции»), `tasks/README.md` (DoD) —
  согласованы; относительные ссылки живые; дублей канона нет; статус T-11 🚧 в
  карточке и сводке.
