**Проверка:** T-11 «Цикл агентов: Agile-петля, единый тестировщик, память и почта» — доработка v2 (журнал, канон агентов и роли, права, память/почта, фичи, документация)
**Версия:** рабочее дерево (незакоммичено) на HEAD `e1b2550`, ветка `exp/agent-cycle-rerun`
**Вердикт:** принято

**P1:** —
**P2:** —
**P3:**
- `.opencode/agents/validator.md:53` — «Прогони полный DoD (см. «Код»)» — в `AGENTS.md` нет раздела «Код» (есть «Сборка, тесты и пересборка»); отсылка неточна → заменить на «см. «Сборка, тесты и пересборка»» либо прямо вписать три команды DoD (одна строка).
- `docs/features/agents-audit.feature:35` — «Primary-агент доступен для приёмки» / «`auditor` — агент режима "all"»: `auditor` имеет `mode: all` (фронтматтер `auditor.md:3`), а не `primary`; формулировка «Primary-агент» устарела (след CHANGELOG-строки «Primary-агент auditor») → переименовать в «Служебный агент (mode: all)» (одна строка).

**Проверки:**
- `cargo fmt --check` → чисто (без вывода).
- `cargo clippy --all-targets -- -D warnings` → `Finished`, без предупреждений.
- `cargo test --all` → **78 passed, 0 failed** (43 lib + 0 main + 4 features_inventory + 8 mcp_draft + 12 publish + 11 rest); `features_inventory` зелёный — счётчики README согласованы.

**Что проверено и ок:**

- **Журнал (BRIEF §5.7):** `docs/questions/Q43.md` и `docs/decisions/D38-agent-cycle.md` существуют; ID уникальны (Q43 не дублируется, `docs/OPEN_QUESTIONS.md` его не содержит; последний Q — Q42, последнее решение §10 — №37, Q43/D38 — новые, не перенос из архива). Поля D38 заполнены верно: `Resolves` → Q43, `Спека` → SPEC §10 №38, `Affects` (6 `agents-*.feature` + AGENTS/roles/rules), `Tasks` → T-11. Вердикт «Сверка с кодом» — ⚪ «не применимо» (процессное решение), обоснован; задачи не из сверки (реализация уже поставлена T-11). Строка решения №38 есть в `docs/SPECIFICATION.md` §10 (строка 859), `docs/TRACEABILITY.md` согласован (Q43 → D38 → `agents-cycle.feature` и др. → T-11, статус `resolved`), «Источник» карточки T-11 и сводки `tasks/README.md` → D38 (Q43).
- **Ссылки:** относительные пути Q43↔D38, D38→SPEC/AGENTS/TRACEABILITY/features, TRACEABILITY→questions/decisions/features/tasks — все ведут в существующие файлы; битых ссылок не найдено.
- **Канон и роли:** `AGENTS.md` §Рабочая группа агентов (цикл, размерные маршруты S/M/L, память/почта, лимиты `steps`, служебная зона) ↔ 10 ролей `.opencode/agents/*.md` — фронтматтер и тело согласованы; роли ссылаются на канон (`AGENTS.md`) без дублей маршрутов (политика Q41). Таблица ролей `AGENTS.md` = 10 файлам `.opencode/agents/`; «Пишет в» каждой роли совпадает с `edit`-правилами её фронтматтера (доп. зоны памяти/ленты — в правах).
- **R2 и права:** `cargo test *` — только у `validator` (проверено по всем 10 фронтматтерам); `cargo build` — ни у кого (в т.ч. `rust-expert` явно «не запускает `cargo build`»); `tester` — `cargo check *` + `cargo fmt *` (без `clippy`, без `test`); `coder`/`rust-expert` — `check`/`clippy`/`fmt`; `docs-writer`/`migrator`/`lead`/`git`/`researcher` — без cargo; `auditor` — `mode: all`, без cargo. `steps` задан у всех 10 ролей. Машинная сверка прав — в ленте (запись `lead`: `opencode debug agents`, 17 агентов; `cargo test` — 1, `cargo build` — 0, `steps` у всех).
- **Память/почта:** 10 файлов `.opencode/memory/<роль>.md` существуют, формат — «Память: <роль> + канон + правило + Чекпойнты» (по `AGENTS.md`); лента `.opencode/mail/T-11.md` в git (не игнорируется: `.opencode/.gitignore` — только `node_modules`/`package*.json`/`bun.lock`; `memory/` и `mail/` видны как untracked, не игнорированы). В `.opencode/agents/**` только 10 `.md`-агентов — памяти/почты там нет (Н11).
- **Фичи:** 6 файлов `docs/features/agents-{cycle,rework,memory-mail,git-approval,sized-routes,audit}.feature` — 27 сценариев (6+3+6+3+4+5); обратные ссылки `# D38 (Q43): …` в шапках всех 6 (BRIEF §5.6 шаг 2 — замечание `migrator` закрыто); строки в `docs/features/README.md` (раздел «Процесс агентов») и «Итого: 42 файла, 250 сценариев» сходятся (backend 27/148 + frontend 9/75 + процесс 6/27); подтверждено тестом `features_inventory` (4/4 green).
- **Документы:** правки `docs/BRIEF.md` §5.3 (адресный прогон — `validator` через `lead`) и §5.7 (полный `cargo test` — только `validator`) согласованы с `review.md` («Доступные команды») и фронтматтерами; `docs/CHANGELOG.md` — запись «Цикл агентов v2 (T-11)» со ссылками D38/Q43/T-11; `docs/README.md` («Куда за чем») и `docs/tasks/README.md` (DoD + T-11 → 🚧) согласованы; карточка T-11 — статус 🚧, раздел «Доработка v2 (2026-09-26)», «Примечания» — на месте. Дублей канона не найдено (маршруты — один раз в `AGENTS.md`, роли ссылаются).
- **`review.md` ↔ `git-workflow.md` ↔ BRIEF:** команды ролей в `review.md` совпадают с фронтматтерами; git-пакет/идемпотентность/push≥5 мин в `git-workflow.md` согласованы с `git.md` и `AGENTS.md` §Git; shell-команды одиночные — закреплено в `AGENTS.md` и `review.md`.

**Вне-объёмные факты** (не блокеры, к приёмке v2 не относятся): пилот v2 (T-03) и аудит `auditor` идут отдельными сессиями (заявлено в ленте/карточке); `validator`/`auditor`/`rust-expert` имеют отличные от остальных `steps` (32/36/24 vs 20–44) — калибровка по ролям заявлена как P2-пункт доработки, не требование приёмки.
