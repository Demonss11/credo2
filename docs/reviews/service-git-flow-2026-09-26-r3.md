# Приёмка (завершающая, r3): git-flow — свёртка раздела «Ветки задачи» в git.md

**Проверка:** адресная завершающая приёмка после P3 аудита r2. Исправлено ровно
одно: `.opencode/agents/git.md` — раздел «Ветки задачи» свёрнут до ссылки на
`.opencode/rules/git-workflow.md` («Старт задачи», «Завершение задачи»); полные
последовательности команд убраны из тела роли (политика Q41 — один источник).
**Версия:** HEAD `5b6f978` + рабочее дерево (изменения не закоммичены); снимок
2026-09-26 (тот же HEAD, что в r1/r2 — новых коммитов нет).
**Вердикт:** принято.

## Находки

**P1:** — критичных проблем нет.

**P2:** — нет.

**P3:** — нет.

## Проверки

- `git diff .opencode/agents/git.md` → в файле относительно HEAD: два права
  (пришли ранее — `git branch -d *` ask, `question` deny) и новый раздел
  `## Ветки задачи` (текущая, свёрнутая редакция). Полных командных цепочек
  (`git switch develop`, `git switch -c …`, `git merge --no-ff …`,
  `git push …`, `git branch -d …`) в теле нет — остались только описательные
  слова «merge `--no-ff` в `develop`, push, удаление ветки».
- `rg -n "git switch|git merge|git push|git branch" .opencode/agents/git.md` →
  совпадения только в строках разрешений YAML (`git switch *`, `git merge *`,
  `git branch -d *`, `git push *` — это права, не инструкции) и в политике
  `git.md:68` («`git push` — с таймаутом не менее 5 минут»). Дублей команд в
  теле нет.
- Ссылка корректна: `.opencode/rules/git-workflow.md` содержит заголовки
  `## Старт задачи` (строка 45) и `## Завершение задачи` (строка 66) — якоря
  ссылки существуют; сводка git.md («Старт — ветка до работы», «завершение —
  merge `--no-ff` в `develop`, push, удаление») совпадает с их содержанием
  (строки 45–53 и 66–73).
- `git status --porcelain` → периметр прежний (10 M-файлов сервисного
  изменения) + `git.md` + новые лента `service-git-flow.md` и три отчёта
  приёмки (`…-2026-09-26.md`, `…-r2.md`, и настоящий); лишних правок вне зоны
  нет.
- `opencode debug agents` → конфиг читается из текущих файлов без `reload`;
  применённые права совпадают с YAML: у `git` shell read-only ветки (`-l`/`-a`/
  `--show-current`), `git branch -d *` = `ask`, `question` = `deny`; модели
  видимых ролей — `deepseek-v4.1-flash`; формат вывода и размер (2380 строк)
  идентичны прогонам r1/r2 — состав и модели не изменились.
- (Права `git` дополнительно сверены напрямую по YAML: `git.md:32` —
  `git branch -d *` ask, `git.md:41` — `question` deny; те же значения, что в
  r2, изменений нет.)

## Что проверено и ок

- **Q41 соблюдён:** источник команд веток — один (`git-workflow.md`,
  «Старт задачи» / «Завершение задачи»); `git.md` и `AGENTS.md` ссылаются на
  него, не воспроизводя цепочки. `AGENTS.md` содержит только формулировку
  «ветка `feature/T-XX-<слаг>` от `develop` создаётся до работы» (строка 53) —
  без команд.
- **Согласованность `git.md` ↔ `git-workflow.md` ↔ `AGENTS.md`** сохранена:
  ветка до работы, merge `--no-ff` в `develop`, push, удаление — во всех трёх
  источниках непротиворечиво.
- **Периметр:** правка — только текст роли в служебной зоне; `src`, `tests`,
  `Cargo.toml`, `Cargo.lock` не тронуты, фичи/код не менялись.

**Почему без полного DoD и `features_inventory`:** изменение — исключительно
текст роли `.opencode/agents/git.md` (служебная зона), код `src/**`, тесты
`tests/**`, `Cargo.*` и фичи `docs/features/**` не изменялись
(`git status --porcelain -- src tests Cargo.toml Cargo.lock` → пусто; счётчики
README 42/255 и процесс 32 — как в r1/r2). `cargo fmt`/`clippy`/`test` на текст
роли не влияют, прогон не дал бы новой информации; `features_inventory` уже
зелёный и фич не касается.
