**Проверка:** T-11 v2 — повторная приёмка после правок (r1 вердикт «принято», 2×P3 + замечание аудита по BRIEF §5.6)
**Версия:** рабочее дерево (незакоммичено) на HEAD `e1b2550`, ветка `exp/agent-cycle-rerun`
**Вердикт:** принято

**P1:** —
**P2:** —
**P3:** —

**Проверки:**

- `git status --porcelain -- src tests Cargo.toml Cargo.lock` → пусто (код, тесты и манифест не менялись).
- `git status --short` → изменённое дерево v2 целиком (`.opencode/**`, `AGENTS.md`, `docs/**`); новых правок вне перечисленных файлов нет. Изолировать «постприёмочный» diff нельзя — дерево не коммитилось (база `e1b2550`), поэтому правки проверены по текущему содержимому перечисленных файлов.
- `rg "question" .opencode/agents/git.md` → нет вхождений (право `question` убрано; на изменяющих командах осталось `ask`: `add/commit/switch/checkout/merge/tag/restore/push/fetch/pull`).

**Что проверено и ок (адресно, по пунктам задания):**

1. **Перечисленные правки — на месте, ничего лишнего:**
   - `git.md` §Пакет и подтверждение: подтверждение — единственное, у `lead` (запись в ленте, до вызова `git`); «пользователя сам не спрашивай»; `question` в permissions нет.
   - `git-workflow.md` §Пакет и подтверждение: `lead` получает одно подтверждение (`question`) и фиксирует пакет в ленте; `git` сверяет по ленте, пользователя не спрашивает; `add/commit/push` под `ask`.
   - `validator.md:53` — «Прогони полный DoD (см. чек-лист «Код» ниже)»; глава «Код» существует (`validator.md:69`, `**Код (`T-XX`):**`), отсылка теперь точна.
   - `agents-audit.feature:34` — заголовок «Сценарий: Агент доступен для приёмки» (было «Primary-агент…»); строка 35 `Дано "auditor" — агент режима "all"` согласована с `auditor.md:3` (`mode: all`).
   - `BRIEF.md` §5.6 п.2 — форма обратной ссылки `# Dn (Qx): краткая формулировка`; заявлено «отдельного блока `Decisions:` в фичах нет» (соответствует фактическим шапкам фич).
2. **Согласованность подтверждения git:** `AGENTS.md:62` «(подтверждение пользователя — один раз)»; `lead.md:94` «одно подтверждение (`question`) — до вызова `git`» + `question: allow` (`lead.md:31`); `git-workflow.md` и `git.md` — один раз/у `lead`/по ленте; `agents-git-approval.feature` — «Пакет подтверждается один раз», «без подтверждения изменяющие команды не выполняются», «только после этого lead вызывает git». Расхождений нет.
3. **Фичи:** 6 файлов `agents-*` (cycle 6, memory-mail 6, sized-routes 4, audit 5, git-approval 3, rework 3 = **27 сценариев**); обратные ссылки `# D38 (Q43)` в шапках всех 6; `README.md:284-286` «Итого: 42 файла, 250 сценариев» (backend 27/148 + frontend 9/75 + процесс 6/27) — не поехали.
4. **DoD полный не запускался — обоснование:** `src/**`/`tests/**` не менялись (см. п.1); правка фичи — только переименование заголовка сценария, `features_inventory` считает сценарии по строкам `Сценарий:`/`Структура сценария:` (`tests/features_inventory.rs:52-58`) и тексты заголовков не валидирует — счётчики 27/42/250 не затронуты; на r1 полный прогон — 78/78 и `features_inventory` зелёный. Регрессионного риска нет.

**Вне-объёмное:** правки вносятся в незакоммиченное дерево v2 (коммит — после приёмки, ролью `git`); аудит `auditor` и пилот T-03 — отдельными сессиями (не блокеры данной проверки).
