# Повторная приёмка T-11 (H5, r2)

**Проверка:** повторная приёмка после правок 2×P2 (Н5) — `.opencode/rules/review.md`
«Доступные команды» и `.opencode/agents/coder.md`.
**Версия:** рабочее дерево поверх `e1b2550` (дата снимка 2026-09-26; правки
после приёмки не закоммичены).
**Вердикт:** принято

**P1:** —
**P2:** —
**P3:** —

**Проверки:**
- `git status --short` → изменены только артефакты T-11 (10 ролей,
  `rules/review.md`, `rules/git-workflow.md`, `AGENTS.md`, docs; + Q43/D38/отчёт
  приёмки); `git status --short -- src tests Cargo.toml Cargo.lock` → пусто.
- `git diff` → `review.md` «Доступные команды»: `validator` — `cargo
  fmt|clippy|test` (без `cargo check`); `coder`, `rust-expert` — `cargo
  check|fmt|clippy` (без `cargo build`); `tester` — `cargo check`, `cargo fmt`.
- Чтение YAML-фронтматтеров `validator`/`coder`/`rust-expert`/`tester` →
  совпадают со списками `review.md`.
- `rg "cargo (build|test|check|clippy|fmt)" .opencode/agents` → `cargo build`
  нет ни у кого; `cargo test` — только `validator`; `cargo check` — только
  `coder`/`rust-expert`/`tester`.

**Что проверено и ок:**
- 2×P2 закрыты: расхождение «Доступные команды» ↔ фронтматтеры устранено.
  Сверка построчно:
  - `validator`: `rg`, `git status|diff|log|show|grep`, `cargo fmt|clippy|test`
    = YAML (fmt/clippy/test + rg + git status/diff/log/show/grep), без `check`.
  - `coder`: `rg`, `git status|diff`, `cargo check|fmt|clippy` = YAML.
  - `rust-expert`: `rg`, `git status|diff`, `cargo check|clippy|fmt` = YAML.
  - `tester`: `rg`, `git status|diff`, `cargo check`, `cargo fmt` = YAML
    (без clippy/test).
- `src/**`, `tests/**`, `Cargo.toml`, `Cargo.lock` не менялись → полный DoD не
  обязателен: на приёмке r1 тесты зелёные (78 passed / 0 failed), после неё
  код не трогали; повторный прогон не добавляет информации.
