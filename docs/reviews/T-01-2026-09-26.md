**Проверка:** T-01 «Черновик: `source`, `source_hash`, `stale`, `test_valid`» (код, незакоммиченные изменения)
**Версия:** рабочее дерево (незакоммичено) на HEAD `50211e5`; изменённые файлы — `src/core.rs`, `src/lib.rs`, `src/mcp.rs`, новый `tests/mcp_draft.rs`
**Вердикт:** принято

**P1:** —
**P2:** —
**P3:** —

**Проверки:**
- `cargo fmt --check` → чисто (без вывода).
- `cargo clippy --all-targets -- -D warnings` → `Finished`, без предупреждений.
- `cargo test --all` → **78 passed** (43 lib + 0 main + 4 features_inventory + 8 mcp_draft + 12 publish + 11 rest), 0 failed.

**Что проверено и ок:**
- **Карточка T-01** (`docs/tasks/T-01-draft-source-hash/README.md`) — «Что сделать» и «Критерий готовности» покрыты кодом; источники Q12/Q29 корректны (Q12/Q29 существуют и решены в `docs/OPEN_QUESTIONS.md`), Q28 в источниках не заявлен (это T-03).
- **`Draft` хранит `source`/`source_hash`** (`src/lib.rs`: `source`, `source_hash` c `#[serde(default)]`, `make_draft` вычисляет `source_hash = sha256:<hex>` из текста; `src/core.rs` — `sha256_of`/`source_hash`).
- **Инвариант 2** (§4.5): `draft_json` (`src/mcp.rs:267-289`) и `list_drafts` отдают только рендеренные строки; внутренний `rule` не публикуется (юнит `create_stores_source_and_hash_q12` и интеграционный `get_draft_canonical_fields_and_no_internals_q29_inv2_inv5` проверяют отсутствие `rule`/`condition_`/`action`).
- **Инвариант 3** (`stale`): `is_stale` (`src/lib.rs:718-729`) — нет файла → `false`, хэши равны → `false`, различаются → `true`; хэш по байтам (бинарный `.dar` не падает). `stale` не блокирует `check.test` (комментарий `src/mcp.rs:103-105`, тест `stale_true_when_dar_hash_differs_q29_inv3`).
- **Инвариант 4** (`test_valid`): `draft_json` вычисляет `test_valid = (last_test_checksum == source_hash)` через `Option` (null → `false`), что эквивалентно `last_test_checksum != null && … == source_hash` (§4.5/Q29). `check.test` фиксирует `last_test_checksum`/`tested_at`; перезапись сохраняет метку, но `test_valid` становится `false` при смене текста (`make_draft` сохраняет `last_test_checksum` из existing).
- **Инвариант 5**: поля `size`/`format` не вводятся (проверяется в юнит- и интеграционных тестах).
- **Сценарии приёмки** (6 из `draft.feature` + «Успешный check.test фиксирует метку» из `test_draft.feature`) не противоречат коду; `tests/features_inventory.rs` проходит (счётчики README согласованы).
- **Обратная совместимость**: старый `sandbox.json` без новых полей грузится без падения (`old_sandbox_entry_without_new_fields_loads`, `old_sandbox_entry_loads_and_is_readable`); обязательность обратной совместимости не требуется (примечание карточки).
- **Новые тесты значимы, а не «зеленеют сами»**: `tests/mcp_draft.rs` гоняет реальный бинарник по MCP-stdio (`env!("CARGO_BIN_EXE_credo2")`), а не приватный `dispatch`; сверены 3 теста с инвариантами — `get_draft_canonical_fields_and_no_internals_q29_inv2_inv5` (инв. 2/5, формат `sha256:<hex>`), `stale_empty_and_binary_dar_q29_inv3` (инв. 3, байтовый хэш), `test_records_checksum_and_invalidates_on_change_q29_inv4` (инв. 4).

**Оценка scope-пересечения с T-03** (ответ `check.create` → `{status, name}`): допустимое поглощение, не противоречие. Контракт `{status: ok, name}` — канон Q28 (п. 4) и Q29 §4.5 (схема `check.create`), поэтому выравнивание ответа корректно независимо от того, какая задача его делает. Остаток T-03 (обязательность `name`, сверка `name` с заголовком) не тронут и в T-01 не заявлен — это не мешает целям T-01. Не находка.

**Вне-объёмные факты — подтверждено, что в T-01 не заявлены как сделанные:**
- `delete_draft` возвращает неканонический конверт (`{status: "deleted"|"not_found", name}` вместо `{status:"ok", deleted:bool}`) — `src/mcp.rs:134-145`, не изменялось в этом diff; задача T-05.
- `check.create` не требует и не сверяет входной `name` (читает только `source`, имя берёт из заголовка) — остаток T-03.
- Тест stale-черновика исполняет текст черновика (`evaluate_rule(&d.rule, …)`), а не текст файла — вне списка приёмки T-01; сценарий «Тестирование устаревшего черновика» (`test_draft.feature:48-54`, строка «выполняется по тексту файла») в критерий готовности T-01 не входит и остаётся расхождением канона/кода вне этой задачи.
