# Приёмка (адресная, r4): служебный коммит очистки логов — правило-исключение

**Проверка:** адресная проверка по порогу существенности перед служебным
коммитом очистки логов. Периметр (vs HEAD `f31d5a3`):
- `.opencode/rules/git-workflow.md` §«Подтверждение и очистка логов» — добавлено
  исключение: служебная очистка коммитится прямо в `develop`; если пакет удаляет
  саму ленту — два шага (коммит подтверждения → коммит очистки), `git` сверяет
  подтверждение из `HEAD`;
- `.opencode/mail/service-git-flow.md` — дописана запись `lead` «пакет подтверждён
  (очистка логов)»;
- `.opencode/memory/git.md`, `.opencode/memory/validator.md` — сброшенные памяти.
**Версия:** HEAD `f31d5a3 chore: git-flow с ветками задач и модели ролей на flash`
+ рабочее дерево; снимок 2026-09-26.
**Вердикт:** принято.

## Находки

**P1:** — критичных проблем нет.

**P2:** — нет.

**P3:** — нет.

## Проверки

- `git diff .opencode/rules/git-workflow.md` → только добавленные строки
  (117–121), ничего не удалено и не переписано; правка — в конце
  §«Подтверждение и очистка логов».
- `git status --porcelain` → ровно `M git-workflow.md`, `M memory/git.md`,
  `M memory/validator.md`, `M mail/service-git-flow.md`; untracked-файлов нет.
- `git status --porcelain -- src tests Cargo.toml Cargo.lock` → пусто.
- `git log -3 --oneline` → `f31d5a3` (HEAD), `5b6f978`, `c2ed297`.
- `git diff .opencode/mail/service-git-flow.md` → к ленте добавлена только запись
  `lead` «пакет подтверждён (очистка логов)» (+13 строк, в конце); остальное
  содержимое совпадает с `HEAD`.
- `git diff --stat .opencode/memory/git.md .opencode/memory/validator.md` →
  только удаления (git.md −10/+2, validator.md −44/+2) — памяти сброшены к
  шаблону, как и заявлено в пакете.

## Согласованность

- **Исключение без противоречия:** `git-workflow.md:26` («Прямые коммиты в
  `master` и `develop` запрещены: работа — в `feature/<идент>`)» — про *работу*;
  новое правило (`git-workflow.md:117–118`) прямо оговаривает, что «правило
  “ветка на изменение” на рабочие данные не распространяется». Это явный
  адресный carve-out для служебной очистки, а не тихая отмена запрета; оба
  положения живут в одном правиле и читаются вместе.
- **`AGENTS.md`:** §Git (строки 290–297) делегирует ограничения
  `git-workflow.md` («Модель ветвления, сценарии и ограничения — …»), своего
  запрета прямых коммитов не дублирует → противоречия нет. §Память и почта
  (строки 112–113) — сервисные ленты `service-<тема>.md` и
  `node .opencode/scripts/clean-logs.mjs`: согласовано.
- **`clean-logs.mjs`:** путь бэкапа `join(tmpdir(), 'opencode',
  'logs-backup-<ts>')` (`clean-logs.mjs:117`) совпадает с формулировкой правила
  `<temp>/opencode/logs-backup-<ts>/` (`git-workflow.md:114`).
- **Двухшаговый протокол** (правило 119–121) совпадает с пакетом из ленты
  (`service-git-flow.md:111–116`): коммит 1 — правило + запись подтверждения,
  коммит 2 — сброс памяти + удаление ленты; `git` сверяет подтверждение из
  истории (`git show HEAD:<лента>`) — команда `git show *` есть в правах `git`.

## Что проверено и ок

- Правка `git-workflow.md` — только добавление, формулировка непротиворечива и
  явна; исключение согласовано с §Ограничения и §Структура git-flow.
- Периметр совпадает с заявленным; `src`, `tests`, `Cargo.*` чисты; фичи/код не
  затронуты.
- Лента отличается от `HEAD` только записью подтверждения; памяти сброшены
  (только удаления).
- Согласованность `git-workflow.md` ↔ `AGENTS.md` §Git/§Память и почта ↔
  `clean-logs.mjs` сохранена.

**Почему без полного DoD и `features_inventory`:** изменение — только текст
правила (`git-workflow.md`) и рабочие данные ролей (лента, памяти); `src/**`,
`tests/**`, `Cargo.*` и `docs/features/**` не менялись
(`git status --porcelain -- src tests Cargo.toml Cargo.lock` → пусто). На текст
правила компиляция/тесты не влияют; `features_inventory` фич не касается.

**Примечание (процесс, не находка):** настоящий отчёт `…-r4.md` создаётся
после формирования подтверждённого пакета и в его два коммита не входит —
после операции очистки он останется untracked, поэтому строка ленты «дерево
чистое» (`service-git-flow.md:117`) будет неточной. Решение за `lead`: включить
отчёт в коммит 1 либо оставить его вне пакета (на содержимое коммитов не
влияет).
