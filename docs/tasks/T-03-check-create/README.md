# T-03. `check.create`: `{name, source}`

- **Статус:** ✅ сделана
- **Приоритет:** P1
- **Зависит от:** —
- **Источник:** Q28 (SPEC §10 №31).
- **Отчёт приёмки:** [`../../reviews/T-03-2026-09-26.md`](../../reviews/T-03-2026-09-26.md).

## Что сделать

- MCP-инструмент принимает оба параметра (обязательны).
- `name` сверяется с заголовком `Правило {name}` (`../GRAMMAR.md`).
- Ответ: `{"status":"ok","name":...}`.
- Повторный вызов перезаписывает черновик («сохранить = обновить»; upsert
  уже есть).

## Критерий готовности

Сценарии `../../features/draft.feature` («Создание черновика через MCP»,
«Повторный check.create перезаписывает черновик», «check.create с
невалидным source отклоняется») и `../../features/agent_minimal.feature`.
