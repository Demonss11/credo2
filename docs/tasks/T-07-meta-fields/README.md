# T-07. `meta.json`: `display_name`, `source_hash`, `compiler_version`

- **Статус:** ⬜ открыта
- **Приоритет:** P2
- **Зависит от:** —
- **Источник:** Q13, Q7 («Следствие для кода» в Q13).

## Что сделать

- Расширить `CheckMeta` (`src/core.rs`) и запись в `lib.rs`:
  `display_name`, `source_hash` исходного текста,
  `compiler_version` (`env!("CARGO_PKG_VERSION")` или версия компилятора).

## Критерий готовности

Сценарий `../../features/publish.feature` («Публикация сохраняет
метаданные»).

## Design note (решить при реализации)

Q13 требует латинский `name`, но v0.1 допускает кириллические имена правил;
откуда брать `display_name` и обязательна ли латиница для опубликованного
`name` — уточнить; при необходимости — отдельное решение.
